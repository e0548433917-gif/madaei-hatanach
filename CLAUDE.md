# הוראות עבודה — עינים למקרא

## בסיום כל שיחת עבודה: לסגור מעגל תיעוד

בכל שיחה שבה תוקן/בוצע משהו בפועל (קוד, דאטה, תוכן) — **לפני סיום השיחה**:

1. **לזהות מה בוצע בפועל** בשיחה הזו (לא רק מה שנערך בקבצים — מה שבאמת *הושלם*,
   בניגוד לניסיון/בדיקה שלא נגמר).
2. **ROADMAP.md** — לבדוק אם הפריט (או תת-פריט) שבוצע מופיע שם. אם כן: למחוק אותו,
   או לצמצם את הבולט אם רק חלק ממנו בוצע (השאר הפריט הרחב, מחקו רק את מה שנסגר —
   ר׳ התקדים ד.1א/ד.1ב מול ד המלא ב-CHANGELOG.md). **אל תשאירו פריט גמור ברשימה** —
   זה בדיוק מה שהקובץ עצמו אומר על עצמו בשורה 5: "כשמשהו יוצא בפועל — המקום שלו
   הוא ב-CHANGELOG, לא כאן".
3. **מסמכי המקור** (docs/מפרט-עינים-למקרא-4.0.md, docs/סבבים/טבלת-סבבים.md,
   docs/תוכנית-תוכן-עתידית.md) — אם הפריט מתועד שם עם קוד סעיף (ד.1א וכו׳), לסמן
   אותו כבוצע (✅ / הערה מפורשת), לא רק למחוק מ-ROADMAP.
4. **CHANGELOG.md** — לא לכתוב ידנית. הוא נכתב אוטומטית ב-`build/pack.ps1` בזמן
   האריזה (`-Title`/`-Notes`, או נגזר מכותרות הקומיטים). לוודא רק שהקומיטים/ה-
   Notes של האריזה הבאה באמת מתארים את מה שבוצע.

**מתי לא נדרש:** שיחת מחקר/בדיקה בלבד בלי שינוי בפועל, או תכנון-בלבד (עדכון docs/
בלי לסגור פריט קיים ב-ROADMAP).

**איך זה נאכף באריזה:** `build/pack.ps1` מטביע את ROADMAP.md כקבוע JS
(`guides/_shared/roadmap-embedded.js`) בכל אריזה — כלומר פריט שנשאר שם למרות
שבוצע **יוצג בפועל למשתמשים** בלשונית "מה חדש" → "מה מתוכנן בהמשך", לא רק עניין
תיעודי פנימי.

## 🔴 כמה אנשים עובדים על הריפו הזה במקביל — קרא לפני שאתה נוגע בקוד

הריפו אינו של מפתח יחיד. נכון ל-30/08/2026 דחפו אליו בפועל `chadbedera`,
`ypl` ו-`palmoni5`, ויש עוד מוזמנים ממתינים. שניים יכולים לעבוד באותה שעה
על אותם קבצים.

**לכן, בכל תחילת עבודה ולפני כל דחיפה:**

```bash
git fetch origin main && git log --oneline HEAD..origin/main
```

אם יצא משהו — **`git pull --rebase` לפני שממשיכים**, לא אחרי. דחיפה על בסיס
ישן נדחית, ו-rebase באמצע עבודה עם שינויים לא־מחויבים הוא בדיוק המקום שבו
מאבדים קוד.

⚠️ **שתי מלכודות שכבר קרו כאן בפועל:**

1. **הענף המקומי אינו בהכרח `main`.** בקלון הזה העבודה נעשית על ענף `reconcile`
   שה-upstream שלו הוא `origin/main`. `git push` סתם נכשל עם
   "cannot resolve 'simple' push to a single destination" עד שהוגדר
   `git config push.default upstream`. אל תניח שאתה על main — `git branch --show-current`.
2. **`main` המקומי היה ענף נטוש** שפיגר ב-47 קומיטים ונשא 3 קומיטים ישנים
   משלו. `git checkout main` היה מחזיר עץ עבודה מגרסה 2.20.22. אל תעבור
   לענף לפי שם בלבד — בדוק `git log --oneline -1` מול `origin/main`.

**אחרי `pack.ps1 -Release`:** הוא דוחף בעצמו. אם מישהו הספיק לדחוף בינתיים,
השחרור ייעצר באמצע — עם תג שאולי כבר נוצר. לבדוק `git tag --list` לפני ניסיון
חוזר, ולא לארוז שוב תחת אותו מספר גרסה (כלל 2.11.6 למטה).

## מלכודות ידועות (אל תחזרו עליהן)

* **`fetch()` של קובץ מקומי מהחבילה (CHANGELOG.md / ROADMAP.md / manifest.json)
  נכשל בפועל בתוך ה-WebView של אוצריא**, גם שהקובץ ארוז וקיים. הפתרון הקבוע בפרויקט:
  להטביע את התוכן כקבוע JS בזמן האריזה (`guides/_shared/*-embedded.js`,
  ר׳ `build/pack.ps1` שלבים 1ג/1ד/1ה) ולקרוא מהקבוע — לא fetch בזמן ריצה. כל
  תכונה חדשה שצריכה קרוא קובץ-מקומי-מהחבילה צריכה ללכת באותו דפוס, לא להמציא
  fetch חדש.
* גרסת אוצריא עצמה (לא של התוסף) — `Otzaria.call('app.getInfo')` לא תמיד עוטף
  את התשובה ב-`.data` (ר׳ com.software_bug_report.newplugin, התוסף הרשמי לדיווח
  באגים, ששולף גם `res?.data || res`). לטפל בשתי הצורות, לא רק `res.data.version`.
* גרסה שכבר שוחררה (יש לה tag `v<version>` בגיטהאב) אסור לארוז שוב תחתיה
  (כלל 2.11.6 — ר׳ ההערה בראש `build/pack.ps1`). לבדוק `git tag --list` לפני
  שמניחים מה המספר הבא.
