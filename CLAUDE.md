# הוראות עבודה — עינים למקרא

## ⭐ בתחילת כל שיחה: מה חדש ומי עובד ברקע — לפני שנוגעים בקוד

**ההוראות והתוכניות מתעדכנות על ידי כמה עובדים במקביל.** מי שמתחיל לעבוד לפי
תמונת מצב בת יממה — כותב על עבודה של מישהו אחר, או חוזר על מה שכבר נעשה. לכן
**הרצף הזה הוא הדבר הראשון בכל שיחה**, גם ב״תיקון קטן״:

```bash
git fetch --all --prune
git branch --show-current              # אתה לא בהכרח על main — ר' מלכודת 1 למטה
git log --oneline HEAD..origin/main    # מה נדחף מאז הפעם הקודמת
git status --short                     # ⚠️ שינויים לא-מחויבים = מישהו עובד כאן עכשיו
git branch -r                          # ענפי עבודה פתוחים של אחרים
git tag --list | tail -5               # מה כבר שוחרר (כלל 2.11.6)
gh pr list ; gh issue list             # דיווחים אמיתיים ממשתמשים
head -40 CHANGELOG.md                  # מה יצא בגרסאות האחרונות ומה תוקן
```

**איך קוראים את התוצאה:**

* `HEAD..origin/main` לא ריק → **`git pull --rebase` לפני שממשיכים**, לא אחרי.
* `git status --short` מראה קבצים משתנים שאינם שלך → **עובד אחר נמצא בעץ העבודה
  הזה ברגע זה.** אל תקמט אותם ואל תדרוס אותם: `git add <רק הקבצים שלך>`, לעולם
  לא `git commit -a`. אם אתה חייב לגעת באותו קובץ — הוסף בסופו/בקטע נפרד, אל
  תשכתב קטע שהוא באמצע העריכה שלו.
* ענף שאינך מכיר ב-`git branch -r` → מישהו עובד עליו. אל תמזג אותו ואל תתייחס
  אליו כזנוח בלי לשאול.

## מדיניות ענפים

**ברירת המחדל המומלצת: ענף לכל עובד, ומיזוג ל-`main` בסוף יחידת עבודה.**

```bash
git switch -c work/<נושא-קצר>     # למשל work/map-search
# ...עבודה + קומיטים...
git fetch origin main && git rebase origin/main
git switch main && git merge --ff-only work/<נושא> && git push
```

כך שני עובדים באותה שעה לא דורסים זה את זה, וכל יחידת עבודה ניתנת לבדיקה או
לביטול בשלמותה. **חריג מותר** (וכך נעשה למשל ב-31/08/2026, לבקשת בעל הפרויקט):
עבודה ישירות על `main` — ורק בתנאי `git pull --rebase` לפני **כל** דחיפה,
וקימוט של הקבצים שלך בלבד.

## 🧹 לסיים נקי: אסור להשאיר עבודה לא-מקומטת

**כל שיחה שנגעה בקוד נסגרת בעץ עבודה נקי.** `git status --short` בסוף השיחה
צריך להיות ריק משינויים שלך. עובד שמשאיר אחריו קבצים משתנים בלי קומיט — הבא
אחריו נתקע (`git pull --rebase` נכשל), לא יודע אם זה עבודה חיה או שריד, ולא
יכול לגעת באותם קבצים. **זה בדיוק הבלגן שקרה כאן ב-31/08/2026.**

הרצף בסוף כל שיחה, בקצרה — **בלי בדיקות מקיפות, זה אמור לקחת דקה:**

```bash
git status --short                       # מה השארתי
git add <רק הקבצים ששיניתי>              # לא -A, ולא commit -a
git commit -m "<נושא>: <מה נעשה>"
git pull --rebase && git push
git status --short                       # חייב לצאת ריק משינויים שלך
```

* **קבצים משתנים שאינם שלך** — משאירים בשקט. הם של עובד אחר.
* **קבצי עבודה זמניים שיצרת** (טיוטות, פלט סקריפטים, גיבויים) — למחוק או
  להוסיף ל-`.gitignore`. לא להשאיר `??` בשורש הריפו.
* **קומיטים לפי נושא**, לא קומיט ענק אחד. כותרת בעברית, בלשון ״מה נעשה״.
* אין דרישה להריץ בדיקות או ולידציה כדי לסגור — רק לא להשאיר לכלוך.

## בסיום כל שיחת עבודה: לסגור מעגל תיעוד

**כל דבר שנעשה בפועל — חייב להיות מתועד בשלושת המקומות שבטבלה.** מותר לגמרי
להעתיק ניסוח ממקום למקום; אף אחד לא מבקש שלושה טקסטים שונים. מה שנדרש הוא
ששלושתם יהיו **מעודכנים ומסודרים**, ושלא ייווצר מצב שפריט גמור עדיין מופיע
כמתוכנן.

| # | איפה | מה כותבים שם | מי כותב |
|---|---|---|---|
| 1 | **`ROADMAP.md`** | מוחקים את הפריט שבוצע, או מצמצמים אותו אם רק חלק ממנו נסגר | ידנית, לפני סוף השיחה |
| 2 | **`הוראות-ותכנונים-למפתח-מצורף.md`** | **מסמנים ✅ על ההוראה עצמה, במקום שבו היא כתובה** — עם תאריך ומספר גרסה. לא מוחקים את ההוראה: מסמנים שקוימה | ידנית, לפני סוף השיחה |
| 3 | **`CHANGELOG.md`** ← לשונית **״מה חדש״** שבתוסף | תיאור למשתמש: מה השתנה עבורו | **אוטומטי** ב-`build/pack.ps1` (`-Title`/`-Notes`, או נגזר מכותרות הקומיטים). **לא לכתוב ידנית** |

**+ מסמכי המקור:** אם הפריט מתועד ב-`docs/מפרט-עינים-למקרא-4.0.md`,
`docs/סבבים/טבלת-סבבים.md` או `docs/תוכנית-תוכן-עתידית.md` עם קוד סעיף
(ד.1א וכו׳) — לסמן שם ✅ גם כן, לא רק למחוק מ-ROADMAP.

**למה זה לא רק בירוקרטיה:** `build/pack.ps1` מטביע את `ROADMAP.md` ואת
`CHANGELOG.md` כקבועי JS (`guides/_shared/*-embedded.js`) **בכל אריזה**. כלומר
(1) ו-(3) הם בדיוק מה שהמשתמשים רואים בלשונית ״מה חדש״ ובקטע ״מה מתוכנן
בהמשך״. פריט שכבר בוצע ונשאר ב-ROADMAP הוא **באג גלוי למשתמש**, לא עניין
תיעודי פנימי.

**מתי לא נדרש:** שיחת מחקר/בדיקה בלבד בלי שינוי בפועל, או תכנון-בלבד (עדכון
`docs/` בלי לסגור פריט קיים ב-ROADMAP).

**⚠️ לפני שמוחקים משימה מרשימה כלשהי — לשאול את בעל הפרויקט.** משימה שנראית
מיותרת עשויה להיות משימה שנדחתה בכוונה, לא כזו שבוטלה.

## 🔴 כמה אנשים עובדים על הריפו הזה במקביל

הריפו אינו של מפתח יחיד. נכון ל-31/08/2026 דחפו אליו בפועל `chadbedera`,
`ypl` ו-`palmoni5`, ויש עוד מוזמנים ממתינים. שניים יכולים לעבוד באותה שעה
על אותם קבצים — וזה כבר קרה בפועל (31/08: שתי סדרות עבודה נפרדות רצו על אותו
עץ עבודה, אחת מהן על `shell/personal.js` ללא קומיט).

⚠️ **שלוש מלכודות שכבר קרו כאן בפועל:**

1. **הענף המקומי אינו בהכרח `main`.** בקלון הזה העבודה נעשית על ענף `reconcile`
   שה-upstream שלו הוא `origin/main`. `git push` סתם נכשל עם
   "cannot resolve 'simple' push to a single destination" עד שהוגדר
   `git config push.default upstream`. אל תניח שאתה על main — `git branch --show-current`.
2. **`main` המקומי היה ענף נטוש** שפיגר ב-47 קומיטים ונשא 3 קומיטים ישנים
   משלו. `git checkout main` היה מחזיר עץ עבודה מגרסה 2.20.22. אל תעבור
   לענף לפי שם בלבד — בדוק `git log --oneline -1` מול `origin/main`.
3. **`git pull --rebase` נכשל כשעץ העבודה מלוכלך** ("cannot pull with rebase:
   You have unstaged changes"). אם הלכלוך אינו שלך — **אל תעשה stash ואל תקמט
   אותו.** זו עבודה של מישהו אחר. עבוד סביבו וקמט רק את הקבצים שלך.

**אחרי `pack.ps1 -Release`:** הוא דוחף בעצמו. אם מישהו הספיק לדחוף בינתיים,
השחרור ייעצר באמצע — עם תג שאולי כבר נוצר. לבדוק `git tag --list` לפני ניסיון
חוזר, ולא לארוז שוב תחת אותו מספר גרסה (כלל 2.11.6 למטה).

## מלכודות ידועות (אל תחזרו עליהן)

* **`fetch()` של קובץ מקומי מהחבילה (CHANGELOG.md / ROADMAP.md / manifest.json)
  נכשל בפועל בתוך ה-WebView של אוצריא**, גם שהקובץ ארוז וקיים. הפתרון הקבוע בפרויקט:
  להטביע את התוכן כקבוע JS בזמן האריזה (`guides/_shared/*-embedded.js`,
  ר׳ `build/pack.ps1` שלבים 1ג/1ד/1ה) ולקרוא מהקבוע — לא fetch בזמן ריצה. כל
  תכונה חדשה שצריכה קרוא קובץ-מקומי-מהחבילה צריכה ללכת באותו דפוס, לא להמציא
  fetch חדש.
* **`ConvertTo-Json` ב-PowerShell 5.1 עוטף מחרוזת שמוזרמת ב-pipe** כאובייקט
  `{"value":…}` במקום כמחרוזת. זה שבר את אריזת 3.3.0 (לשונית ״מה חדש״ קרסה).
  בכל הטבעה: `ConvertTo-Json -InputObject ([string]$x) -Compress`, לא `$x | ConvertTo-Json`.
* גרסת אוצריא עצמה (לא של התוסף) — `Otzaria.call('app.getInfo')` לא תמיד עוטף
  את התשובה ב-`.data` (ר׳ com.software_bug_report.newplugin, התוסף הרשמי לדיווח
  באגים, ששולף גם `res?.data || res`). לטפל בשתי הצורות, לא רק `res.data.version`.
* גרסה שכבר שוחררה (יש לה tag `v<version>` בגיטהאב) אסור לארוז שוב תחתיה
  (כלל 2.11.6 — ר׳ ההערה בראש `build/pack.ps1`). לבדוק `git tag --list` לפני
  שמניחים מה המספר הבא.
* **CSS לא-תקין חוסם פרסום לחנות של אוצריא** — לא רק מוריד את תגית ״מראה תואם
  לאוצריא״. ר׳ `docs/מפרט-עיצוב-אוצריא.md`. כל תוספת CSS נכתבת בזהירות תחבירית.
