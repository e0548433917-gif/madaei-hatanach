# חבילות בנויות — 576MB של גרסאות ישנות. נבנות מהמקור, אין סיבה לעקוב אחריהן.
# גרסאות שמופצות בפועל יעלו כ-Release בגיטהב, לא כקובץ בריפו.
dist/

# אריחי מפה אופליין (OSM) — אופציונליים, מוכנסים ידנית ע״י מי שרוצה (ר' openOfflineMapHelp
# ב-guides/places/js/app.js). לא נכללים בחבילה כברירת מחדל, ולכן גם לא בריפו.
guides/places/tiles/

# זמניים
*.tmp
*.log
.DS_Store
Thumbs.db
desktop.ini
