# ברוך הבא לפיתוח ״עינים למקרא״ 🎉

**נשלחה אליך הזמנת שיתוף לריפו** (הרשאת `push` — בלי Pull Request, דחיפה ישירה).
היא ממתינה לאישורך ב-<https://github.com/e0548433917-gif/madaei-hatanach/invitations>
או בקישור שהגיע למייל. **צריך לאשר אותה לפני שתוכל לדחוף.**

> 📄 **הקובץ הזה הוא מקור האמת של הודעת הקבלה.** הוא נשלח אוטומטית כתגובה לכל
> בקשת הצטרפות (`.github/workflows/join-request.yml`), וגם מודבק ידנית על ידי מי
> שמטפל בבקשה. **עריכה כאן = שינוי ההודעה לכל מי שיצטרף מכאן והלאה** — אין צורך
> לגעת ב-workflow.

---

## ⚠️ קרא את זה קודם — כאן עובדים כמה אנשים במקביל

הריפו אינו של מפתח יחיד. שניים יכולים לערוך את אותו קובץ באותה שעה, וזה כבר קרה
בפועל יותר מפעם אחת. ארבעה כללים שמונעים דריסה:

1. **`git status --short` לפני שנוגעים בכלום.** שינויים שאינם שלך = **מישהו עובד
   בעץ הזה ברגע זה.** אל תקמט אותם, אל תעשה להם `stash`, אל תדרוס. עבוד סביבם.
2. **לעולם לא `git add -A` ולא `git commit -a`.** רק `git add <הקבצים שלך>`,
   בשמות מפורשים.
3. **`git pull --rebase` לפני כל `push`**, לא אחרי שנכשל.
4. **ענף משלך:** `git switch -c work/<נושא-קצר>`, ומיזוג ל-`main` בסוף יחידת עבודה.

הפירוט המלא — כולל שלוש מלכודות שכבר עלו כאן ביוקר (הענף המקומי אינו בהכרח
`main`; `main` המקומי עלול להיות נטוש ולפגר בעשרות קומיטים; `pull --rebase` נכשל
על עץ מלוכלך) — נמצא ב-[`CLAUDE.md`](../CLAUDE.md), בסעיפים ״בתחילת כל שיחה״,
״מדיניות ענפים״ ו״מלכודות ידועות״. **חובה לקרוא לפני הקומיט הראשון.**

---

## 🔑 מה מותר בלי אישור, ומה דורש אישור מנהל

הפרויקט מתוחזק על ידי **מנהלי הארגון** (`GINIM-LMKRA`). לכל מפתח יש הרשאת `push`,
והכלל פשוט:

| | דוגמאות | אישור? |
|---|---|---|
| ✅ **ביצוע משימה קיימת** | פנייה בתווית `תכנון שדרוג` · פריט מ-`ROADMAP.md` · תיקון באג פתוח · הרחבת תוכן במדריך קיים · תיקון טעות בנתונים | **לא נדרש.** קח, בצע, דחוף |
| 🔴 **שינוי מהותי** | שינוי ארכיטקטורה או מבנה תיקיות · מחיקה/צמצום של פיצ׳ר קיים · שינוי ב-`docs/מפרט-עינים-למקרא-4.0.md` · **מחיקת משימה** מ-`ROADMAP.md` · `manifest.json` (הרשאות, `network.allowlist`) · כל דבר ב-`.github/workflows/` · ממסר הדיווחים, טוקנים וסודות · **אריזה ושחרור גרסה לחנות** (`pack.ps1 -Release`) · שינוי שם או מיתוג | **כן — מאחד המנהלים** |

**איך מבקשים אישור:** פותחים Issue עם התווית `דרוש-אישור`, מתייגים מנהל, וממשיכים
בינתיים למשימה הבאה. **לא עוצרים ולא מחכים** — זה הכלל החוצה את כל הפרויקט.

**בספק — זה מהותי.** שינוי שקשה להחזיר לאחור, שנוגע במה שמשתמשים כבר רואים, או
שסותר מסמך מחייב — שואלים לפניו, לא אחריו.

---

## 📋 תור המשימות — מה לוקחים, ובאיזה סדר

**הסדר קבוע. מפתח חדש (או מפתח שסיים יחידת עבודה) לוקח את הפריט הראשון בתור
שאיש עוד לא התחיל.**

> ## 👈 קודם כול: רשימת המשימות היא **ב-Issues**, לא במסמכים
> 
> ```bash
> gh issue list --label "תכנון שדרוג" --state open --limit 100
> ```
> 
> [כל התכנון הפתוח, 53 פניות](https://github.com/e0548433917-gif/madaei-hatanach/issues?q=is%3Aopen+label%3A%22תכנון+שדרוג%22) — **כל פריט תכנון של הפרויקט פוצח לפנייה משלו** (02/09/2026).
> כל פנייה מכילה את מה שצריך כדי להתחיל: מה נעשה, מה נשאר, באילו קבצים, ומה חוסם.
> **תלויות מסומנות במספר הפנייה החוסמת** — פנייה שכתוב בה "חסום עד #NN" לא מתחילים לפניה.
> 
> המסמכים (`ROADMAP.md` ושאר `docs/`) הם **הפירוט וההקשר**, וליד כל פריט בהם רשום מספר הפנייה שלו. הפנייה היא תור העבודה; המסמך הוא הרקע.

| # | המשימה | איפה מוגדרת |
|---|---|---|
| 1 | **סבב 3 — מנוע הזיהוי v2** (השדרוג הגדול) | [`ROADMAP.md`](../ROADMAP.md) §״סבב 3״ · [`docs/סבבים/טבלת-סבבים.md`](סבבים/טבלת-סבבים.md) · [`docs/הערות-תוכן-ממשתמשים.md`](הערות-תוכן-ממשתמשים.md) |
| 2 | ✅ **עיצוב — הושלם** (v3.5.0, 02/09/2026). נשאר רק **נספח ב׳** — בורר גופנים מסונן + בורר תצוגת שם ה׳ | פנייה **#58** · [`docs/מפרט-עיצוב-אוצריא.md`](מפרט-עיצוב-אוצריא.md) נספח ב׳ · כללי עיצוב לתוספים: [`docs/הנחיות-עיצוב-לתוספי-אוצריא.md`](הנחיות-עיצוב-לתוספי-אוצריא.md) |
| 3 | **סבב ת׳ — תוכן ונתונים** (אושר מראש, אחרי סבב 3) | [`docs/תוכנית-תוכן-עתידית.md`](תוכנית-תוכן-עתידית.md) |

**מה פתוח כעת:** משימה 1 (מנוע הזיהוי, #38–#40) היא הראשונה בתור. במקביל אפשר
לקחת כל פנייה אחרת שאיש לא התחיל — במיוחד באגים (#41–#46) ותוכן (סבב ת׳), שאינם
נוגעים באותם קבצים כמו מנוע הזיהוי.

### ⚠️ לפני שנועלים ליין — לבדוק מה כבר תפוס

**שניים על אותה משימה = עבודה כפולה שאחת ממנה תימחק.** לפני שמתחילים:

```bash
git branch -r                       # ענף work/<נושא> פתוח = מישהו עליה
git log --all --oneline -25         # קומיטים אחרונים בכל הענפים
gh issue list --label "תכנון שדרוג" --state open --limit 100   # מי כבר משויך/הצהיר
```

ואז **להצהיר בפנייה עצמה** — לא לפתוח פנייה חדשה, כי כבר יש פנייה לכל משימה:

```bash
gh issue comment <מספר הפנייה> --body "עובד על זה — <שם> · ענף work/<נושא> · החל <תאריך>"
gh issue edit <מספר הפנייה> --add-assignee "@me"
```

**המשימה תפוסה? קח את הבאה בתור. אל תיתקע ואל תחכה** — אם יש חפיפה חלקית, השתלב:
קח תת-משימה נפרדת מתוך אותו סבב, ותאם ב-Issue של מי שכבר עליה. עצירה והמתנה
לתשובה היא הדבר היחיד שאסור כאן.

---

## איך עובדים כאן עם קלוד

העבודה מתנהלת בשיחות קלוד, ויש לזה שני בלוקים קבועים:

* **בלוק A** — פותחים איתו שיחה ראשונה. פעם אחת.
* **בלוק B** — קלוד מפיק אותו בשבילך **בסוף כל שיחה**, מעודכן, ואתה מדביק אותו
  בשיחה הבאה. הוא כולל את ההוראה להפיק את עצמו שוב, ולכן הלולאה סוגרת את עצמה —
  אינך צריך לכתוב שום דבר מחדש, אף פעם.

**מודל מומלץ:** Sonnet 5 לעבודת תוכן ולתיקונים; Opus 5 לתכנון, לארכיטקטורה או
לבאג שלא נכנע.

---

## בלוק A — פרומט פתיחה (להדביק בשיחת קלוד חדשה, פעם אחת)

```text
אני מצטרף כמפתח לפרויקט "עינים למקרא" — תוסף לתוכנת אוצריא (ספריית לימוד
תורנית): מדריך מאוחד לתנ״ך/משנה/תלמוד עם זיהוי אחיד בלחיצה ימנית בטקסט.
ריפו: https://github.com/e0548433917-gif/madaei-hatanach
יש לי הרשאת כתיבה ישירה (collaborator/push) ואישרתי את ההזמנה.

⚠️ עובדים כאן כמה מפתחים במקביל, על אותם קבצים, באותה שעה.

── מה לעשות עכשיו, בסדר הזה ──
1. הרץ את רצף "בתחילת כל שיחה" מתוך CLAUDE.md:
   git fetch --all --prune · git branch --show-current · git log --oneline HEAD..origin/main
   · git status --short · git branch -r · git tag --list | tail -5
   · gh pr list · gh issue list · head -40 CHANGELOG.md
   אם HEAD..origin/main אינו ריק — git pull --rebase לפני שממשיכים.
2. קרא במלואם, לפני שנוגעים בקוד:
   • הוראות-ותכנונים-למפתח-מצורף.md  ← לסרוק מחדש בכל שיחה, הוא מתעדכן
   • CLAUDE.md                        ← כללי עבודה, מדיניות ענפים, מלכודות
   • docs/README.md ואז docs/מפרט-עינים-למקרא-4.0.md  ← המפרט המחייב; גובר על כל מסמך אחר
   • ROADMAP.md                       ← מה פתוח
3. ענף משלי: git switch -c work/<נושא-קצר>
   לעולם לא git add -A ולא git commit -a — רק הקבצים שערכתי, בשמות מפורשים.
   git pull --rebase לפני כל push.
   git status --short מראה שינויים שאינם שלי → עובד אחר נמצא כאן עכשיו: לא לגעת,
   לא stash, לעבוד סביבם.

── הליין שלי ──
⚠️ רשימת המשימות היא ב-Issues, לא במסמכים. הרץ קודם:
  gh issue list --label "תכנון שדרוג" --state open --limit 100
53 פניות (#38–#90) — כל פריט תכנון של הפרויקט פוצח לפנייה משלו. בכל פנייה כתוב
מה נעשה, מה נשאר, באילו קבצים, ומה חוסם. תלות מסומנת כ-"חסום עד #NN" — לא
מתחילים פנייה חסומה לפני שהחוסמת נסגרה.

סדר: 1. מנוע הזיהוי v2 (#38 → #39/#40) ראשון בתור.  2. במקביל — כל פנייה
פתוחה שאיש לא התחיל, במיוחד באגים (#41–#46) ותוכן (סבב ת'), שאינם נוגעים
באותם קבצים.  ✅ העיצוב הושלם ב-3.5.0; נשאר רק #58 (בורר גופנים + שם ה').

המסמכים (ROADMAP.md, docs/) הם הפירוט וההקשר — ליד כל פריט בהם רשום מספר
הפנייה שלו. הפנייה היא תור העבודה; המסמך הוא הרקע.

⚠️ לפני שאתה נועל ליין — בדוק מה כבר תפוס: git branch -r · git log --all --oneline -25 ·
ומי כבר משויך/הגיב בפניות. ואז הצהר **בפנייה עצמה**, אל תפתח חדשה:
gh issue comment <מספר> --body "עובד על זה — <שם> · ענף work/<נושא>"
gh issue edit <מספר> --add-assignee "@me"
המשימה תפוסה? קח את הבאה בתור, או תת-משימה נפרדת מתוך אותו סבב, ותאם בפנייה של מי שעליה. **אל תיתקע ואל תחכה — השתלב.**

סדר עדיפויות רוחבי: באג פתוח קודם לכול, גם אם הוא לא בתור. אחריו התור שלמעלה.
תעדף רצף עבודה ולא עצירות. שאלה שעולה באמצע → פתח GitHub Issue והמשך למשימה
הבאה שאינה תלויה בתשובה, במקום לעצור ולחכות.

── אחרי כל קטע עבודה משמעותי ──
commit+push לפי נושא (כותרת בעברית, בלשון "מה נעשה"), ואז סגירת מעגל תיעוד:
  • ROADMAP.md — מחיקת/צמצום הפריט שבוצע. ⚠️ לפני מחיקת משימה — לשאול את בעל הפרויקט.
  • הוראות-ותכנונים-למפתח-מצורף.md — ✅ + תאריך + גרסה על ההוראה עצמה, במקום שבו
    היא כתובה. לא למחוק אותה — לסמן שקוימה.
  • CHANGELOG.md — לא לגעת ידנית. הוא נכתב אוטומטית ב-build/pack.ps1.

── לסיים נקי ──
שיחה שנגעה בקוד נסגרת כש-git status --short ריק משינויים שלי. קבצי עבודה זמניים
שיצרתי — למחוק או להוסיף ל-.gitignore. לא להשאיר עבודה לא-מקומטת: זה חוסם את
העובד הבא.

── בתחילת השיחה ──
שאל אותי אם להריץ אוטומטית ברצף. אם כן — המשך ממשימה למשימה בלי לעצור, עד
שהשיחה נעשית ארוכה מדי (וחבל על הטוקנים), ואז עצור והפק בלוק המשך.

── בסוף כל שיחה (חובה) ──
(א) סיכום קצרצר של מה שבוצע בפועל.
(ב) מה נשאר בליין שלי, ומה השלב הבא המומלץ. **לפני שאתה ממליץ — בדוק בגיט מה
    כבר התחילו ומה לא** (git fetch · git branch -r · git log --all --oneline -25 ·
    gh issue list). אם הליין שלי נגמר — תן לי את הפריט הבא בתור המשימות שאיש
    לא התחיל. אם יש חפיפה חלקית — תן תת-משימה נפרדת, לא המתנה.
(ג) בלוק "המשך" מוכן להדבקה בשיחה חדשה, לפי התבנית שב-docs/הצטרפות-מפתח.md
    ("בלוק B"). מלא בו את השורות המשתנות, השאר את שאר המבנה כמו שהוא — כולל
    ההוראה שבסוף כל שיחה תפיק שוב בלוק המשך מעודכן. ציין באיזה מודל לפתוח
    ומה הצפי.
```

---

## בלוק B — תבנית המשך (קלוד מפיק אותה מעודכנת בסוף כל שיחה)

```text
המשך עבודה בפרויקט "עינים למקרא" (תוסף לאוצריא).
ריפו: https://github.com/e0548433917-gif/madaei-hatanach
⚠️ עובדים כאן כמה מפתחים במקביל, על אותם קבצים, באותה שעה.

1. הרץ את רצף "בתחילת כל שיחה" מ-CLAUDE.md (fetch · branch --show-current ·
   log HEAD..origin/main · status --short · branch -r · tag · gh pr/issue list ·
   head -40 CHANGELOG.md). HEAD..origin/main לא ריק → git pull --rebase.
2. סרוק מחדש את הוראות-ותכנונים-למפתח-מצורף.md — הוא מתעדכן בין שיחות.
3. git switch <הענף שלי> ; git pull --rebase
   רק הקבצים שלי. לעולם לא -A ולא commit -a. שינויים שאינם שלי — לא לגעת.

── איפה עצרנו ──
ענף:         <<קלוד ימלא>>
נושא נוכחי:  <<קלוד ימלא>>
כבר נעשה:    <<קלוד ימלא — קומיטים/קבצים>>
השלב הבא:    <<קלוד ימלא>>
חסימות / Issues פתוחים:  <<קלוד ימלא, או "אין">>
מה תפוס אצל אחרים:  <<קלוד ימלא — ענפים/הצהרות "עובד על:" שראה בגיט>>

── תור המשימות (כשהליין הנוכחי נגמר) ──
gh issue list --label "תכנון שדרוג" --state open --limit 100
53 פניות (#38–#90), כל פריט תכנון בנפרד. לוקחים את הראשונה שאיש לא התחיל.
תפוס → הבאה בתור. "חסום עד #NN" → לא מתחילים לפני שהחוסמת נסגרה.
**לא להיתקע ולא לחכות — להשתלב.**

── כללי עבודה ──
• סדר עדיפויות: באג פתוח → התור שלמעלה. רצף עבודה, לא עצירות.
• אחרי כל קטע: commit+push לפי נושא, וסגירת מעגל תיעוד — ROADMAP.md
  (⚠️ לשאול לפני מחיקת משימה) + ✅ ותאריך בהוראות-ותכנונים-למפתח-מצורף.md.
  ב-CHANGELOG.md לא נוגעים ידנית.
• שאלה באמצע → GitHub Issue, וממשיכים למשימה הבאה.
• לסיים נקי: git status --short ריק משינויים שלי בסוף השיחה.

── בתחילת השיחה ──
שאל אם להריץ אוטו' ברצף. אם כן — המשך ממשימה למשימה עד שהשיחה ארוכה מדי, ואז עצור.

── בסוף השיחה (חובה) ──
(א) סיכום קצרצר.  (ב) מה נשאר + השלב הבא.  (ג) הפק מחדש את הבלוק הזה עצמו,
מעודכן בשורות "איפה עצרנו", מוכן להדבקה בשיחה הבאה — כולל הוראה זו.
ציין מודל מומלץ ומה הצפי.
```

---

## שאלות?

פתח [Issue](https://github.com/e0548433917-gif/madaei-hatanach/issues/new) — זו
דרך התקשורת המועדפת בפרויקט, גם באמצע עבודה. אל תעצור ותחכה לתשובה: פתח, והמשך
למשימה הבאה שאינה תלויה בה.

**תודה על העזרה!**
