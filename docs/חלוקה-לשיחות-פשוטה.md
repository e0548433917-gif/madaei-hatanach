# תמונ״ך — חלוקה לשיחות, פשוט וברור

**עודכן: ה׳ באב תשפ״ו (05/08/2026)** · מהדורה 2 — סדר הסבבים שונה לפי בקשתך
**שיחה אחת = משימה אחת = גרסה אחת.** לא להתחיל שיחה לפני שהקודמת סיימה והגרסה נשמרה בגיט.

> ## ⭐⭐ הוראה מחייבת — בסוף כל שיחה, לפני שאתה מסיים את התשובה
>
> **לעדכן את שני קבצי התוכנית ולכתוב מה נעשה בפועל.** לא מה שתוכנן — מה שנעשה,
> עם המספרים האמיתיים, וגם מה נשאר פתוח ולמה. לסמן ✅ בכל טבלה שהמשימה מופיעה בה,
> ולעדכן את טבלת ״▶ מה עכשיו״ שמתחת.
>
> **⚠️ כל אחד משני הקבצים קיים בשני עותקים זהים — לעדכן את שניהם:**
> `שדרוג 3.8\<הקובץ>` **וגם** `madaei-hatanach\docs\<אותו קובץ>`.
> לערוך את זה שב-`שדרוג 3.8\`, להעתיק אותו על זה שב-`docs\`, ולוודא ב-`md5sum`
> שהם זהים לפני ה-commit. **commit נפרד** בסגנון ״תוכניות: …״, **ולדחוף לגיטהאב.**
>
> הפירוט המלא: `תוכנית-עבודה-תמונך-3.0.md` חלק ז׳ סעיף 6.
> **זה נכשל בעבר** — 4.2 נשארה מסומנת ⬜ אחרי שכבר בוצעה ונדחפה.

---

## הגרסה הנוכחית: **2.12.3** — נארזה כ״ג באב תשפ״ו (06/08/2026), **12.2MB**

### ▶ מה עכשיו (כ״ג באב) — **פיצ׳רים בלבד**

> 🧭 **החלטת תכנון, ה׳ באב:** ״נתמקד קודם בכל הפיצ׳רים לפני תיקון התוכן.״
> **הסדר החדש: סבב 2 → סבב 4 → סבב 3 → סגירה.** כל עבודת התוכן הוקפאה ועברה
> ל-[תוכנית-תוכן-עתידית.md](תוכנית-תוכן-עתידית.md). זה לא עולה כלום — התוכן נוגע רק
> ב-`guides/*/data/`, והפיצ׳רים ב-`shell/`, ואין ביניהם תלות.
> **חריג אחד:** 2.6 (עץ משפחה) עדיין חסום עד ת.1ב. אם רוצים אותו ב-3.0.0 — ת.1ב חוזר פנימה.

**2.1–2.9 בוצעו** (2.1–2.5 נארזו ב-2.12.1/2.12.3; **2.7–2.9 בוצעו כ״ג באב ועדיין לא נארזו**).
**✅ 4.2 ו-4.3 בוצעו. 2.12.3 היא האריזה הראשונה שכוללת את 4.2.**
**חדשות (כ״ג באב): 4.4–4.8 נוספו** מקובץ ״הערות על הפופאפ החדש.docx״ ומהודעותיך —
שיפורי הפופאפ, שני באגים בדפי HTML, ״מה חדש״, ואיחוד איקונים. פירוט מלא בטבלת סבב 4.

| | שיחה | קבצים | מודל |
|---|---|---|---|
| ▶ | **2.10א** — ״ערך היום״ לפי תאריך | `home.js`, `index.html`, `router.css` | Sonnet |
| ▶ | **4.1** — דיווח באגים (הממסר כבר קיים) | `personal.js`, `manifest.json` | Opus |
| ▶ | **4.4** — שיפורי פופאפ הזיהוי | `bridge.js` | Sonnet |
| ▶ | **4.5** — דף HTML: איקון + שמירה לעמוד הבית | `home.js`, `personal.js` | Sonnet |
| ▶ | **4.6** — תיקוני זיהוי בדפי HTML מותאמים | `identify.js` | Sonnet |
| ▶ | **4.7** — לשונית ״מה חדש״ באזור האישי | `personal.js`, `router.css` | Sonnet |
| ▶ | **4.8** — האחדת איקונים בכל התוסף | `bridge.js`, `guides.js`, `entry-detail.js`, `results-ui.js`, `router.css` | Sonnet |
| ▶ | **2.11** — טעינה עצלה של הדאטה (**אחרונה** בחבורה — מכיילת מול המצב הסופי) | `data.js`, `bridge.js` | Opus |

> **כל השורות שלמעלה זרות זו לזו בקבצים** מלבד: 4.1↔4.7 (שתיהן ב-`personal.js` —
> לא במקביל), ו-2.11 שכדאי להריץ **אחרונה** מכל הרשימה (גם אחרי 2.10א/4.1/4.4–4.8),
> כי היא נוגעת בשורש הטעינה וצריכה לכייל מול המצב הסופי ולא מול מצב ביניים.
> **2.6 (עץ משפחה) נשאר 🔴 חסום עד ת.1ב.**
הבלוקים המוכנים להדבקה נמצאים בסוף המסמך.

---

## ✅ מה כבר בוצע

| גרסה | מה | קבצים | מתי |
|---|---|---|---|
| ✅ 2.11.0 | **תיקון המעבר בזיהוי (באג 996)** — נכתב מנגנון handoff דרך `plugin.storage` | `router.js` | ג׳ באב |
| ✅ 2.11.0 | **שורת חיפוש בעמוד השער** — מסגרת זהב, השלמה חיה מ-2,615 ערכים, Enter = זיהוי מלא של פסוק מודבק | `index.html`, `router.css`, `router.js` | ג׳ באב |
| ✅ 2.11.0 | **האזור האישי** — 5 לשוניות: סימניות, העריכות שלי, הצעות ודיווחים, דפי HTML, משוב. ״משוב״ ו״הוספת HTML״ הועברו לתוכו | `index.html`, `router.css`, `router.js` | ג׳ באב |
| ✅ 2.11.1 | **איקון לאזור האישי** — בוצע במודל אחר, מחוץ לעץ העבודה | `index.html`, `icon/cats/personal.png` | ג׳ באב |
| ✅ 2.11.1 | **סנכרון עץ העבודה** — עד ד׳ באב העץ היה 2.11.0, ו-2.11.1 היה קיים רק כחבילה בנויה. סונכרן | | ד׳ באב |
| ✅ 0.1 | **ריפו git מקומי** — `.gitignore` (מחריג `dist/` = 576MB), `.gitattributes`, commit ראשון | `madaei-hatanach/.git` | ד׳ באב |
| ✅ 2.11.3–2.11.5 | **סבב 1 במלואו** — ר׳ טבלת סבב 1 למטה | | ד׳ באב |
| ✅ 2.11.6 | **שני תיקונים אחרי בדיקה בפועל באוצריא** — חיתוך הקטע הנבחר בפופאפ · `lastConsumedTs` שעצר את לולאת הצ׳יפים | `bridge.js` | ד׳ באב |
| ✅ ת.0 | **ולידציה של כל הרשומות** (2,686 אחרי 1.5) — סקריפט + דוח. **זו גם משימה 5.1.** הממצאים: [ת0-ממצאים.md](ת0-ממצאים.md) | `tools/validate.js`, `tools/lib/`, `tools/validation-report.md` | ד׳ באב |

### ❌ מה **לא** עובד למרות שנכתב

| מה | הסטטוס האמיתי | לאן זה הולך |
|---|---|---|
| ~~**הזיהוי לא מופיע במעבר לתוסף**~~ | ✅ **תוקן ב-2.11.3 ואומת בפועל באוצריא (ד׳ באב) — עובד מצוין.** הסיבה השורשית: `storageGet` החזיר את המעטפת `{value:...}` של `storage.get` במקום הערך שבתוכה, ולכן `rec.text` היה `undefined` והקוד יצא בשקט. תוקן, ותוקנו גם שני החשדים המקוריים | ✅ סגור |
| ~~**רקע לבן באיקון האזור האישי**~~ | ✅ **תוקן ב-2.11.4 (ד׳ באב).** המסגרת האדומה (2px בכל צד) נחתכה, הרקע הוסר לשקיפות במפתח על חום הזהב, והאיקון ממורכז ב-192×192 עם שוליים כשל שאר `icon/cats/*.png` | ✅ |
| ~~**🔴 71 ערכים חסרים בתמונ״ך**~~ | ✅ **תוקן ב-2.11.5 (ד׳ באב).** דומם 67←136, בית המקדש 79←81 — ראה למטה | ✅ סגור |

---

## 🔴 ממצא חדש (ד׳ באב) — הדאטה בתמונ״ך אינו זהה למדריכים העצמאיים

שאלת אם קבצי הדאטה מעודכנים. **בדקתי ערך-מול-ערך את כל שש הקטגוריות מול הגרסה האחרונה של כל מדריך עצמאי:**

| קטגוריה | במדריך העצמאי | בתמונ״ך | חסר |
|---|---|---|---|
| אישים (peopleguide 3.7.0) | 1,914 | 1,914 | ✅ 0 |
| מקומות (placeguide 2.8.0) | 313 | 313 | ✅ 0 |
| בע״ח (animalguide 1.9.0) | 114 | 114 | ✅ 0 |
| צומח (floraguide 1.9.0) | 124 | 124 | ✅ 0 |
| **בית המקדש** (1.9.0) | 81 | 79 | ❌ **2** |
| **דומם** (domemguide 2.2.1) | **136** | **67** | ❌ **69 — יותר מחצי!** |

**דומם — לא ערכים בודדים, אלא 7 תת-קטגוריות שלמות שלא נכנסו:**

| תת-קטגוריה | כמה חסר |
|---|---|
| `tools` — כלי עבודה | 19 |
| `weapons` — כלי נשק | 9 |
| `celestial` — גרמי שמים | 8 |
| `foods` — מאכלים | 8 |
| `instruments` — כלי נגינה | 8 |
| `coins` — מטבעות (7 מתוך 12 נכנסו) | 5 |
| `clothing` — לבוש | 4 |
| `cult` — פולחן | 4 |
| `vessels` — כלים (7 מתוך 11 נכנסו) | 4 |

דוגמאות למה שחסר: אדרת · גביע · חנית · דרבן · דוחן · דבש · אשרה · במה · הילל בן שחר · דינר (חז״ל) · הים (המוצק).

**בית המקדש — שני ערכים מלאים:** ״חנוכת הבית מחדש בידי החשמונאים״ · ״הרחבת הורדוס למקדש — כפי שמתאר יוסף בן מתתיהו״. שניהם `cat: "sheni"`, עם `methods`, `midrash` ומקורות — ערכים אמיתיים, לא כותרות.

**זה לא בעיית גרסה.** בדקתי גם את domemguide 2.2.0 (22/07) — גם שם 136. כלומר הפער קיים מאז החילוץ הראשון ב-24/07, לא נוצר מעדכון מאוחר.

### ✅ הוכרע (ד׳ באב)

**ההשמטה אינה מכוונת — זו טעות.** משימה 1.5 מחזירה את **כל 71 הערכים**, כולל כל 7 תת-הקטגוריות של דומם (`tools`, `weapons`, `celestial`, `foods`, `instruments`, `clothing`, `cult`) ואת ההשלמות ב-`coins` וב-`vessels`.

אחרי 1.5: דומם יעלה מ-67 ל-136 ערכים, ביהמ״ק מ-79 ל-81, וסך הרשומות בתוסף מ-2,615 ל-**2,686**.

---

## 🔄 השינוי בסדר הסבבים

> **ביקשת:** להקדים את הפיצ׳רים (מה שהיה ״סבב 3״) לפני העבודה הגדולה על מנוע הזיהוי, ולשלב מיד אחרי סבב הבאגים והנראות.
>
> **בוצע.** הסדר החדש: **0 תשתית → 1 באגים ונראות → 2 פיצ׳רים → 3 מנוע הזיהוי → 4 דיווחים ובילד → 5 ולידציה וסגירה.**
> מנוע הזיהוי הוא הסיכון הגדול ביותר וגם היקר ביותר. אם הוא ייתקע — עדיף שהמשתמשים כבר יקבלו את הפיצ׳רים ולא ישבו על 2.11 חודשיים.
>
> **בנוסף:** רעיונות ההרחבה (חלק ח׳ במהדורה 1) הופכים ממשאלות למשימות ממוספרות בתוך סבב 2, מחולק ל**חלק א׳ (קרוב וזול)** ו**חלק ב׳ (בינוני)** — בדיוק כמו שהצעת.

---

## הסבבים והשיחות

עמודת ⏱ = זמן משוער · עמודת 🎫 = טוקנים משוערים (בהנחה שהפיצול בוצע; בלעדיו הכל פי 2–3)

### 🔧 סבב 0 — תשתית · יעד 2.11.2 — ✅ **הושלם**

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ✅ | **0.1** | ריפו git + `.gitignore` + סנכרון 2.11.1 | — | Claude Code | 15 דק׳ | 15K |
| ✅ | **0.2** | **פיצול `router.js` ל-12 מודולים** + הסרת `path` המת מ-`CATEGORIES` | `shell/router.js` + `index.html` **בלבד** | **Claude Code** | 60–75 דק׳ | 130K |

**0.2 — 12 קבצים:** `core.js` · `data.js` · `identify.js` · `refs.js` · `guides.js` · `entry-detail.js` · `edit-forms.js` · `results-ui.js` · `home.js` · `personal.js` · `settings.js` · `bridge.js`
**יוצא: 2.11.2 — אפס שינוי במה שהמשתמש רואה.** מיפוי שורות מלא בנספח ב׳ בתוכנית המלאה.
⚠️ **חובה להתבסס על 2.11.1**, לא על 2.10.0 ולא על 2.11.0.

✅ **בוצע ד׳ באב (04/08/2026).** 2,617 שורות → 12 קבצים. אומת: כל שורה עברה בדיוק פעם אחת ·
202 מזהים ברמה העליונה, אותו סט בדיוק, אפס כפילויות · 8 מסלולי בדיקה בדפדפן עברו, אפס שגיאות בקונסול.
נארז: `dist/com.chadbedera.madaeihatanach-2.11.2.otzplugin`.

> **לקח שנתפס בבדיקה:** `PREFIXES` היה אמור לעבור ל-`identify.js`, אבל `wholeWordRe`
> ב-`core.js` צורך אותו **בזמן טעינה** (בבניית `DIVINE_NAME_RE`) — ולכן `core.js` נפל
> ב-`ReferenceError` והתוסף עלה חצי-מת בלי שום הודעת שגיאה. הועבר ל-`core.js`.
> זה בדיוק הסיכון שהטבלה בנספח ב׳ הזהירה ממנו, והבדיקה בדפדפן היא זו שתפסה אותו.

---

### 🐛 סבב 1 — באגים ונראות · יעד 2.11.5 ✅ **הושלם (ד׳ באב)**

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ✅ | **1.1** | **הזיהוי לא מופיע במעבר לתוסף** — ✅ **בוצע, 2.11.3.** שני החשדים תוקנו, **ובנוסף נמצאה הסיבה השורשית האמיתית: `storage.get` מחזיר `{value:...}` ו-`storageGet` החזיר את המעטפת במקום הערך.** ✅ **אומת בפועל באוצריא — הזיהוי עובד מצוין.** | `bridge.js` + `data.js` | **Opus** | 40 דק׳ | 40K |
| ✅ | **1.2** | **רקע לבן באיקון האזור האישי** — ✅ **בוצע, 2.11.4.** מסגרת אדומה 2px נחתכה · רקע קרם/לבן הוסר לשקיפות (מפתח על חום הזהב, כמו ב-`people.png`) · 192×192, האמנות 152×149 בשוליים 20/26 — בטווח של שאר `cats/*.png` | `icon/cats/personal.png` | Claude Code | 10 דק׳ | 8K |
| ✅ | **1.3** | **עיצוב חלון התוצאות** — ✅ **בוצע, 2.11.5.** `#resultsModal` ל-flex column עם `overflow:hidden`; רק `#resultsList`/`#noResults` גוללים (`flex:1;overflow-y:auto`). כותרת/ציטוט/צ׳יפים דביקים למעלה. CSS בלבד, אומת בדפדפן (scroll מתוך `resultsList` לא מזיז את הכותרת) | `router.css` | Sonnet | 40 דק׳ | 45K |
| ✅ | **1.4** | **קיצור הפופאפ בספרייה** — ✅ **בוצע, 2.11.5.** סיכום לפי קטגוריה + עד 5 התאמות מובילות + ״ועוד N — לחיצה על אישור תציג את כולן״. תווית התפריט שונתה ל-**״זהה דצח״מ (ומקדש) בתמונ״ך!״** | `bridge.js` | Sonnet | 20 דק׳ | 20K |
| ✅ | **1.5** | **🔴 החזרת 71 הערכים החסרים** — ✅ **בוצע, 2.11.5.** דומם 67←136 (7 תת-קטגוריות חדשות + השלמות מטבעות/כלי משכן) · בית המקדש 79←81. חולצו מ-domemguide 2.2.1 / beithamikdash 1.9.0 (`index.html` שלהם) ב-vm sandbox והושוו לפי `name`. `node tools/validate.js`: 2615→2686 רשומות, 0 שדות חובה חסרים | קבצי הדאטה של דומם וביהמ״ק | Claude Code (סקריפט) | 40 דק׳ | 35K |

✅ סבב 1 סגור במלואו. `build/pack.ps1` רץ — חבילה 12.2MB, בטווח הצפוי.

**שני תיקונים אחרי בדיקה בפועל באוצריא (נכנסו ל-2.11.6):**
* **ההוראה בפופאפ נחתכה.** הכותרת הכילה את הקטע הנבחר במלואו, וכשמסמנים פסקה שלמה
  היא לבדה מילאה את החלון. הקטע נחתך ל-40 תווים בכותרת ול-120 בגוף, וההוראה הועברה
  **לפני** רשימת ההתאמות. גודל הגופן עצמו הוא של `ui.showConfirm` של אוצריא ואינו בשליטתנו.
* **צ׳יפי הסינון ״הבהבו ולא נלחצו״.** `consumePendingIdentify` הסתמך רק על מחיקת הרשומה
  מהאחסון; כשהמחיקה לא תופסת, הפולינג של 1.5 שניות בנה את חלון התוצאות מחדש בלולאה.
  נוסף `lastConsumedTs` בזיכרון המופע (נמדד: 6 סבבים → 6 רינדורים לפני, 1 אחרי).

> ⚠️ **לקח תפעולי — כל אריזה מקבלת מספר גרסה משלה.** 2.11.5 נארז פעמיים (לפני
> התיקונים ואחריהם) תחת אותו מספר. אוצריא **אינה** מחליפה קבצים בהתקנה־מחדש של
> אותו מספר גרסה, והתיקון לא הגיע למי שהתקין מעל הקודם — מה שנראה כמו באג בקוד.
> לכן נוצרה 2.11.6. **אין לארוז פעמיים תחת אותו מספר, גם על תיקון בודד.**

---

### ✨ סבב 2 — פיצ׳רים ותוכן חדש · יעד 2.12.x

> 🔴 **לפני שמנסחים את הפרומט של סבב 2 — לקרוא את זה.**
> **2.1–2.5 מותרים במקביל לת.1** (קבצים זרים לגמרי: סבב 2 נוגע ב-`index.html`/`home.js`/
> `entry-detail.js`/`guides.js`/`results-ui.js`/`router.css`/`icon/`, ות.1 נוגע ב-`guides/*/data/*.js`
> ו-`shell/refs.js`). תנאי יחיד: כל שיחה עושה `git add` **רק לקבצים שלה**.
> **2.6 ירד לסוף חלק ב׳ (ה׳ באב)** וחסום עד שהצעת ה-`id` של **ת.1ב** תאושר. אין להכניס אותו
> לפרומט של סבב 2 כל עוד ההחלטה לא התקבלה. **2.7–2.11 אינם תלויים בו** ואין סיבה שיחכו.
> **⚡ גם 4.1א מותר במקביל** (`personal.js` + `router.css`) — ר׳ סבב 4.

#### חלק א׳ — קרוב וזול

| # | שיחה | מה | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|
| ✅ | **2.1** | **בוצע, 2.12.1 (ה׳ באב).** האיקון הוחלף (אותו מפתח על חום הזהב של 1.2, 192×192) + הפריסה בשער: **מספר עמודות קבוע** — 3 מעל 620px ו-2 מתחת — במקום `auto-fit` שנתן 4+3 קטוע. האזור האישי ודפי ה-HTML ירדו לשורה שנייה משלהם, כך שהוספת דף לא קוטעת את שורת המדריכים | Sonnet | 15 דק׳ | 12K |
| ✅ | **2.2** | **בוצע, 2.12.1.** ארבעת שדות המקום נסרקים מילה-מילה (הם טקסט חופשי ולא שם נקי), בעדיפות לצירוף על מילה בודדת. **90 מתוך 133 השדות מקושרים** לכרטיס המקום עם המפה. אינדקס של שמות **שלמים** בלבד ולא `buildLookup` — שם חלקי קישר לכרטיס הלא נכון (״אלון בצענים״→אלון בכות) | Sonnet | 30 דק׳ | 30K |
| ✅ | **2.3** | **בוצע, 2.12.1.** קטגוריה · תקופה · א-ב, בגוף מתקפל עם ״☰ סינון״ ושורת ״סינון פעיל״. הועתק מ״אישים״ 3.7.0 בלבד. **+ לבקשתך: שורת החיפוש שבתוך כל מדריך קיבלה את ההשלמה החיה ואת ה-Enter=זיהוי של השער** — `attachLiveSearch` אחת משרתת את שתיהן | Sonnet | 45 דק׳ | 50K |
| ✅ | **2.4** | **בוצע, 2.12.1.** מודול חדש `shell/co-mentions.js` (**13 מודולים**). פענוח ברמת **פסוק** — גימטריה + פרישת טווחים — כי `parseVerseRef` עוצר בפרק. **3,290 פסוקים, 2,112 מתוך 2,686 הערכים מקבלים שכנים** (חציון 4), בנייה 97ms. חוצה מדריכים: תובל קין ↔ ברזל, יובל ↔ כנור | Sonnet | 40 דק׳ | 40K |
| ✅ | **2.5** | **בוצע, 2.12.3 (כ״ג באב).** מודול חדש `shell/print.js` (**14 מודולים**) + `@media print` מלא. עמוד ההדפסה נבנה ב-`#printRoot` שבתוך `index.html` ומוצג רק בהדפסה — **בלי `window.open`**, שאין כזה ב-WebView של אוצריא. ״🖨 ייצוא כרטסת״ בשלושת המקורות: סימניות · חלון הזיהוי · תצוגה מסוננת של מדריך. **הכפתור 🖨️ שבכרטיס תוקן ולא הוכפל.** `maskDivineName` חל גם בהדפסה — נבדק על 369 הערכים שיש בפסוקיהם שם המפורש | Sonnet | 40 דק׳ | 35K |

#### חלק ב׳ — בינוני

| # | שיחה | מה | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|
| ✅ | **2.7** | **בוצע (כ״ג באב, טרם נארז).** אינדקס פסוק→פרשה + בורר בשער, ובהמשך שורת ״לימוד״ נפרדת (פרשת השבוע פעיל; נ״ך ומשנה ותלמוד — placeholder ל-2 הסבבים הבאים) | Sonnet | 60 דק׳ | 55K |
| ✅ | **2.8** | **בוצע (כ״ג באב, טרם נארז).** מפת מסע לידה→מגורים→פטירה→קבורה על מפת ה-Leaflet הקיימת, דרך `journeyPointsFor`/`scanPlaceMatches` המשותפים עם 2.2 | Sonnet | 50 דק׳ | 45K |
| ✅ | **2.9** | **בוצע (כ״ג באב, טרם נארז).** ציר זמן לפי דורות במדריך אישים, לפי השדה `cat`/`era` הקיים | Sonnet | 40 דק׳ | 35K |
| ⬜ | **2.10א** | **״ערך היום״ / מאורעות התנ״ך לפי תאריך** — חילוץ התאריכים מ״יומא דהילולא״ ומ״ביוגרפיות״, סינון מה ששייך לתנ״ך, עמוד + ריבוע בשער. **חילוץ וסינון, לא מחקר** | Sonnet | 60 דק׳ | 50K |
| ⬜ | **2.11** | **טעינה עצלה של הדאטה** — 19MB נטענים כרגע בפתיחה. אינדקס קטן מיד + דאטה לפי דרישה = פתיחה מיידית | Opus | 60 דק׳ | 50K |
| 🔴 | **2.6** | **עץ משפחה אינטראקטיבי — אחרון בסבב (הועבר לכאן ה׳ באב).** 🔴 **חסום עד ת.1ב. ת.0 גילה ש״שם״ אינו מזהה ייחודי:** 139 קישורים נצמדים לאדם הלא נכון בעל אותו שם, ו-45 לא נוצרים בגלל הבהרה בסוגריים. השדות קיימים אך **אינם ״מלאים״ במובן שאפשר לבנות עליהם עץ**. דורש `id` ייחודי לכל רשומה — ר׳ [ת0-ממצאים.md](ת0-ממצאים.md) חלק א׳ | Opus (תכנון) → Sonnet | 90 דק׳ | 90K |

> ⚡ **למה 2.6 ירד לסוף (ה׳ באב).** הוא היה ראשון בחלק ב׳ וחסם בפועל את כל הסבב.
> **נבדק אחד-אחד: 2.7, 2.8, 2.9, 2.10א ו-2.11 אינם קוראים את שדות המשפחה כלל** ואינם
> בונים גרף — אין ביניהם לבין 2.6 שום תלות. **2.2 ו-2.4** נוגעות באותו שורש (התאמה לפי שם)
> אבל אינן חסומות: אצל 2.2 שמות המקומות כמעט ייחודיים והכשל הוא ״לא נמצא״ ולא ״נמצא הלא נכון״,
> ואצל 2.4 ההתאמה היא בתוך פסוק ולא בין רשומות. כשת.1ב יוסיף `id` — שתיהן רק ישתפרו,
> בלי כתיבה מחדש.

> **על 2.10 — ההערה שלך התקבלה במלואה.** צדקת בהפרדה: **2.10א** (מה שקיים כבר בתוספים) הוא חילוץ פשוט וייעשה בוודאות. **2.10ב** — סריקת *כל* התאריכים בתנ״ך, כולל מה שלא מפורש בפסוק וצריך מפרשים/סדר עולם/תלמוד — היא **עבודת תוכן ומחקר, לא קוד**, והיא לא חוסמת את 3.0.0. נרשמה כמשימת תוכן ת.2. המבנה שייבנה ב-2.10א יקלוט אותה אחר כך בלי שינוי קוד.

---

### 🧠 סבב 3 — מנוע הזיהוי v2 · יעד 2.13.0 (השדרוג הגדול)

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ⬜ | **3.1** | **תכנון** 8 השכבות + סט בדיקות של 60–100 פסוקים + סקריפט הרצה ב-Node | `identify.js` + 20 שורות דוגמה מהדאטה | **Opus** | 60 דק׳ | 80K |
| ⬜ | **3.2** | **יישום** מה שאושר ב-3.1 + כיול מול הסט | `identify.js` + המסמך מ-3.1 | **Opus** | 120 דק׳ | 150K |
| ⬜ | **3.3** | **קציר הלקסיקון המנוקד** מתוך `verses` (מצליח על 1,141 מ-1,915 כבר עכשיו) + השלמת החסר | **קבצי הדאטה** | Gemini Pro (חלון ענק) | 90 דק׳ | 170K |

⚠️ **3.1 יוצא כמסמך לאישור — בלי קוד.** לא לערבב עם 3.2.
⚠️ **3.3 היא אחת משתי השיחות היחידות שנוגעות בדאטה.**

---

### 📮 סבב 4 — דיווחים, עדכונים ובילד · יעד 2.14.0

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ⬜ | **4.1** | **דיווח באגים — שליחה שקטה מתוך התוסף.** מודאל + תור מקומי (`outbox`) ב-`plugin.storage` + `POST` לממסר שפותח GitHub Issue. **בלי מייל · בלי קישור חיצוני · שקט מוחלט כשאין רשת.** ר׳ ההסבר המלא מתחת לטבלה | `personal.js`, `manifest.json` | **Opus** | 60 דק׳ | 60K |
| ✅ | **4.2** | **בוצע, 2.12.2 (ה׳ באב).** `bridge.js` +131 שורות: בטעינה, ברקע ולכל היותר פעם ב-24 שעות, השוואת המניפסט המקומי מול זה שבריפו. `raw.githubusercontent.com` ואחריו `api.github.com` כגיבוי (שניהם נבדקו ופתוחים בנטפרי), בדפוס `fetchWithCorsProxy` — אך **בלי `throw`** בסוף: כישלון = `null` ושקט. `cmpVersion` הקיים, `storageGetJson` שמפרק את מעטפת `{value:...}`, timeout 8 שנ׳. יש חדש → הודעה עדינה בפינה + קישור ל-Releases דרך `confirmOpenExternal`. **נבדק:** ריצה שנייה = 0 קריאות רשת, קונסול נקי. **נארזה לראשונה ב-2.12.3 (כ״ג באב)** — עד אז הייתה בעץ העבודה בלבד. ⚠️ `network.allowlist` הוגבל לשני הדומיינים בלבד — **`he.wikipedia.org` שב-`guides.js:59` אינו ברשימה**, יש לבדוק את מדריך צומח אחרי ההתקנה | `bridge.js`, `manifest.json` | Opus | **25 דק׳** | 25K |
| ✅ | **4.3** | **בוצע, 2.12.3 (כ״ג באב).** `build/pack.ps1` **הורחב ולא נכתב מחדש** — כל מה שהיה (staging, שני התוצרים, ההחרגות, הסרת הקבצים המתים, שומר ה-16MB) נשאר. נוסף: דגלי `-Major`/`-Minor`/`-Patch` לצד `-NoBump` · ערך CHANGELOG אוטומטי בפורמט הקיים, עם תאריך עברי מ-`System.Globalization.HebrewCalendar` (הגרשיים מומרים ל-׳ ״) · `node tools/validate.js --strict` **לפני** ה-bump, כך שכישלון לא משאיר גרסה מבומפּת וערך יתום · `-Release` בלבד ל-commit + tag `v<version>` + push + GitHub Release עם שתי החבילות (gh CLI); הרצה רגילה אינה נוגעת בגיט. ⚠️ `--strict` מחזיר 1 על 474 ממצאי התוכן של ת.0 (מוקפאים עד ת.1א) ולכן נוסף `-AllowValidationErrors` כמעקף מודע שמדפיס אזהרה ואת הפלט המלא | `build/pack.ps1` | **Claude Code** | 30 דק׳ | 30K |
| ⬜ | **4.4** | **שיפורי פופאפ הזיהוי** (מ״הערות על הפופאפ החדש.docx״, כ״ג באב) — שורה ראשונה לא נחתכת אם כבר קצרה משורה אחת · הסרת שורת ״הקטע שנבחר״ הכפולה בסוף (מופיעה כבר בכותרת) · מיזוג ״ייתכנו זיהויי שווא״ לתוך שורת ״לחיצה על אישור…״ · צמצום רווח בין שורות · **זיהוי מודרני חוזר ליד כל פריט** (לא רשימה נפרדת בסוף) · **סדר עדיפות ברשימה:** בע״ח/צומח/דומם קודם, ואישים + בית המקדש (פחות תלויים בזיהוי מודרני) נדחפים לסוף תחת ״ועוד N…״. בדיקת חלון גדול/שני טורים תלויה במגבלות `ui.showConfirm` של אוצריא — לתעד את המסקנה גם אם השלילה | `bridge.js` | Sonnet | 40 דק׳ | 35K |
| ⬜ | **4.5** | **דף HTML: איקון + שמירה לעמוד הבית.** שני באגים קשורים: (א) בהוספת דף HTML אין שדה לצרף לו איקון משלו לכרטיס בעמוד הבית — היום כל דף מקבל 📄 קבוע. (ב) **דף שנוסף לא נשמר לעמוד הבית אחרי סגירת אוצריא** — נשאר נגיש רק דרך האזור האישי. לבדוק את `renderCustomPageCards`/`getHtmlPagesIndex` ב-`home.js`: האם ה-`storageSet` של האינדקס אכן persist־י, או שהרינדור בעמוד הבית לא נקרא מחדש בעליית התוסף | `home.js`, `personal.js` | Sonnet | 35 דק׳ | 30K |
| ⬜ | **4.6** | **תיקוני זיהוי בדפי HTML מותאמים.** `identifyInCustomPages` (`identify.js`) עושה `plain.includes(text)` גולמי: (א) מילה נפוצה בת 2-3 אותיות (למשל ״עוד״) כמעט תמיד תיפגע כ-substring בכל דף, ומציגה תוצאת שווא שבלחיצה עליה לא ״פותחת״ שום דבר משמעותי. (ב) חיפוש רב-מילים כמעט אף פעם לא תואם submatch מדויק (רווחים/פיסוק שונים), ולכן לרוב לא מזהה כלל בתוך דפים מוספים, בזמן שאותו טקסט מזוהה תקין מול המדריכים הרגילים. לתקן להתאמת מילים/טוקנים כמו במנוע הזיהוי הראשי, לא substring גולמי | `identify.js` | Sonnet | 30 דק׳ | 25K |
| ⬜ | **4.7** | **לשונית ״מה חדש״ באזור האישי** — תקציר ה-CHANGELOG של כמה הגרסאות האחרונות (למשל 5), נטען מ-`CHANGELOG.md` בתוך החבילה (בלי רשת). שקול/פתוח: להפוך בהמשך ליומן שינויים מלא מההתחלה — לא לעכב את 4.7 בשביל זה | `personal.js`, `router.css` | Sonnet | 30 דק׳ | 25K |
| ⬜ | **4.8** | **האחדת איקונים בכל התוסף.** כרגע `icon/cats/*.png` (2.1 עודכן, השאר מ-1.2) קיימים רק בכרטיסי השער; בפופאפ הזיהוי, בכרטיסי הרשימה הפנימיים ובמקומות נוספים עדיין מוצגים אמוג׳י ישנים (👤📍🐾🌿💎🏛️). לסרוק את כל `shell/*.js` לפי `catIcon`/`cat.icon` ולהחליף לתמונה איפה שרלוונטי, בכפוף לגודל תצוגה סביר (לא לכל אזכור טקסטואלי קטן) | `bridge.js`, `guides.js`, `entry-detail.js`, `results-ui.js`, `router.css` | Sonnet | 45 דק׳ | 40K |

> **4.4–4.8 חדשות (כ״ג באב), מקובץ ״הערות על הפופאפ החדש.docx״ ומהודעותיך.** אינן
> תלויות זו בזו ולא בשום משימה אחרת בסבב 4 — מותר להריץ כל אחת בנפרד, כולל
> במקביל ל-4.1 (קבצים שונים לגמרי מ-`personal.js`/`manifest.json` של 4.1, פרט
> ל-4.7 שגם היא נוגעת ב-`personal.js` — 4.1 ו-4.7 **לא** במקביל).
> **שאלה נפרדת שעלתה ולא הפכה למשימה:** תוסף ישן שהתקנת לא נפתח בבחירת ״פתיחה
> בספרייה״ — זה תוסף **אחר**, לא תמונ״ך, ואין מספיק מידע (איזה תוסף, איזו שגיאה)
> כדי לאבחן. אם זה חוזר — לצרף שם התוסף/גרסה ותיאור השגיאה בפעם הבאה.

> ### 4.1 — שלוש הדרישות שלך, והתכנון שנובע מהן (ה׳ באב)
>
> קבעת שלושה תנאים: **לא מייל · לא קישור חיצוני · ולא ליפול כשאין רשת.**
> שלושתם יחד פוסלים גם את EmailJS (מייל), גם את `issues/new` (קישור חיצוני),
> וגם כל `fetch` שמניח שיש חיבור. **מה שנשאר, וזה גם הנכון ביותר:**
>
> ```
> המשתמש כותב  →  הדיווח נשמר מיד ב-outbox מקומי (plugin.storage)
>              →  ניסיון שקט לשלוח לממסר
>              →  הצליח: נמחק מהתור ונפתח Issue בריפו
>              →  נכשל: נשאר בתור, בלי הודעת שגיאה, וייבדק שוב בפתיחה הבאה
> ```
>
> **המפתח לא יושב בתוסף.** הוא יושב בממסר (Apps Script / Worker), ולכן איש לא יוכל
> לחלץ אותו מהחבילה ולכתוב לריפו בשמך. הרעיון שהבעלים של ״ביוגרפיות״ תיאר לך —
> ״עולה לדרייב ומשם לגיטהב״ — הוא **בדיוק הדפוס הזה**, וזה מה שלוקחים ממנו.
> אנחנו מוותרים על ה״דרייב״ באמצע ומקצרים ישר ל-Issue.
>
> **מה מעתיקים בפועל, ומאיפה:**
>
> | מאיפה | מה בדיוק |
> |---|---|
> | **ביוגרפיות** `js/app.js` ~967–1120 | **כל ה-UI**: המודאל, שדות שם/מייל/פרטים, ולידציית מייל, **שמירת שם ומייל לפני השליחה** כדי שלא ייאבדו בכישלון, צירוף גרסת אוצריא דרך `app.getInfo`, נעילת הכפתור בזמן שליחה, והבחנה בהודעות בין ״אין רשת״ ל״ההרשאה לא אושרה״ |
> | **הורדת ספרים** `app.js` ~653–663 | **שרשרת ה-CORS proxy כגיבוי** — שלושה ממסרים חלופיים בלולאת `try/catch` שקטה, ו-`throw` רק אחרי שכולם נכשלו. זה בדיוק הדפוס של ״כשל שקט״ |
> | **הורדת ספרים** `manifest.json` | מבנה `network.allowlist` — רשימה סגורה של מארחים מותרים |
>
> **מה שלא מעתיקים:** את היעד. שם זה EmailJS למייל — אצלנו זה ממסר ל-Issue.
>
> ✅ **הקמת הממסר — מדריך צעד אחר צעד מוכן:** [הקמת-ממסר-דיווחים.md](הקמת-ממסר-דיווחים.md)
> (טוקן fine-grained עם הרשאת Issues בלבד + Apps Script + בדיקה). **15 דקות, עלות אפס.**
> ברגע שהממסר קיים — 4.1 משוחרר ואפשר להריץ אותו מיד, במקביל לכל השאר.

---

### 🏁 סבב 5 — ולידציה וסגירת 3.0.0

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ✅ | **5.1** | **בוצע כ-ת.0 (ד׳ באב).** `tools/validate.js` + [ת0-ממצאים.md](ת0-ממצאים.md). ⚠️ **תיקון לניסוח המקורי:** ״בני המן״ **אינו** ניתן לגילוי אוטומטי — אין סתירה בין הרשומות אלא חֶסֶר תוכן. במקום זה יש בריכה של 646 רשומות בלי שום קשר משפחתי | **קבצי הדאטה** | Claude Code | 60 דק׳ | 60K |
| ⬜ | **5.2** | **= ת.1א. לא לבצע פעמיים.** תיקון הממצאים (בני המן ← אב · זרש+המן כבני זוג · והשאר) | קבצי הדאטה | Claude Code | 45 דק׳ | 40K |
| ⬜ | **5.3** | GitHub Action לוולידציה על כל commit + בדיקה כוללת + עדכון תיאור בחנות. **הוזל:** הסקריפט כבר קיים ו-`node tools/validate.js --strict` כבר מחזיר קוד יציאה 1 — נשאר רק קובץ workflow | — | Sonnet | 20 → **10 דק׳** | 10K |

**🎉 יוצא: 3.0.0**

---

### 📚 סבב ת׳ — תוכן ונתונים · מסלול עצמאי, רץ במקביל לכל השאר

> 🧊 **הוקפא במכוון (החלטתך, ה׳ באב): קודם כל הפיצ׳רים — 2, 4, 3. תיקון התוכן אחר כך.**
> **כל תוכנית התוכן עברה למסמך משלה:** [תוכנית-תוכן-עתידית.md](תוכנית-תוכן-עתידית.md).
> שם נמצאות ת.1א · ת.1ב · ת.2 · ת.3 · ת.4 · ת.5 · מ.1 במלואן, כולל רשימות העבודה
> שכבר הופקו מת.0. **שום דבר לא בוטל** — רק הוצא מהדרך כדי שסדר העבודה יישאר נקי.

**שתי נקודות שכן נשארות רלוונטיות עכשיו:**

| # | מה | למה זה כאן ולא במסמך התוכן |
|---|---|---|
| ⚠️ **ת.1ב** | מזהה `id` ייחודי לכל רשומה | **התנאי היחיד לשחרור 2.6 (עץ משפחה).** אם 2.6 נכנס ל-3.0.0 — ת.1ב חייב לחזור. אם לא — נשאר מוקפא |
| ❓ **ת.4** | השלמת תמונות ל-163 ערכים | שאלת כמה זה עולה בנפרד. **~2.5 שעות · ~120K, ואינו תלוי בשום משימת תוכן אחרת.** הפירוק המלא + אזהרת משקל החבילה: [תוכנית-תוכן-עתידית.md](תוכנית-תוכן-עתידית.md) |

---

## סיכום עלות

| סבב | ⏱ זמן | 🎫 טוקנים |
|---|---|---|
| ✅ 0 — תשתית | 1 שעה (בוצע) | 135K |
| ✅ 1 — באגים ונראות | 2 שעות (בוצע) | 113K |
| ✅ ת.0 — ולידציה (= 5.1) | 1 שעה (בוצע) | 60K |
| ⚡ 4.1א — דיווח באגים (הוקדם) | 25 דק׳ | 30K |
| 2 — פיצ׳רים | 7 שעות | 492K |
| 3 — מנוע זיהוי | 4.5 שעות | 400K |
| 4 — יתרת דיווחים ובילד | 1.5 שעות | 105K |
| 5 — סגירה (5.1 בוצע, 5.2 = ת.1א) | 1 שעה | 50K |
| ת׳ — תוכן (ת.1א + ת.1ב) | 1.75 שעות | 95K |
| **סה״כ שנותר** | **~16 שעות** | **~1.17M** |

---

## ⭐ הוראה מחייבת — בסוף כל סבב

**כל מודל שעובד על הפרויקט הזה חייב, בסיום כל סבב, לפני שהוא מסיים את התשובה:**

1. **לעדכן את שני קבצי התוכנית** — לסמן ✅ במקום ⬜ על מה שנסגר, ולרשום מה נשאר פתוח.
2. **לתת בלוק מוכן להעתקה** בפורמט הזה בדיוק:

```
▶ הסבב הבא: <מספר ושם>
▶ לפתוח ב: <Opus / Sonnet / Claude Code / Gemini>, שיחה חדשה
▶ להדביק בשיחה החדשה:

<בלוק הטקסט המלא להדבקה>
```

---

## 📋 תבנית פתיחה — להעתיק לכל שיחה חדשה

```
תוסף אוצריא ״תמונ״ך״, גרסה X.Y.Z.

אנא בצע את סבב <N> כפי המפורט בקבצי ההוראות בתיקייה:
  שדרוג 3.8\תוכנית-עבודה-תמונך-3.0.md
  שדרוג 3.8\חלוקה-לשיחות-פשוטה.md

הפרויקט: Downloads\תוספי תנך ותלמוד\madaei-hatanach — ריפו git.
המשימה: <מספר המשימה> בלבד. אל תיגע בשום קובץ אחר.

בסוף תן לי:
  (א) diff / רשימת הקבצים ששונו
  (ב) שורת changelog
  (ג) מספר הגרסה החדש
  (ד) את ההוראה לסבב הבא בפורמט המחייב
```

> **הערה למי שעובד ב-Claude Code:** אין צורך ״להעלות״ קבצים — הוא קורא מהדיסק ישירות. עמודת ״מעלים״ רלוונטית רק לשיחות באתר או באפליקציה.

---

## ▶ הבלוקים המוכנים — להעתקה

### ✅ 2.5 + 4.3 — בוצע (כ״ג באב, 2.12.3)

הבלוק המשולב רץ כמתוכנן, בטור: 2.5 נבדקה בדפדפן ונדחפה בקומיט משלה, ורק אחריה
4.3 — כך ש-`pack.ps1` ארז עץ נקי. **יצא 2.12.3, 12.2MB**, הכולל את 2.5, את 4.3
ואת 4.2 (שעד אז הייתה בעץ העבודה בלבד). הפירוט המלא בטבלאות שלמעלה.

---

### ▶ 2.7–2.11 — סיום סבב 2 (חלק ב׳), הבלוק הבא להרצה

**ארבע משימות בטור ואחת אחרונה.** 2.7, 2.8, 2.9 ו-2.10א אינן תלויות זו בזו
ונוגעות בקבצים שונים, ולכן אפשר להריץ אותן ברצף עם commit לכל אחת. **2.11
(טעינה עצלה) חייבת להיות אחרונה** — היא משנה את שורש הטעינה שכל השאר נשענות
עליו, וכיול שלה מול מצב ביניים יידרוש כתיבה מחדש. **2.6 אינו בבלוק** — הוא
חסום עד ת.1ב.

```
תוסף אוצריא ״תמונ״ך״, גרסה 2.12.3. סיום סבב 2 חלק ב׳ — חמש משימות,
בסדר הזה ולא במקביל: 2.7 → 2.8 → 2.9 → 2.10א → 2.11.

קבצי ההוראות:
  שדרוג 3.8\תוכנית-עבודה-תמונך-3.0.md   ← ״סבב 2 חלק ב׳״ + סעיף 2.10
  שדרוג 3.8\חלוקה-לשיחות-פשוטה.md

הפרויקט: Downloads\תוספי תנך ותלמוד\madaei-hatanach
ריפו: https://github.com/e0548433917-gif/madaei-hatanach

⚠️ לפני שאתה מתחיל: git status. אם יש קבצים לא-מחויבים — עצור ושאל.
⚠️ 2.6 (עץ משפחה) אינו בבלוק הזה. הוא חסום עד ת.1ב. אל תיגע בו.
⚠️ commit נפרד לכל משימה, עם git add רק לקבצים שלה. לא commit אחד לכולן.

═══ 2.7 — ״מי בפרשה הזו״ ═══

קבצים: shell/refs.js (או מודול חדש shell/parasha.js) · shell/home.js ·
index.html · shell/router.css.

מיפוי פסוק→פרשה הוא **טבלה קבועה** — 54 פרשות, טווח פתיחה וסיום לכל אחת.
לכתוב אותה ביד בקוד, לא לייצר אותה מ-AI ולא למשוך מרשת. הפרסינג של
מראי מקום כבר קיים ב-refs.js (parseVerseRef / פרישת טווחים) — 2.4 כבר
פרש 3,290 פסוקים ברמת פסוק בודד ב-co-mentions.js. להשתמש באותו מנגנון,
לא לכתוב שני.

מה לבנות: עמוד/תצוגה שמקבלת פרשה ומחזירה את כל הערכים מכל ששת המדריכים
שיש להם פסוק בתוך טווח הפרשה, מקובצים לפי מדריך. בשער — בורר פרשה.
לכבד את maskDivineName. לבדוק בדפדפן: פרשה אחת לפחות מכל חומש.

═══ 2.8 — מפת מסע לאישיות ═══

קבצים: shell/entry-detail.js · shell/router.css.

בכרטיס אישים, ארבעת שדות המקום (birthPlace/dwelling/deathPlace/burialPlace)
כבר מקושרים לכרטיסי המקומות מאז 2.2 — 90 מתוך 133. להשתמש **באותו**
lookupPlaceByPhrase ובאותו placesNameIndex, לא לבנות התאמה שנייה.

מה לבנות: מפת Leaflet קטנה בתוך כרטיס האישיות, עם עד ארבעה סימונים
בסדר לידה → מגורים → פטירה → קבורה וקו ביניהם. אופליין בלבד
(Natural Earth, בדיוק כמו renderOfflineMiniMap הקיים). אישיות בלי אף
מקום עם geo — בלי מפה בכלל, לא מפה ריקה.

═══ 2.9 — ציר זמן לפי דורות ═══

קבצים: shell/guides.js · shell/router.css.

השדה cat כבר מסודר לפי ספרים, ו-catsCache['people'] מחזיק label+era
לכל קטגוריה — זה כל מה שצריך. אין דאטה חדש.

מה לבנות: תצוגת ציר במדריך אישים, לצד תצוגת הרשת הקיימת (מתג בסרגל,
לא החלפה). לכבד את הסינון הקיים (matchesGuideFilters) — ציר שלא מכבד
את הצ׳יפים יסתור את מה שכתוב בהם.

═══ 2.10א — ״ערך היום״ / מאורעות התנ״ך לפי תאריך ═══

קבצים: shell/home.js · index.html · shell/router.css · קובץ דאטה חדש
קטן (למשל guides/_shared/dates.js) — **רק** לתאריכים שיחולצו.

זו עבודת חילוץ וסינון, לא מחקר. המקורות:
  שדרוג 3.8\-יומא-דהילולא1.0.1.otzplugin (609KB, זה zip)
  ״ביוגרפיות v2.2.3.otzplugin״ — מעט מאורעות תנ״ך לפי תאריך
לחלץ, לסנן את מה ששייך לתנ״ך בלבד, ולבנות עמוד ״מאורעות התנ״ך בתאריך
זה״ + ריבוע ״ערך היום״ בשער. **לא לסרוק את התנ״ך אחר תאריכים** — זו
2.10ב, משימת תוכן שאינה בסבב הזה. המבנה חייב לקלוט אותה אחר כך בלי
שינוי קוד.

═══ 2.11 — טעינה עצלה של הדאטה (אחרונה, ורק אחרי שהארבע יושבות) ═══

קבצים: shell/data.js · shell/bridge.js · shell/identify.js.

19MB נטענים כיום בפתיחה דרך ה-iframe הנסתר. המטרה: אינדקס זיהוי קטן
שנטען מיד, והדאטה המלא של כל מדריך לפי דרישה.

⚠️ שלושה דברים שכבר נשענים על טעינה מלאה ברקע ויישברו בשקט אם לא
יטופלו יחד: refreshEntryLinksWhenReady (entry-detail.js), coMentionsReady
(co-mentions.js) ו-placesNameIndex. לוודא שכל אחד מהם עדיין מקבל את מה
שהוא צריך, ולבדוק בדפדפן שקישורי המקומות ו״מוזכר יחד עם״ עדיין מופיעים.
למדוד ולדווח: זמן עד שהשער אינטראקטיבי, לפני ואחרי.

═══ בסוף — אריזה ותיעוד ═══

לארוז עם הסקריפט שנבנה ב-4.3:
  .\build\pack.ps1 -Minor -AllowValidationErrors -Title "…" -Notes "…","…"
(-Minor כי זה סוף סבב שלם: 2.12.3 → 2.13.0. -AllowValidationErrors נדרש
עד שת.1א תיסגר — לוודא שהמספרים בפלט הם הממצאים המוכרים ולא רגרסיה.)

ואז התיעוד, לפי ההוראה המחייבת בראש חלוקה-לשיחות-פשוטה.md: לסמן ✅
ולכתוב מה נעשה בפועל **בשני העותקים הזהים של כל קובץ**, לאמת ב-md5sum,
commit נפרד ״תוכניות: …״, ולדחוף.

═══ בסוף תן לי ═══
(א) diff לכל משימה בנפרד
(ב) מה נבדק בדפדפן בכל אחת
(ג) שורת changelog
(ד) מספר הגרסה שנארזה + גודל החבילה
(ה) git commit + push
(ו) את ההוראה לסבב הבא בפורמט המחייב
```

### 4.1 — דיווח באגים ⚠️ רק אחרי שהממסר קיים

**אל תפתח את השיחה הזו לפני שהקמת את הממסר** (Apps Script + טוקן עם הרשאת
Issues בלבד). בלעדיו אין לאן לשלוח, והשיחה תיתקע באמצע.

```
תוסף אוצריא ״תמונ״ך״, גרסה <הגרסה הנוכחית>.

אנא בצע את משימה 4.1 כפי המפורט בקבצי ההוראות:
  שדרוג 3.8\תוכנית-עבודה-תמונך-3.0.md   ← ראה ״סבב 4״ + נספח ג.11
  שדרוג 3.8\חלוקה-לשיחות-פשוטה.md

הפרויקט: Downloads\תוספי תנך ותלמוד\madaei-hatanach
כתובת הממסר: <ה-URL שקיבלת מ-Apps Script>

המשימה: 4.1 בלבד — דיווח באגים בשליחה שקטה.
קבצים: shell/personal.js + manifest.json בלבד.

שלוש דרישות מחייבות: לא מייל · לא קישור חיצוני · שקט מוחלט כשאין רשת.

קרא קודם שני מקורות:
  * ״ביוגרפיות v2.2.3.otzplugin״ (בתיקיית שדרוג 3.8, זה zip), js/app.js
    שורות ~967–1120 — משם לוקחים את כל ה-UI: המודאל, שדות שם/מייל/פרטים,
    ולידציית המייל, שמירת השם והמייל ב-localStorage לפני השליחה,
    צירוף גרסת אוצריא דרך app.getInfo, ונעילת הכפתור בזמן שליחה.
    אל תיקח את היעד שלהם — הם שולחים ל-EmailJS, כלומר למייל.
  * ״הורדת ספרים v3.1.3.otzplugin״, app.js ~653–663 — משם לוקחים את דפוס
    ה-try/catch השקט עם ממסרים חלופיים.

מה לבנות:
  * הדיווח נשמר קודם כל ב-outbox ב-plugin.storage, לפני כל ניסיון שליחה.
  * ואז ניסיון POST שקט לממסר. הצליח → נמחק מהתור והמשתמש רואה ״נשלח״.
    נכשל → נשאר בתור, בלי הודעת שגיאה, וייבדק שוב בפתיחה הבאה.
  * בפתיחת התוסף: אם יש פריטים בתור — לנסות לשלוח אותם ברקע, בשקט.
  * הודעת ״נשלח״ רק על הצלחה אמיתית. לעולם לא ״נשלח״ על משהו שרק נכנס לתור.
  * מגבלה: עד 20 פריטים בתור, והישן ביותר נמחק — שלא ייערם לנצח.
  * manifest.json: network.allowlist עם מארח הממסר בלבד.

אל תיגע בשום קובץ אחר.

בסוף תן לי: (א) diff, (ב) שורת changelog, (ג) מספר גרסה חדש,
(ד) git commit + push, (ה) את ההוראה לסבב הבא בפורמט המחייב.
```

---

## ארבעה כללים שלא כדאי לשבור

1. **אף פעם לא להעלות קבצי דאטה לשיחת קוד.** הם 19MB. שיחות **3.3 ו-5.1 בלבד** נוגעות בהם.
2. **`git commit` לפני כל סבב.** מהיום זה הגיבוי — 2,615 הרשומות מגובות.
3. **שיחה אחת = משימה אחת = גרסה אחת.** לא לערבב.
4. **בסוף כל סבב — לעדכן את הקבצים האלה ולתת את ההוראה לסבב הבא.** אחרת מאבדים את החוט.
