# קבצי תוכן חדשים שהועלו — 25/08/26, טרם עובדו

חומר גולמי (CSV בתוך .txt) שהועלה בסוף שיחת פרסום 2.40.0.

**✅ עודכן 30/08/26 — עובד ושולב** ע"י `tools/merge-pending.js` (הסקריפט נשאר
בריפו וניתן להריץ שוב; `node tools/merge-pending.js` = דוח יבש, `--write` = שילוב).
מה שולב בפועל:

| מקור | נכנס אל | חדשים | הערה |
|---|---|---|---|
| ערך-היום-מאורעות-1..4 | `TANAKH_DATE_EVENTS` (dates.js) | 69 | מתוך 123 שורות; 87 ייחודיות, 18 כבר היו |
| כלים-מהמקורות-1..5 | `DATA` (domem) | 198 | קבצים 2 ו-3 היו כפילות מלאה של 1 |
| מטבעות.txt | `DATA`, קטגוריה `coins` | 20 | 4 כבר היו (הקטגוריה לא הייתה ריקה) |
| צבעים.txt | תוקן בדיעבד | — | ר׳ "באג הצבעים" למטה |

* **כלים-מהמקורות-1.txt עד 5.txt** — רשימות "כלים" (שם/קטגוריה/מקור מדויק/
  ציטוט/ביאור) מהתנ״ך, המשנה והתלמוד. ⚠️ **חשד כפילות חזק בין הקבצים** —
  למשל כלים-מהמקורות-1 וכלים-מהמקורות-3 חופפים כמעט לגמרי (חלק מ-3 אף מכיל
  שיבושי OCR/קידוד, כמו `"ב me'"` במקום `"ב'"`). לפני כל שילוב בפועל: לאחד,
  להשוות ל-`guides/domem/data/domem-data.js` הקיים כדי לא ליצור כפילות עם
  ערכים שכבר שם, ולתקן שיבושי קידוד.
* **מטבעות.txt** — טבלת מטבעות ויחידות משקל (פרוטה עד ככר קודש), עם יחס
  מוניטרי ומשקל כסף/זהב מדויק לכל אחד, מקור וציטוט. **לא ברור עדיין אם זו
  תוספת ל״דומם״ הקיים או קטגוריה/מדריך נפרד** (מטבעות הם לא "דומם" במובן
  הרגיל של הקטגוריה) — להכריע לפני יישום.
* ✅ **צבעים.txt — שולב** (29 ערכים, קטגוריה `colors`), 25-26/08/26.
  ⚠️ **באג שהתגלה ותוקן 30/08/26**: הסקריפט של אותה שיחה מיפה את העמודות הפוך —
  לקח את ה-`ref` מעמודת ה*ביאור* ואת ה-`text` מהציטוט, ושרשר `${undefined}`.
  התוצאה: 48 רשומות `makorot` שהטקסט שלהן נגמר ב-"undefined", 18 רשומות בלי `ref`
  כלל, ו-18 ערכים שה-`explanation` שלהם היה "גוון המוזכר במקורות שלהלן. undefined".
  `makorot` ו-`explanation` נבנו מחדש מהקובץ המקורי (עמודה 3=מקור, 4=ציטוט,
  5=ביאור). **הלקח**: לאמת מיפוי עמודות מול הכותרת של ה-CSV, ולבדוק `grep -c
  undefined` על קובץ הדאטה אחרי כל שילוב.

* **ערך-היום-מאורעות-1.txt עד 4.txt** — עוד מאורעות ל"ערך היום" (חודש/תאריך/
  מאורע/מקור מדויק/ציטוט/הסבר), באותו מבנה כמו מה שכבר ב-`TANAKH_DATE_EVENTS`
  (`guides/_shared/dates.js`). ⚠️ **גם כאן חשד כפילות חזק בין הקבצים עצמם**
  (1787312123822 ו-1787312148006 חופפים ברובם) **וגם מול מה שכבר קיים**
  ב-dates.js (בין השאר מ-40 המאורעות שנוספו הלילה) — לפני שילוב: לאחד את 4
  הקבצים, ואז להשוות שורה-שורה מול dates.js הקיים כדי לא להכפיל אירועים
  זהים באותו תאריך.

* **השלמת-ערכים-מורחבת-פי-22.docx** — "אנציקלופדיה ומילון מורחב: ערכים,
  מחצבים, כלים ומונחים מתנ״ך וחז״ל", מחולק למדורים (1. דומם — אבנים
  יקרות/מתכות/מחצבים, ועוד מדורים בהמשך הקובץ) באותה מתכונת (מקור מדויק/
  ציטוט/זיהוי מודרני/הסבר מפורט). קובץ Word בינארי (לא חולץ טקסט מלא) —
  חופף תמטית לקבצי "כלים" למעלה; לבדוק היקף חפיפה לפני שילוב.

## שומר השם (התוסף החיצוני שהמשתמש רוצה לשאוב ממנו הגדרות)

`שומר_השם_1_4_0.otzplugin` (ב-Downloads של המשתמש, לא הועתק לריפו — קובץ
בינארי/ארוז, לא מתאים ל-git כמו קבצי הטקסט למעלה). **תיקון הבנה**: זה תוסף
של יוסי כהן (`com.yosikohn.shemshomrer`) **להמרת שמות קדושים לכינויים**
במסמכי טקסט/וורד (מניעת מחיקת שם ה׳), לא תוסף לפורמט ציטוט כללי. הבקשה
המקורית של המשתמש הייתה ש"עינים למקרא" ישאב משם את **הגדרות ההמרה של שמות
הקדושה**, כדי שציטוטי פסוקים/קטעים בתוסף הזה יכבדו את אותו כינוי — לא נבדק
עדיין איך התוסף ההוא שומר/חושף את ההגדרות בפועל (localStorage? קובץ?).

---

# תיקיית "חומרים להרחבה" — נבדקה 01/09/2026

⚠️ **רוב הקבצים בתיקייה הזו נפסלו. אל תשלבו מהם בלי לקרוא את הטבלה.**

הדפוס החוזר: הקובץ פותח בשורות אמיתיות ואיכותיות, ואחרי כ-200–250 שורות
עובר למילוי אוטומטי — הרכבה קומבינטורית של שמות, טקסט תבניתי באנגלית,
או חזרה מחזורית על אותן רשומות. הכותרות ושמות ספרי המחקר (פליקס, עמר,
משורר) אמיתיים לכל האורך, ולכן **אי אפשר לזהות את הזיוף מהעמודות הראשונות** —
צריך לבדוק את אמצע הקובץ ואת סופו.

| קובץ | שורות | מצב |
|---|---|---|
| `מקומות בחזל.xlsx` | 510 | ❌ **זהה בייט-לבייט** ל-`talmudic_babylonian_settlements_500_with_sources.xlsx`. ~15 שמות אמיתיים בלבד; כל 34 היישובים בכל מחוז מצביעים על אותה נקודה, ו"סורא" משובצת תחת "מחוז פומבדיתא". ✅ 5 ערכים אומתו ושולבו (`cat:"bavel"` ב-places.js) |
| `חי ארוך.xlsx` = `talmudic_fauna_500_pure.xlsx` | 500 | ❌ מ-~240 והלאה: `"Zoological identification for X in Talmudic literature"` באנגלית. 89 ערכי זיהוי ייחודיים בלבד מתוך 500 |
| `talmudic_fauna_synonyms_500.xlsx` | 500 | ❌ רק **65 שמות ייחודיים** שחוזרים במחזור, עם `(240)`, `(245)` בתוך השם |
| `חי.xlsx` | 510 | ❌ הסוף: "זכר ארבה", "חרק לווייתן", "חרק דג" |
| `כלים.xlsx` | 415 | ❌ ~200 ראשונות אמיתיות (פרוטה, דינר, סלע). הסוף: "מנת קדר", "סל רחיים", "סל מגש" |
| `צמחים.xlsx` | 510 | ❌ ~250 ראשונות אמיתיות (חטים, שעורים, גפן). הסוף: "שמן אורז", "תמצית שמים", "תמצית תפוח" |
| **`חי מסודר.xlsx`** | 51 | ✅ **אמיתי לכל האורך** |
| **`חי נוסף.xlsx`** | 105 | ✅ **אמיתי לכל האורך** |
| **חמשת ה-docx של ארכיאולוגיה** | — | ✅ **תוכן אמיתי ואיכותי.** V2 הוא המלא (5,916 תווים) |

## מה כבר שולב

* **`animal-data.js`: 115 → 132.** מתוך 156 השורות בשני קבצי החי הטובים —
  90 כבר היו במדריך, ומהשאר סוננו שמות זואולוגיים מודרניים שאינם מאוזכרים
  בחז"ל (לובסטר, סלמון, `Drosophila`, `Anopheles`, נמלת האש) ושתי שגיאות
  שבקובץ עצמו: "חוגלת המדבר" זוהתה כ"קורקבן" (איבר, לא עוף), ו"יעלת חן"
  ממשלי הוא דימוי לאישה ולא בעל חיים.
* **`places.js`: 317 → 322.** ר' הקומיט של `cat:"bavel"`.

⚠️ **הערכים החדשים נכנסו עם `k:null` ו-`verses`/`midrash`/`academic` ריקים
בכוונה.** המקור נתן שם, שם מדעי ומראה-מקום אחד — לא ציטוט מנוקד ולא מקור
נבדק. **אין להשלים אותם בניחוש.** מי שמשלים — שיביא ציטוט אמיתי ממקור נבדק.

## מה שנשאר לעשות מהתיקייה

**מסמכי הארכיאולוגיה** — יש בהם ממצאים אמיתיים שעדיין **אינם** ב-54 הערכים
של `guides/archaeology/`:

* **בולת גמריהו בן שפן** — מ"בית הבולות" בעיר דוד; גמריהו הוא הסופר המלכותי
  בחצר יהויקים, מפורש בירמיהו
* **בולת יהוכל בן שלמיהו** ו**בולת גדליהו בן פשחור** — שני שרים של צדקיהו
  מאותו פסוק (ירמיהו לח, א), שחותמותיהם נמצאו במרחק מטרים זה מזה
* **בולת ישעיהו** — נחשפה בעופל בסמוך לבולת חזקיהו
* **תפילין מקומראן וממצדה** — עשרות קפסולות ורצועות עור, ובהן פרשיות בכתב
  עברי עתיק ובכתב אשורי

ארבע הבולות הן הצלבה ישירה לפסוק מפורש — בדיוק המבנה שמדריך הארכיאולוגיה
בנוי סביבו.

## ⚠️ פתוח: כלים.xlsx וצמחים.xlsx לא עברו את אותה עבודת חילוץ

בשונה מ"חי" (שם היו שני קבצים נקיים לכל האורך) — ב-**כלים.xlsx** וב-**צמחים.xlsx**
אין קובץ נקי מקביל. השורות הראשונות בכל אחד (עד בערך 200–250) נראות אמיתיות
לגמרי (פרוטה/דינר/סלע; חטים/שעורים/גפן), בדיוק כמו שהיה בקבצי החי לפני
שהתחיל המילוי. **מישהו צריך לעבור עליהן ולמצוא את נקודת המעבר בפועל** —
כמו שנעשה כאן ל"חי" — ולחלץ משם את מה שאמיתי אל `guides/domem/data/` (כלים)
ו-`guides/flora/data/` (צמחים). זה לא בוצע בסבב הזה.
