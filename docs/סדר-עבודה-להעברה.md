# עינים למקרא — סדר עבודה להמשך (מסמך העברה)

**נוצר: 6.8.26 · עודכן: 9.8.26.** מסמך אחד שמרכז את **סדר הביצוע המומלץ** על כל מה שפתוח בפרויקט, ומצביע לקובץ המדויק שבו כתוב כל דבר. מיועד למי שממשיך את הריפו — כולל בהרצה עצמאית עם חשבון Claude נפרד.

> ⚠️ **מעקב פתוח (עודכן 9.8.26):** יש שני עותקי מקור לשלושת התוספים חולין/מומים/סוכה — [`mishna-talmud-addons/`](../mishna-talmud-addons/) (מקור עצמאי מלא: manifest+icon) ו-`guides/talmud-tools/` (בלי manifest/icon, הטמעה ישירה בתוך עינים למקרא). **הוכרע: הכיוון הנכון הוא הטמעה** (`guides/talmud-tools/`) — זה מה שבפועל נבנה ושודר ב-2.14.0 (ר׳ `CHANGELOG.md`). **`mishna-talmud-addons/` יימחק ברגע שמוודאים ש-`guides/talmud-tools/` שלם ועובד** — לא לפני.

## ✅ נסגר ב-31/08/2026 — אל תפתחו מחדש

השיחה ״תכנון עיצוב דף הבית״ נקטעה באמצע (שגיאת SSL חוזרת), ושש הבקשות
האחרונות שבה מעולם לא נענו. הן אותרו בתמלול, טופלו, ונדחפו. **הן סגורות —
מי שנתקל בהן במסמך ישן לא צריך לבצע אותן שוב:**

| מה | איפה בקוד | סטטוס |
|---|---|---|
| דיווחים מגיעים למייל ולא לגיטהאב | `postReportToRelay`, `personal.js` | ✅ הסדר הופך — Web App תמיד, `feedback.report` העתק |
| כתובת מייל חובה בדיווח (בשני המסלולים) | `personal.js` + `edit-forms.js` | ✅ בטופס, ובהצעת עריכה נשמרת ונשאלת פעם אחת |
| ההדגשה של ״מה נערך״ לא נראתה | `markdownToPlain`, `personal.js` | ✅ Markdown ל-Issue, טקסט קריא לאוצריא |
| כרטיסייה נשלחה כמה פעמים | `<cat>_sent_v1`, `data.js` | ✅ חתימת FNV-1a; עריכה חדשה פותחת שליחה מחדש |
| גלגל ההגדרות על כפתור ה-+ במפה במסך מלא | `guides/places/css/map.css` | ✅ מוסתר כל עוד המסך המלא פתוח |
| השוואת בורר גופנים/שם ה׳ מול סידורון ושני טורים | — | ✅ **נענתה בתיעוד בלבד**: נספח ב׳ ב-`מפרט-עיצוב-אוצריא.md`. לא בוצע בקוד, בכוונה |

⚠️ **שני דברים שנשארו פתוחים מאותה שיחה, ושניהם פעולה שלך ולא של קלוד:**
העלאת ״עינים למקרא+״ לחנות (ידנית), ובדיקה על מכשיר של המתגים ליומן —
הם היחידים שמוחקים מידע מהיומן של אוצריא ולא רק מוסיפים.

## גישה לריפו

* **ריפו:** https://github.com/e0548433917-gif/madaei-hatanach (ציבורי, יש הרשאת כתיבה ישירה — לא צריך Pull Request)
* **עץ עבודה מקומי (אם עובדים על המחשב של הכותב):** `C:\Users\0548433917\Downloads\למיין\אוצריא\תוספי תנך ותלמוד\madaei-hatanach` (הנתיב הישן `Downloads\תוספי תנך ותלמוד\` כבר לא קיים)
* **לפני שנוגעים בקוד — לקרוא:** [`README.md`](../README.md) (סקירה כללית) ואז [`למפתחים.md`](../למפתחים.md) (ארכיטקטורה: מבנה תיקיות, איך ה-shell עובד, איך אורזים גרסה).
* **בדקו GitHub Issues פתוחים לפני שמתחילים** (`gh issue list`) — יש שם דיווחים אמיתיים מהתוסף בשימוש בפועל, לא רק ממה שכתוב כאן.

## שני חוקים מחייבים בסוף כל סבב

1. **לסמן ✅ בשני קבצי התוכנית** — [`תוכנית-עבודה-תמונך-3.0.md`](תוכנית-עבודה-תמונך-3.0.md) **וגם** [`חלוקה-לשיחות-פשוטה.md`](חלוקה-לשיחות-פשוטה.md) (שניהם מקור אמת) — ולתת בלוק מוכן להעתקה לשיחה הבאה.
2. **כל סבב רציני (לא תיקון קטן) מסתיים ב-`build/pack.ps1 -Release`** — commit + תג `v<version>` + push + GitHub Release. לא להשאיר עבודה גמורה רק בעץ המקומי.

**וכלל בטיחות:** יכולות לרוץ כמה שיחות על אותו עץ עבודה בו-זמנית. **לפני `git commit` — `git status`**, ולהוסיף רק את הקבצים שהשיחה הזו עצמה נגעה בהם.

---

## סדר עבודה מומלץ (מלמעלה למטה = לפי עדיפות)

### א. סבב 2 שנותר
בלוקים מוכנים ב-[`חלוקה-לשיחות-פשוטה.md`](חלוקה-לשיחות-פשוטה.md) § חלק ב׳. **2.7 · 2.8 · 2.9 · 2.10א בוצעו.** נשאר:
* **2.7ב** — כרטיס נ״ך (קטן, תוכן כבר קיים)
* **2.11** — טעינה עצלה (להריץ אחרונה — מכיילת מול המצב הסופי; **גם ת.4/ת.5 בתוכן ממתינות להכרעה סביבה, ר׳ סעיף ו׳**)
* **2.5ב** 🆕 — בחירה חלקית בייצוא/הדפסה מחלון הזיהוי
* **2.10ג / 2.10ד** 🆕 — טיימר מ״בית המקדש״ לתוך ערך היום, ואז הסרת קרדיט ״ביוגרפיות״ (2.10ד תלוי בת.2 בתוכן)
* **2.6** — עץ משפחה (⚠️ חסום עד ת.1ב)

### ב. 4.1 — דיווח באגים
דורש הקמת ממסר חד-פעמית (15 דק׳, מדריך מוכן): [`הקמת-ממסר-דיווחים.md`](הקמת-ממסר-דיווחים.md). **כבר בוצע** לפי CHANGELOG 2.13.2 — לוודא שסומן ✅ בשני קבצי התוכנית.

### ג. 4.4–4.8 — תיקוני פופאפ/איקונים/HTML
עצמאיים זה מזה (מלבד 4.7 מול 4.1 — לא במקביל). **4.8 (איחוד איקונים) מגובה ב-GitHub Issue #1 אמיתי** — עדיפות.

### ד. סבב 3 — מנוע הזיהוי v2 (השדרוג הגדול)
⚠️ **טרם בוצע, נכון ל-9.8.26** (אין רישום ב-CHANGELOG ל-3.1/3.2/3.3). 3.1 (תכנון, Opus, בלי קוד) חייב לצאת כמסמך ולקבל אישור מפורש **לפני** שמתחילים 3.2/3.3.

### ה. סבב 5 — סגירת 3.0.0
5.2 (= ת.1א) ואז 5.3 (GitHub Action). ברגע ש-5.2 מבוצע, **ת.1ב** משתחררת ופותחת את 2.6.

### ו. תוכן (סבב ת׳) — 🟢 **אושר מראש, מתחיל אחרי שסבב 3 נסגר**
**אין צורך לבקש אישור נוסף** — האישור ניתן ב-9.8.26. הסדר המומלץ נשאר: קודם סבב 3, ואז ישר לכאן. כל [`תוכנית-תוכן-עתידית.md`](תוכנית-תוכן-עתידית.md): ת.1א–ת.7, מ.1–מ.3.
* ⚠️ **החרגה שכבר רצה במקביל:** סריקת מסכת בכורות (משנה/בבלי/ירושלמי) הוקדמה לפי הוראת הכותב — ר׳ [`מפרט-מקרא-מפורש-4.0.md`](מפרט-מקרא-מפורש-4.0.md) אם קיים.
* ⚠️ **פתוח להכרעה: ת.4 (תמונות)** — לאסוף עכשיו עם כיווץ, או לחכות ל-2.11 שמייתר אותו לגמרי? עדיין לא הוכרע.
* **ת.7** 🆕 — סריקה מחדש של התנ״ך והמפרשים: ערכים חסרים (למשל השסועה, רש״י פרשת ראה) וכפילויות שם (למשל ״צילה״).

### ז. תוסף ״משנה ותלמוד״ (חלק ט׳)
* **ט.2 (מדריכים מוטמעים) כבר בוצע** — `guides/talmud-tools/`, שודר ב-2.14.0.
* **ט.1 (בורר דף + זיהוי) בתהליך** — השלד (רשימת מסכתות) קיים, בחירת דף וזיהוי לפי דף עדיין לא. **תלוי במ.2** (תיוג מקורות תלמודיים בתוכן) כדי שהזיהוי לא יחזיר מסך ריק.
* **ט.3 (עירובין/מנחות/זבחים)** — תכנון בלבד, לא לפני שט.1 שלם.
* **לא לחכות ל-3.0.0 בשביל:** [`assets/icons-mishna-talmud/README.md`](../assets/icons-mishna-talmud/README.md) (איקונים) ו-**מ.1** (המפרט) — אפשר במקביל לכל השאר.

### ח. אחרי שהכול נסגר (3.0.0 + ט׳ + סבב ת׳) — לא לעצור
לפתוח מיד ״סבב שכלול״: לעבור על כל הפרויקט מחדש ולחפש מה אפשר לשפר.

---

## מה **אסור** להעלות לריפו הציבורי

**ספרי פליקס** (`שדרוג 3.8\פליקס`) — מוגנים בזכויות יוצרים עד 2075. מותר להשתמש בהם כמקור לבדיקה ולניסוח מחדש עם ייחוס, **אסור** להעתיק מהם או להעלות אותם עצמם.

---

## שאלות

לפתוח **Issue** בריפו — לא תגובה בפורום.
