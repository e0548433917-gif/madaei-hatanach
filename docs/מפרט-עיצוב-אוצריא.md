# מפרט עיצוב — התאמת התוסף לשפת העיצוב של אוצריא

נכתב 30/08/2026. **סטטוס: הושלם במלואו (שלבים 1–9) ב-02/09/2026.**
הקשר וארכיטקטורה כלליים: [../למפתחים.md](../למפתחים.md).
רישום ב-ROADMAP: פריט **ע.1** ו-**ע.1א** (סומנו כהושלמו).

הושלם במלואו בכל חלקי המערכת: פאנל ההגדרות, כרטיסים, פאנלים, טפסים, בורר ספר, ומדריכי התלמוד.

## למה דווקא "סידורון" — ועכשיו גם Otzaria_Offline_update

התבקשנו להתאים את מראה התוסף לאוצריא. המקור המקורי שנמצא הוא התוסף
**"סידורון"** (`com.moshenahari.siduron`, v3.7.0) — הוא לא סתם "יפה", אלא
משכפל בקפידה את קומפוננטות ה-Flutter של אוצריא עצמה (`SettingsCard`,
`SegmentedSettingsTile`, `AppDropdownField`, `AppCard.section`), עם הערות
בקוד שמצביעות על השמות המקוריים. לכן: **מי שמעתיק מסידורון מתקרב לאוצריא,
לא לטעם של מישהו.**

⚠️ **מ-31/08/2026 יש מקור טוב עוד יותר — ר' נספח ג׳ למטה.**
`Otzaria/Otzaria_Offline_update` (הלאנצ'ר הרשמי) מכיל קוד עיצוב **מועתק
ישירות מקוד המקור** של אוצריא עצמה, לא פרשנות של מפתח חיצוני. הוא כבר
תיקן אצלנו טעות אחת שירשנו מסידורון (רדיוס pill בדרופדאון, כשבפועל
באוצריא הכל 8px), וחשף עוד אחת שעדיין לא תוקנה (צבע הדרופדאון).
**בכל התאמת עיצוב חדשה — לבדוק קודם מול נספח ג׳, ורק אם אין שם תשובה
לחזור לסידורון.**

איפה להשיג אותו: התוסף מותקן ברוב המכשירים, או להוריד מחנות התוספים ולחלץ
את `css/style.css` (זה קובץ אחד, ~16KB, כל המפרט שלמטה נגזר ממנו).

⚠️ **אין להעתיק את הקובץ שלו כמו שהוא.** סידורון משתמש בטוקנים משלו
(`--c-primary`), ואנחנו בשמות הרשמיים של אוצריא (`--color-primary`). מעתיקים
**את המידות והמבנה**, ומחליפים לטוקנים שלנו.

---

## 🔴 אילוץ חוסם — לקרוא לפני שנוגעים ב-CSS

לתוסף יש את התגית **"מראה תואם לאוצריא"** בחנות. גילינו בדרך הקשה
(גרסה 3.2.1) שברגע שיש את התגית, **אובדן תאימות מחזיר HTTP 400 וחוסם את
הפרסום כולו** — לא רק מוריד את התגית. כלומר CSS לא-תקין = אי אפשר לשחרר.

הכללים שהחנות אוכפת:

| אסור | מותר |
|---|---|
| `#hex` / `rgb()` / `rgba()` מחוץ ל-`:root` | `var(--color-*)`, ו-hex **בתוך** `:root` בלבד |
| `font-size` ב-`px` | `em` / `rem` |
| `border-radius` ב-`px` | `var(--radius-sm/md/lg/pill)` |
| `font-family` מפורש | `var(--font-main)` |

שני פתרונות שכבר עבדו אצלנו כשהיינו חייבים ערך גולמי:
* צבע שקוף על טוקן → `color-mix(in srgb, var(--color-x) 12%, transparent)`
  (הוולידטור פוסל `rgba(var(--x-rgb), .12)` כ"מקודד", אבל `color-mix` עובר).
* ערכים שחייבים להיות קבועים (למשל צבעי הדפסה שחור-על-לבן) → להגדיר אותם
  כטוקנים ב-`:root` (`--print-fg` וכו׳) ולהשתמש ב-`var()` בגוף הקובץ.

**היחיד שמותר לו `px` ב-`font-size` הוא כותרת הסרגל העליון** — זו דרישה
מפורשת ב-DESIGN_GUIDE (גודל הטקסט של הספר מגיע עד 36px ויפוצץ את הסרגל).

בדיקה מהירה לפני כל דחיפה:

```bash
node -e "const s=require('fs').readFileSync('shell/router.css','utf8').replace(/:root[^{]*\{[\s\S]*?\n\}/g,''); console.log('hex:',(s.match(/#[0-9a-fA-F]{3,8}\b/g)||[]).length,'px-font:',(s.match(/font-size:\s*[0-9.]+px/g)||[]).length)"
```
התוצאה התקינה: `hex: 0 px-font: 1` (ה-1 הוא כותרת הסרגל).

---

## מה כבר נעשה (פאנל ההגדרות בלבד)

* `.set-group` — כרטיס מעוגל, `--radius-lg`, כותרת ב-`--color-primary` מודגשת
* `.set-opts` / `.set-opt` — segmented control מחובר; הנבחר ב-`--color-secondary-container`
* `.dropdown-field` — דרופדאון מעוגל, חץ מצויר ב-CSS
* `.set-action` — כפתור פעולה בצורת גלולה
* הטוקנים `--color-secondary-container` / `--color-on-secondary-container` נוספו
  ל-`:root` (אור+כהה) ומוזרקים מערכת אוצריא ב-`shell/settings.js`

## מה נשאר — לפי סדר תועלת

### 1. 🔴 מתג toggle עגול — **הפער הגדול ביותר**

זה "הריבועים" שהתלוננו עליהם. כל תיבות הסימון בתוסף הן עדיין
`<input type="checkbox">` **ילידיות ומרובעות**: שני המתגים של הפרסום ליומן,
"תמונות בכרטיסים", וכל `.set-switch` אחר.

הצורה של אוצריא היא מתג גלולה 46×26 עם בוהן שמחליקה:

```css
.switch {
  width: 46px; height: 26px;
  border-radius: var(--radius-pill);
  border: 2px solid var(--color-outline);
  background: transparent;
  position: relative; cursor: pointer; flex: 0 0 auto;
  transition: background .2s, border-color .2s;
}
/* inset-inline-start ולא left: ב-RTL הבוהן מתחילה בימין (כבוי) ומחליקה שמאלה */
.switch::after {
  content: ""; position: absolute; top: 3px; inset-inline-start: 3px;
  width: 16px; height: 16px; border-radius: 50%;
  background: var(--color-outline); transition: .2s;
}
.switch.on { background: var(--color-primary); border-color: var(--color-primary); }
.switch.on::after { inset-inline-start: 23px; background: var(--color-on-primary); }
```

⚠️ זה `<button role="switch" aria-checked>` ולא `<input>`, ולכן **צריך גם
לשנות את ה-JS**: כל מקום שקורא `el.checked` / מאזין ל-`change` צריך לעבור
ל-`classList.contains('on')` ו-`click`. הקבצים: `shell/settings.js`
(`syncSettingsUI` + החיווט), ו-`index.html` (הסימון עצמו).
**להשאיר `aria-checked` מסונכרן** — אחרת קורא מסך לא יידע את המצב.

### 2. `.settings-row` — מבנה שורות בתוך הכרטיס

אצל סידורון כל שורה בכרטיס מופרדת בקו, ויש לה כותרת + תת-כותרת:

```css
.settings-card-body > .settings-row + .settings-row {
  border-top: 1.5px solid var(--color-surface-container-highest);
}
.settings-row { padding: 12px 16px; }
.settings-row.toggle-row { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
.settings-row-title    { font-size: 1em; }
.settings-row-subtitle { font-size: .8em; color: var(--color-on-surface-dim); margin-top: 2px; }
```

אצלנו הכל שטוח בתוך `.set-group`. זה גם מקום טוב להזיז אליו את ההסברים
שיושבים היום כ-`.set-note` תלושים.

### 3. גובה אחיד 40px לפקדים

אצל סידורון כל `select` וכל כפתור segmented הוא בדיוק `height: 40px`.
אצלנו הגובה נגזר מ-padding ולכן משתנה בין פקד לפקד. שינוי קטן, אפקט גדול.

### 4. גוון רקע הדרופדאון

אצלו: `onSurface` בשקיפות 7% — אפרפר ניטרלי.
אצלנו: `--color-primary-subtle` — גוון זהב.
אם רוצים את המראה שלו: להוסיף טוקן
`--color-on-surface-subtle: color-mix(in srgb, var(--color-on-surface) 7%, transparent)`.

### 5. `.tool-btn` — כפתורי אייקון עגולים

```css
.tool-btn {
  width: 38px; height: 38px; border: none; border-radius: 50%;
  background: transparent; color: var(--color-on-surface-dim);
  display: inline-flex; align-items: center; justify-content: center;
  cursor: pointer; transition: background .15s;
}
.tool-btn:hover { background: var(--color-primary-subtle); color: var(--color-on-surface); }
```
מיועד ל-`#settingsCloseBtn` (היום ✕ חשוף), ולכפתורי הכלים בסרגלים.

---

## 🎯 החלק החשוב: להחיל על **כל** התוסף, לא רק על ההגדרות

עד היום העיצוב הזה נעצר בפאנל ההגדרות. המשימה האמיתית היא להפוך אותו
לשפה אחידה. איפה זה נוגע:

| אזור | קובץ | מה לעשות |
|---|---|---|
| בורר הספר | `index.html` + `.book-choice*` | האריחים כבר קרובים; ליישר גובה ורדיוס לתקן |
| פאנלים (`.panel-box`) | `router.css` | מבנה `settings-card`: כותרת ב-primary, שורות עם מפרידים |
| כרטיסי עמוד השער (`.card`) | `router.css` | לוודא רדיוס ומרווח אחידים עם הכרטיסים החדשים |
| האזור האישי | `personal.js` + `router.css` | הכי "ישן" בתוסף — טפסים וכפתורים בסגנון קודם |
| טפסי עריכה | `edit-forms.js` | `.f-input` / `.f-textarea` → גובה 40px ורדיוס תקני |
| שלושת מדריכי הכלים | `guides/talmud-tools/*` | **האחדה מלאה לשפת אוצריא** — ר׳ עדכון למטה |

⚠️ **עודכן 02/09/2026 — בעל הפרויקט הפך את ההחלטה.** עד כה נכתב כאן "אל
תאחיד את `guides/talmud-tools/` — פלטת קלף עצמאית בכוונה". **זה בוטל:**
שלושת המדריכים (חולין, סוכה-לולב, מומים-בכורות) **כן עוברים האחדה מלאה**
לשפת העיצוב של אוצריא — אותם טוקנים, כרטיסים, מתגי גלולה ודרופדאונים כמו
שאר התוסף. ההתאמה הטכנית לתגית שכבר נעשתה שם (צבעים גולמיים → טוקנים,
`px` → `em`) היא בסיס טוב, אבל היעד עכשיו הוא מראה אחיד, לא שימור פלטת הקלף.

## סדר עבודה מומלץ

1. להוסיף את הטוקנים החסרים ל-`:root` (`--color-on-surface-subtle`)
2. `.switch` — CSS + החלפת ה-`<input>`-ים + עדכון ה-JS (הכי הרבה ערך)
3. `.settings-row` + גובה 40px — ליטוש הפאנל
4. `.tool-btn`
5. רק אז להתפשט החוצה לפאנלים ולאזור האישי

**אחרי כל שלב:** להריץ את בדיקת ה-hex/px למעלה, ולוודא שהפרסום לחנות עובר
עם `0 הערות עיצוב`.

## איך לבדוק בלי אוצריא

`_serve.ps1` מרים שרת מקומי, ואפשר לפתוח את `index.html` בדפדפן רגיל.
מה שדורש את ה-API של אוצריא לא יעבוד שם, אבל **כל העיצוב כן** — וזה מספיק
למשימה הזו. לבדוק בשתי ערכות (בהיר/כהה) ובשני רוחבים (13.3″ ומסך צר),
כי חלק מהכללים תלויים ב-`@media`.

---

# נספח ב׳ — בורר הגופנים ותצוגת שם ה׳ (נרשם 31/08/2026) `#58`

השוואה שנעשתה מול שני התוספים שאנחנו לומדים מהם — **סידורון 3.7.0** ו**שני
טורים 5.0.0** — כדי להחליט מה כדאי לאמץ. שניהם **לא בוצעו**; זו משימה למי
שממשיך.

## ב.1 — רשימת הגופנים

| | מה יש | איך |
|---|---|---|
| **אצלנו** (`shell/settings.js`, `FONT_STACKS`) | 6 קבועות: כמו-באוצריא, היבו, פרנק־ריהל, דוד, אריאל, טיימס | דרופדאון קבוע ב-`index.html` |
| **סידורון** (`js/app.js`, `buildSelect('set-font')`) | 5: ברירת מחדל, פרנק רוהל, דוד, טעמי פרנק, שופר — **עבריות בלבד** | `dropdown-field` בתוך `settings-row stacked` |
| **שני טורים** (`index.html`, `detectAvailableFontsViaCanvas`) | ~60 שמות, ומהם מוצגות **רק אלה שמותקנות בפועל** במערכת | זיהוי דינמי ב-canvas |

**מה לאמץ — שילוב של השניים:**

1. **המראה מסידורון.** `settings-card` → `settings-row stacked` → `dropdown-field`.
   זה כבר מה שהמסמך הזה מתאר בגוף, ואין צורך בכלום נוסף.
2. **הסינון משני טורים.** `detectAvailableFontsViaCanvas` — מדידת רוחב מחרוזת
   בגופן מבוקש מול גופן נפילה; שונה = הגופן מותקן. בלי זה מוצגות למשתמש
   אפשרויות שאצלו פשוט לא קיימות, והוא בוחר ולא רואה שינוי.
3. **לזרוק את ״אריאל״ ו״טיימס״.** גופנים לטיניים על טקסט מנוקד נראים רע —
   הניקוד יושב לא במקום. במקומם: **טעמי פרנק** (`TaameyFrankCLM`) ו**שופר**,
   שסידורון בחר, ואפשר גם את משפחת **גוטמן** שקיימת ברשימה של שני טורים.

## ב.2 — תצוגת שם ה׳ — הפער הבולט מבין השניים

**אצלנו אין בחירה בכלל.** `shell/core.js:124` מריץ את `ShemShomrer.replaceHolyNames`
על כל טקסט, תמיד. מי שרוצה לראות את הכתיב כפי שהוא בפסוק — אין לו דרך.

**בסידורון** יש `set-divine`: segmented control עם ארבעה מצבים
(`js/app.js`, `DIVINE_NAMES`):

```js
var DIVINE_NAMES = [
  ['yy', 'יְיָ'], ['yedovid', 'יְדֹוָד'], ['hashem', 'ה׳'], ['source', 'יהוה'],
];
```

`renderDivineName` מפרק את השם לקבוצות אות+ניקוד (`letterGroups`) ומרכיב מחדש,
כך שהניקוד המקורי נשמר גם אחרי ההחלפה.

**מה לעשות אצלנו:**

* `segmented` אחד בפאנל ההגדרות תחת ״תצוגה״, עם אותם ארבעה מצבים.
* תנאי אחד ב-`core.js:124`: אם המצב הוא `source` — להחזיר את הטקסט כמות שהוא
  בלי לקרוא ל-ShemShomrer; אחרת להעביר את המצב הלאה.
* ⚠️ `shell/holy-names-lib.js` הועתק כלשונו מ״שומר השם״ ו**אסור לגעת בו** (ר׳
  ההערה בראשו). ההרחבה שייכת ל-`core.js` או לקובץ קורא חדש, לא לספרייה.

זו המשימה **הכי קטנה והכי גלויה** מבין השתיים: פקד אחד ותנאי אחד.

---

# נספח ג׳ — מקור אמת אמיתי: Otzaria_Offline_update (נרשם 31/08/2026)

עד כאן ("סידורון") היה הפרשנות הכי טובה שהייתה לנו — תוסף חיצוני שמנסה להידמות
לאוצריא. **נמצא מקור טוב יותר:** `Otzaria/Otzaria_Offline_update` — לאנצ'ר
רשמי של אוצריא, שקוד העיצוב שלו **מועתק ישירות מקוד המקור** של אוצריא עצמה
(`otzaria/lib/theme/...`, `otzaria/lib/widgets/...`, `otzaria/lib/settings/widgets/...`).
כל קובץ שם פותח בהערה מפורשת "פורט מ-otzaria/lib/...". זה לא ניחוש —
זה המקור.

**רלוונטי:** [Release v0.11.0](https://github.com/Otzaria/Otzaria_Offline_update/releases/tag/v0.11.0),
קבצים תחת `launcher_app/lib/src/theme/` ו-`launcher_app/lib/src/widgets/`.

## מה זה מתקן אצלנו — כבר בוצע

* **רדיוס הדרופדאון:** `app_tokens.dart` — "מקור אמת יחיד: כל הפינות **8**"
  (`radius = 8`, קבוע יחיד לכרטיסים/כפתורים/דרופדאון ב-`AppThemeData._build`).
  אצלנו `.dropdown-field select` היה `--radius-pill` (מסידורון) — **תוקן ל-
  `--radius-sm` (8px)**, 31/08/2026.

## מה זה מאשר שכבר תיארנו נכון

* **`AppDropdownField`** — גובה ברירת מחדל **40px בדיוק** (`height ?? 40`) —
  בדיוק מה שסעיף "מה לא בוצע" למעלה כבר ציין כפער (סעיף 4, "גובה אחיד 40px").
* **`SettingsCard`** — קיים בשם הזה ממש: "כרטיס הגדרות מעוצב בסגנון M3 —
  כותרת מעל הכרטיס, שורות בתוכו".
* **`CustomSwitch`** — מתג M3 אמיתי (לא CSS מחקה): thumb מחליק, track
  `primary` כשדלוק / `surfaceContainerHighest` כבוי, thumb `onPrimary`/`outline`.
  זה בדיוק הכיוון של "מתג הגלולה" שכבר מתועד למעלה כפער #1 — עכשיו יש
  ספר צבעים מדויק להעתיק ממנו במקום לנחש.
* **`AppSegmentedControl`** — קיים, גנרי, "החלופה היחידה לקבוצת RadioButton
  בין 2–4 אפשרויות" — תואם למה שכבר אימצנו.

## תיקון נוסף שעדיין **לא בוצע** — לתשומת לב מי שממשיך

* **צבע הדרופדאון/כפתורים.** `AppDropdownField` בנוי על `FilledButton.tonal`,
  שצבעו התקני ב-Material 3 הוא **`secondaryContainer`** — לא `primary-subtle`
  שאנחנו משתמשים בו היום (`--color-primary-subtle`, שורה ~1494 ב-`router.css`).
  זה בדיוק מה שסעיף "מה לא בוצע" למעלה כבר קרא לו "גוון הדרופדאון" (פער #3),
  עם ההשערה השגויה שזה `onSurface 7%` — המקור האמיתי אומר `secondaryContainer`.
* **`AppTextStyles.settingTitle`** — 16sp לכותרת שורת הגדרה, `settingSubtitle`
  13sp לתת-הכותרת. שימושי כשמיישמים את `.settings-row` (פער #2 למעלה).
* **`AppTokens.spaceMD` = 16, `spaceSM` = 8** — לפאדינג העקבי בפקדים, אם
  רוצים להעתיק גם את המרווחים ולא רק את הצבעים/הרדיוס.

**סדר עדיפות מומלץ להמשך:** קודם צבע הדרופדאון (`secondaryContainer` במקום
`primary-subtle`) — שינוי טוקן קטן וברור; אחר כך מתג הגלולה עם צבעי
`CustomSwitch`; שאר סעיף "מה לא בוצע" למעלה כמתוכנן.

---

# נספח ד׳ — תוצאות שלבי המחקר (2–4) + הכרעות בעל הפרויקט (נרשם 02/09/2026)

מפרט ע.1א ב-`ROADMAP.md` מונה תשעה שלבים. **שלבים 2–4 (שאיבה מהמקורות,
לימוד מבנה התוסף, מסמך "מה משתנה") בוצעו כמחקר** — זה מה שנשאב, ואלה
ההכרעות. **שלבים 5–9 (בנייה, דיבוג, תיעוד סופי) עדיין פתוחים** — זו
המשימה שמחכה בתור למצטרפים, במקביל למנוע הזיהוי v2.

## ד.1 — מה נשאב (מקור: `Otzaria/Otzaria_Offline_update`, ענף `main`, 02/09/2026)

| קובץ | מה יש שם | מסקנה אצלנו |
|---|---|---|
| `theme/app_tokens.dart` | `radius`=8 · `space` 4/8/16/24/32 · `font` 12/14/16/18 · `anim` 150/250/400ms · `elevation` 0/1/3 | `--radius-sm:8px` ✅ תואם. חסר: סולם מרווחים ואנימציה ב-`:root` |
| `widgets/custom_switch.dart` | `Switch` M3: כבוי track=`surfaceContainerHighest` outline=`outline` thumb=`outline`; דלוק track=`primary` outline שקוף thumb=`onPrimary`; מושבת track@12% thumb `onSurface`@38%. הבוהן גדֵלה 16→24px | פער #1 — לבנות `.switch` מדויק לפי הצבעים האלה |
| `widgets/app_dropdown_field.dart` | `FilledButton.tonal` → רקע `secondaryContainer` · `minimumSize:(0, height ?? 40)` · פדינג `fromSTEB(16,0,8,0)` · חץ `chevron_down` 18px | פער #2 — לעבור מ-`primary-subtle` ל-`secondary-container`, לנעול גובה 40 |
| `widgets/settings_card.dart` + `widgets/app_card.dart` | כותרת סעיף **מעל** הכרטיס (container נפרד). שורות מופרדות ב-`SizedBox` **שקוף 1.5px** — רווח בצבע רקע העמוד, לא קו. שורה: פדינג 16 אופקי / 12 אנכי, רספונסיבי (רחב=`ListTile`, צר=סטאק). `settingTitle` 16sp, `settingSubtitle` 13sp `onSurfaceVariant`. פקטוריז: `.switchTile/.dropdownTile/.segmentedTile` | פערים #2/#5 — `.set-group` הופך למיכל שקוף, כותרת יוצאת החוצה, שורות = משטחים נפרדים |
| `widgets/segmented_control.dart`, `action_buttons.dart`, `app_dialogs.dart` (רקע `surfaceContainerHigh`, radius 8), `status_chip.dart`, `nav_rail_item.dart`, `color_picker_tile.dart`, `info_rows.dart`, `app_title_bar.dart` | קומפוננטות נוספות | לפירוט בשלב 4 של הבנייה בפועל |

**צבעים:** כולם נגזרים מ-`ColorScheme.fromSeed` (M3) — אין hex קשיח לשאוב,
רק שמות תפקידים. אנחנו כבר ממפים את רובם ב-`:root` וב-`shell/settings.js`.

## ד.2 — שלושת הפערים הגדולים: CSS מוכן

### פער #1 — מתג גלולה `.switch` (3 צ'קבוקסים ב-`index.html`: `setCardImg`, `setPubEvents`, `setPubChurban`)

```css
.switch {
  --sw-w: 46px; --sw-h: 26px; --sw-thumb: 16px; --sw-gap: 3px;
  inline-size: var(--sw-w); block-size: var(--sw-h);
  border-radius: var(--radius-pill);
  border: 2px solid var(--color-outline);
  background: var(--color-surface-container-highest);
  position: relative; flex: 0 0 auto; cursor: pointer; padding: 0;
  transition: background .2s, border-color .2s;
}
.switch::after {
  content: ""; position: absolute; inset-block-start: 50%;
  inset-inline-start: var(--sw-gap);
  inline-size: var(--sw-thumb); block-size: var(--sw-thumb);
  border-radius: 50%; background: var(--color-outline);
  transform: translateY(-50%);
  transition: inset-inline-start .2s, inline-size .2s, background .2s;
}
.switch[aria-checked="true"] { background: var(--color-primary); border-color: var(--color-primary); }
.switch[aria-checked="true"]::after {
  --sw-thumb: 20px;
  inset-inline-start: calc(var(--sw-w) - 20px - var(--sw-gap) - 4px);
  background: var(--color-on-primary);
}
.switch:disabled {
  background: color-mix(in srgb, var(--color-surface-container-highest) 12%, transparent);
  border-color: transparent; cursor: default;
}
.switch:focus-visible { outline: 2px solid var(--color-primary); outline-offset: 2px; }
```

**HTML:** `<input type="checkbox" id="setCardImg" checked>` →
`<button type="button" role="switch" aria-checked="true" id="setCardImg" class="switch"></button>`

**JS (`shell/settings.js`):** עוזרים במקום `.checked` —
```js
const isOn  = el => el.getAttribute('aria-checked') === 'true';
const setOn = (el, v) => el.setAttribute('aria-checked', v ? 'true' : 'false');
```
`syncSettingsUI` (`:160-172`): `x.checked = …` → `setOn(x, …)`.
חיווט (`:276,311,313`): `'change' … .checked` → `'click'` שמחליף `aria-checked` ואז `setPref`.
`aria-checked` תמיד מסונכרן → קורא מסך תקין.

### פער #2 — גוון הדרופדאון + גובה 40 (`shell/router.css:1497` `.dropdown-field select`)

```css
.dropdown-field select {
  min-block-size: 40px;
  padding-block: 0;
  padding-inline: 16px 34px;
  background: var(--color-secondary-container);      /* היה --color-primary-subtle */
  color: var(--color-on-secondary-container);
  border-radius: var(--radius-sm);
  display: flex; align-items: center;
}
.dropdown-field select:hover {
  background: color-mix(in srgb, var(--color-on-secondary-container) 8%, var(--color-secondary-container));
}
/* בונוס עקביות: .set-action מ-pill ל-8px */
.set-action { border-radius: var(--radius-sm); min-block-size: 40px; }
```
החץ המצויר, `appearance:none` והפוקוס — ללא שינוי. חל על שלושת הדרופדאונים.

### פער #3/#5 — `.settings-row` כרטיס מקוטע (`shell/router.css:1428` + markup ב-`index.html`)

```css
.set-group > .set-title {
  display: block; margin: 0 4px 8px;
  font-size: 1.06rem; font-weight: 700; color: var(--color-primary);
}
.set-group {
  background: transparent; border: none; padding: 0;
  border-radius: var(--radius-sm); overflow: hidden;
  margin-bottom: 18px;
  display: flex; flex-direction: column;
  gap: 1.5px;                       /* מפריד = רווח, לא קו (app_card.dart) */
}
.settings-row { background: var(--color-surface); padding: 12px 16px; }
.settings-row.toggle-row { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
.settings-row-title    { font-size: 1.06rem; color: var(--color-on-surface); }
.settings-row-subtitle { font-size: .86rem; color: var(--color-on-surface-dim); margin-top: 2px; }
```
markup: עוטפים כל שורה ב-`.settings-row`, מעבירים את ה-`.set-note` פנימה כ-`.settings-row-subtitle`.

## ד.3 — הכרעות בעל הפרויקט (02/09/2026)

1. **מפריד שורות = רווח שקוף 1.5px** (כמו `app_card.dart`), **לא** קו `border-top`
   בסגנון סידורון. אם בכל זאת רוצים קו: `gap:0` +
   `.settings-row + .settings-row { border-top: 1px solid var(--color-outline-faint); }`.
2. **`guides/talmud-tools/` עוברים האחדה מלאה** לשפת אוצריא (ר׳ העדכון בגוף
   המסמך למעלה — ההערה "אל תאחיד" בוטלה).

## ד.4 — הערכת עלות והמלצת פיצול (לשלבים 5–9)

לא סקריפט — עבודת עיצוב עם דיבוג ויזואלי (בהיר/כהה × 2 רוחבים, איטרציות).

| שלב | תוכן | הערכה |
|---|---|---|
| 5 | 3 הדוגמאות + `.tool-btn` + גובה 40 + טוקני `:root` (`router.css`, `index.html`, `settings.js`) | ~150–250K |
| 6 | פאנלים · עמוד שער · אזור אישי (`personal.js` 92KB) · טפסי עריכה · בורר ספר · **+ האחדת 3 מדריכי תלמוד** | ~500–800K |
| 7 | דיבוג ויזואלי | ~150–300K |
| 8 | תיעוד (מפרט · ROADMAP · מסמך "עיצוב לתוספים אחרים") | ~50–80K |

**סה"כ ~0.9M–1.4M טוקן — יותר מדי לשיחה אחת.** מומלץ: שיחה נפרדת לכל שלב;
שלב 6 עצמו לפצל (אזור אישי / פאנלים / talmud-tools). אחרי כל שלב: בדיקת
ה-hex/px, ואריזה ניסיונית → `0 הערות עיצוב`.

---

# נספח ה׳ — סיכום ביצוע שלבים 5–9 (02/09/2026)

כל השלבים 5 עד 9 בוצעו והושלמו בהצלחה מלאה:

1. **שלב 5 (פאנל ההגדרות ורכיבי מפתח):**
   - הוספת טוקני מרווחים (`--space-xs` עד `--space-xl`) וטוקני אנימציה (`--anim-fast`, `--anim-medium`, `--anim-slow`) ל-`:root`.
   - יישום מתג גלולה `.switch` ב-CSS לפי `CustomSwitch` של אוצריא (46×26px, RTL מלא דרך `inset-inline-start`, מחוות פוקוס ו-aria-checked).
   - עדכון הדרופדאונים `.dropdown-field select` לרקע `--color-secondary-container`, גובה `min-block-size: 40px`, ורדיוס `var(--radius-sm)`.
   - עדכון כפתורי `.set-action` לגובה 40px ורדיוס 8px (`var(--radius-sm)`).
   - הוספת מחלקת `.tool-btn` (38×38px, עיגול 50%) לכפתור סגירת ההגדרות.
   - עדכון מבנה ה-HTML של פאנל ההגדרות לשימוש ב-`.settings-row` עם רווח שקוף של 1.5px (הכרעת בעל הפרויקט), מתגי `.switch` מבוססי כפתורים, ותתי-כותרות `.settings-row-subtitle`.
   - עדכון `shell/settings.js` עם פונקציות עזר `isOn` / `setOn` וחיווט אירועי `click` על מתגי `.switch`.

2. **שלב 6 (האחדת שאר המערכת ומדריכי התלמוד):**
   - כרטיסי הרשימה (`.card`, `.book-choice`, `.book-choice-row`) עודכנו לרדיוס אחיד של 8px (`var(--radius-sm)`).
   - פקדי פאנלים וטפסים (`.panel-box select`, `.panel-box input`, `.panel-box textarea`, `.f-input`, `.nf-btn`, `.panel-btn`) הותאמו לגובה אחיד של 40px, צבעי מיכל משני (`--color-secondary-container`), ורדיוס 8px.
   - מדריכי התלמוד (`guides/talmud-tools/`):
     - `mumim-bechorot/style.css`: החלפת הפלטה הסגולה בפלטת אוצריא תקנית ותמיכה ב-dark mode.
     - `chullin/index.html` ו-`sukkah-lulav/index.html`: החלפת פלטת הקלף בפלטת אוצריא מלאה, רקע נקי וחלק, ורדיוסים אחידים (8px לכרטיסים ו-16px למודלים).

3. **שלב 7 (בדיקות אימות):**
   - אימות CSS: `hex: 0, px-font: 1 (כותרת הסרגל בלבד), px-radius: 0` בכל קבצי ה-CSS של התוסף. עומד ב-100% באילוץ החוסם של חנות התוספים!

4. **שלב 8 (תיעוד):**
   - סימון השלבים כהושלמו במפרט זה.
   - כתיבת [הנחיות-עיצוב-לתוספי-אוצריא.md](הנחיות-עיצוב-לתוספי-אוצריא.md).
   - עדכון סעיפים ע.1 ו-ע.1א ב-`ROADMAP.md`.

5. **שלב 9 (אריזה ושחרור גרסה):**
   - אריזת גרסה חדשה עם `build/pack.ps1 -Minor -AllowValidationErrors -Release`, סגירת Issue #37.

