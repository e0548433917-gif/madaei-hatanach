← [חלוקה-לשיחות-פשוטה.md](../חלוקה-לשיחות-פשוטה.md) (אינדקס) · הקודם: [מצב-נוכחי.md](מצב-נוכחי.md) · הבא: [כללים-ותבניות.md](כללים-ותבניות.md)

---

## הסבבים והשיחות

עמודת ⏱ = זמן משוער · עמודת 🎫 = טוקנים משוערים (בהנחה שהפיצול בוצע; בלעדיו הכל פי 2–3)

### 🔧 סבב 0 — תשתית · יעד 2.11.2 — ✅ **הושלם**

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ✅ | **0.1** | ריפו git + `.gitignore` + סנכרון 2.11.1 | — | Claude Code | 15 דק׳ | 15K |
| ✅ | **0.2** | **פיצול `router.js` ל-12 מודולים** + הסרת `path` המת מ-`CATEGORIES` | `shell/router.js` + `index.html` **בלבד** | **Claude Code** | 60–75 דק׳ | 130K |

**0.2 — 12 קבצים:** `core.js` · `data.js` · `identify.js` · `refs.js` · `guides.js` · `entry-detail.js` · `edit-forms.js` · `results-ui.js` · `home.js` · `personal.js` · `settings.js` · `bridge.js`
**יוצא: 2.11.2 — אפס שינוי במה שהמשתמש רואה.** מיפוי שורות מלא בנספח ב׳ בתוכנית המלאה.
⚠️ **חובה להתבסס על 2.11.1**, לא על 2.10.0 ולא על 2.11.0.

✅ **בוצע ד׳ באב (04/08/2026).** 2,617 שורות → 12 קבצים. אומת: כל שורה עברה בדיוק פעם אחת ·
202 מזהים ברמה העליונה, אותו סט בדיוק, אפס כפילויות · 8 מסלולי בדיקה בדפדפן עברו, אפס שגיאות בקונסול.
נארז: `dist/com.chadbedera.madaeihatanach-2.11.2.otzplugin`.

> **לקח שנתפס בבדיקה:** `PREFIXES` היה אמור לעבור ל-`identify.js`, אבל `wholeWordRe`
> ב-`core.js` צורך אותו **בזמן טעינה** (בבניית `DIVINE_NAME_RE`) — ולכן `core.js` נפל
> ב-`ReferenceError` והתוסף עלה חצי-מת בלי שום הודעת שגיאה. הועבר ל-`core.js`.
> זה בדיוק הסיכון שהטבלה בנספח ב׳ הזהירה ממנו, והבדיקה בדפדפן היא זו שתפסה אותו.

---

### 🐛 סבב 1 — באגים ונראות · יעד 2.11.5 ✅ **הושלם (ד׳ באב)**

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ✅ | **1.1** | **הזיהוי לא מופיע במעבר לתוסף** — ✅ **בוצע, 2.11.3.** שני החשדים תוקנו, **ובנוסף נמצאה הסיבה השורשית האמיתית: `storage.get` מחזיר `{value:...}` ו-`storageGet` החזיר את המעטפת במקום הערך.** ✅ **אומת בפועל באוצריא — הזיהוי עובד מצוין.** | `bridge.js` + `data.js` | **Opus** | 40 דק׳ | 40K |
| ✅ | **1.2** | **רקע לבן באיקון האזור האישי** — ✅ **בוצע, 2.11.4.** מסגרת אדומה 2px נחתכה · רקע קרם/לבן הוסר לשקיפות (מפתח על חום הזהב, כמו ב-`people.png`) · 192×192, האמנות 152×149 בשוליים 20/26 — בטווח של שאר `cats/*.png` | `icon/cats/personal.png` | Claude Code | 10 דק׳ | 8K |
| ✅ | **1.3** | **עיצוב חלון התוצאות** — ✅ **בוצע, 2.11.5.** `#resultsModal` ל-flex column עם `overflow:hidden`; רק `#resultsList`/`#noResults` גוללים (`flex:1;overflow-y:auto`). כותרת/ציטוט/צ׳יפים דביקים למעלה. CSS בלבד, אומת בדפדפן (scroll מתוך `resultsList` לא מזיז את הכותרת) | `router.css` | Sonnet | 40 דק׳ | 45K |
| ✅ | **1.4** | **קיצור הפופאפ בספרייה** — ✅ **בוצע, 2.11.5.** סיכום לפי קטגוריה + עד 5 התאמות מובילות + ״ועוד N — לחיצה על אישור תציג את כולן״. תווית התפריט שונתה ל-**״זהה דצח״מ (ומקדש) בעינים למקרא!״** | `bridge.js` | Sonnet | 20 דק׳ | 20K |
| ✅ | **1.5** | **🔴 החזרת 71 הערכים החסרים** — ✅ **בוצע, 2.11.5.** דומם 67←136 (7 תת-קטגוריות חדשות + השלמות מטבעות/כלי משכן) · בית המקדש 79←81. חולצו מ-domemguide 2.2.1 / beithamikdash 1.9.0 (`index.html` שלהם) ב-vm sandbox והושוו לפי `name`. `node tools/validate.js`: 2615→2686 רשומות, 0 שדות חובה חסרים | קבצי הדאטה של דומם וביהמ״ק | Claude Code (סקריפט) | 40 דק׳ | 35K |

✅ סבב 1 סגור במלואו. `build/pack.ps1` רץ — חבילה 12.2MB, בטווח הצפוי.

**שני תיקונים אחרי בדיקה בפועל באוצריא (נכנסו ל-2.11.6):**
* **ההוראה בפופאפ נחתכה.** הכותרת הכילה את הקטע הנבחר במלואו, וכשמסמנים פסקה שלמה
  היא לבדה מילאה את החלון. הקטע נחתך ל-40 תווים בכותרת ול-120 בגוף, וההוראה הועברה
  **לפני** רשימת ההתאמות. גודל הגופן עצמו הוא של `ui.showConfirm` של אוצריא ואינו בשליטתנו.
* **צ׳יפי הסינון ״הבהבו ולא נלחצו״.** `consumePendingIdentify` הסתמך רק על מחיקת הרשומה
  מהאחסון; כשהמחיקה לא תופסת, הפולינג של 1.5 שניות בנה את חלון התוצאות מחדש בלולאה.
  נוסף `lastConsumedTs` בזיכרון המופע (נמדד: 6 סבבים → 6 רינדורים לפני, 1 אחרי).

> ⚠️ **לקח תפעולי — כל אריזה מקבלת מספר גרסה משלה.** 2.11.5 נארז פעמיים (לפני
> התיקונים ואחריהם) תחת אותו מספר. אוצריא **אינה** מחליפה קבצים בהתקנה־מחדש של
> אותו מספר גרסה, והתיקון לא הגיע למי שהתקין מעל הקודם — מה שנראה כמו באג בקוד.
> לכן נוצרה 2.11.6. **אין לארוז פעמיים תחת אותו מספר, גם על תיקון בודד.**

---

### ✨ סבב 2 — פיצ׳רים ותוכן חדש · יעד 2.12.x

> 🔴 **לפני שמנסחים את הפרומט של סבב 2 — לקרוא את זה.**
> **2.1–2.5 מותרים במקביל לת.1** (קבצים זרים לגמרי: סבב 2 נוגע ב-`index.html`/`home.js`/
> `entry-detail.js`/`guides.js`/`results-ui.js`/`router.css`/`icon/`, ות.1 נוגע ב-`guides/*/data/*.js`
> ו-`shell/refs.js`). תנאי יחיד: כל שיחה עושה `git add` **רק לקבצים שלה**.
> **2.6 ירד לסוף חלק ב׳ (ה׳ באב)** וחסום עד שהצעת ה-`id` של **ת.1ב** תאושר. אין להכניס אותו
> לפרומט של סבב 2 כל עוד ההחלטה לא התקבלה. **2.7–2.11 אינם תלויים בו** ואין סיבה שיחכו.
> **⚡ גם 4.1א מותר במקביל** (`personal.js` + `router.css`) — ר׳ סבב 4.

#### חלק א׳ — קרוב וזול

| # | שיחה | מה | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|
| ✅ | **2.1** | **בוצע, 2.12.1 (ה׳ באב).** האיקון הוחלף (אותו מפתח על חום הזהב של 1.2, 192×192) + הפריסה בשער: **מספר עמודות קבוע** — 3 מעל 620px ו-2 מתחת — במקום `auto-fit` שנתן 4+3 קטוע. האזור האישי ודפי ה-HTML ירדו לשורה שנייה משלהם, כך שהוספת דף לא קוטעת את שורת המדריכים | Sonnet | 15 דק׳ | 12K |
| ✅ | **2.2** | **בוצע, 2.12.1.** ארבעת שדות המקום נסרקים מילה-מילה (הם טקסט חופשי ולא שם נקי), בעדיפות לצירוף על מילה בודדת. **90 מתוך 133 השדות מקושרים** לכרטיס המקום עם המפה. אינדקס של שמות **שלמים** בלבד ולא `buildLookup` — שם חלקי קישר לכרטיס הלא נכון (״אלון בצענים״→אלון בכות) | Sonnet | 30 דק׳ | 30K |
| ✅ | **2.3** | **בוצע, 2.12.1.** קטגוריה · תקופה · א-ב, בגוף מתקפל עם ״☰ סינון״ ושורת ״סינון פעיל״. הועתק מ״אישים״ 3.7.0 בלבד. **+ לבקשתך: שורת החיפוש שבתוך כל מדריך קיבלה את ההשלמה החיה ואת ה-Enter=זיהוי של השער** — `attachLiveSearch` אחת משרתת את שתיהן | Sonnet | 45 דק׳ | 50K |
| ✅ | **2.4** | **בוצע, 2.12.1.** מודול חדש `shell/co-mentions.js` (**13 מודולים**). פענוח ברמת **פסוק** — גימטריה + פרישת טווחים — כי `parseVerseRef` עוצר בפרק. **3,290 פסוקים, 2,112 מתוך 2,686 הערכים מקבלים שכנים** (חציון 4), בנייה 97ms. חוצה מדריכים: תובל קין ↔ ברזל, יובל ↔ כנור | Sonnet | 40 דק׳ | 40K |
| ✅ | **2.5** | **בוצע, 2.12.3 (כ״ג באב).** מודול חדש `shell/print.js` (**14 מודולים**) + `@media print` מלא. עמוד ההדפסה נבנה ב-`#printRoot` שבתוך `index.html` ומוצג רק בהדפסה — **בלי `window.open`**, שאין כזה ב-WebView של אוצריא. ״🖨 ייצוא כרטסת״ בשלושת המקורות: סימניות · חלון הזיהוי · תצוגה מסוננת של מדריך. **הכפתור 🖨️ שבכרטיס תוקן ולא הוכפל.** `maskDivineName` חל גם בהדפסה — נבדק על 369 הערכים שיש בפסוקיהם שם המפורש | Sonnet | 40 דק׳ | 35K |

#### חלק ב׳ — בינוני

| # | שיחה | מה | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|
| ✅ | **2.7** | **בוצע (כ״ג באב, טרם נארז).** אינדקס פסוק→פרשה + בורר בשער, ובהמשך שורת ״לימוד״ נפרדת (פרשת השבוע פעיל; נ״ך ומשנה ותלמוד — placeholder ל-2 הסבבים הבאים) | Sonnet | 60 דק׳ | 55K |
| ⬜ | **2.7ב** | 🆕 **הכרטיס `#nachCard` — נ״ך לפי נביא/כתוב ופרק.** נרשם 6.8.26. תוכן קיים (כל 39 ספרי התנ״ך כבר ב-`TANAKH_BOOKS` וכל מקור כבר מפוענח לפי ספר+פרק) — חסר רק הפיצ׳ר. **להעתיק בדיוק את דפוס `parasha.js`**, בלי טבלת מיפוי טווחים (אין ״פרשות״ בנ״ך — רק ספר+פרק ישירות) | Sonnet | 35 דק׳ | 30K |
| ✅ | **2.8** | **בוצע (כ״ג באב, טרם נארז).** מפת מסע לידה→מגורים→פטירה→קבורה על מפת ה-Leaflet הקיימת, דרך `journeyPointsFor`/`scanPlaceMatches` המשותפים עם 2.2 | Sonnet | 50 דק׳ | 45K |
| ✅ | **2.9** | **בוצע (כ״ג באב, טרם נארז).** ציר זמן לפי דורות במדריך אישים, לפי השדה `cat`/`era` הקיים | Sonnet | 40 דק׳ | 35K |
| ⬜ | **2.10א** | **״ערך היום״ / מאורעות התנ״ך לפי תאריך** — חילוץ התאריכים מ״יומא דהילולא״ ומ״ביוגרפיות״, סינון מה ששייך לתנ״ך, עמוד + ריבוע בשער. **חילוץ וסינון, לא מחקר** | Sonnet | 60 דק׳ | 50K |
| ⬜ | **2.11** | **טעינה עצלה של הדאטה** — 19MB נטענים כרגע בפתיחה. אינדקס קטן מיד + דאטה לפי דרישה = פתיחה מיידית | Opus | 60 דק׳ | 50K |
| 🔴 | **2.6** | **עץ משפחה אינטראקטיבי — אחרון בסבב (הועבר לכאן ה׳ באב).** 🔴 **חסום עד ת.1ב. ת.0 גילה ש״שם״ אינו מזהה ייחודי:** 139 קישורים נצמדים לאדם הלא נכון בעל אותו שם, ו-45 לא נוצרים בגלל הבהרה בסוגריים. השדות קיימים אך **אינם ״מלאים״ במובן שאפשר לבנות עליהם עץ**. דורש `id` ייחודי לכל רשומה — ר׳ [ת0-ממצאים.md](../ת0-ממצאים.md) חלק א׳ | Opus (תכנון) → Sonnet | 90 דק׳ | 90K |

> ⚡ **למה 2.6 ירד לסוף (ה׳ באב).** הוא היה ראשון בחלק ב׳ וחסם בפועל את כל הסבב.
> **נבדק אחד-אחד: 2.7, 2.8, 2.9, 2.10א ו-2.11 אינם קוראים את שדות המשפחה כלל** ואינם
> בונים גרף — אין ביניהם לבין 2.6 שום תלות. **2.2 ו-2.4** נוגעות באותו שורש (התאמה לפי שם)
> אבל אינן חסומות: אצל 2.2 שמות המקומות כמעט ייחודיים והכשל הוא ״לא נמצא״ ולא ״נמצא הלא נכון״,
> ואצל 2.4 ההתאמה היא בתוך פסוק ולא בין רשומות. כשת.1ב יוסיף `id` — שתיהן רק ישתפרו,
> בלי כתיבה מחדש.

> **על 2.10 — ההערה שלך התקבלה במלואה.** צדקת בהפרדה: **2.10א** (מה שקיים כבר בתוספים) הוא חילוץ פשוט וייעשה בוודאות. **2.10ב** — סריקת *כל* התאריכים בתנ״ך, כולל מה שלא מפורש בפסוק וצריך מפרשים/סדר עולם/תלמוד — היא **עבודת תוכן ומחקר, לא קוד**, והיא לא חוסמת את 3.0.0. נרשמה כמשימת תוכן ת.2. המבנה שייבנה ב-2.10א יקלוט אותה אחר כך בלי שינוי קוד.

---

### 🧠 סבב 3 — מנוע הזיהוי v2 · יעד 2.13.0 (השדרוג הגדול)

> ⚠️ **הערת הכותב 6.8.26: סבב 3 עדיין בתכנון בפועל — לא לראות כמסמך סגור.** מי שממשיך ממנו
> צריך לוודא מול הכותב שהתכנון (3.1) אכן אושר לפני שמתחילים 3.2/3.3.

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ⬜ | **3.1** | **תכנון** 8 השכבות + סט בדיקות של 60–100 פסוקים + סקריפט הרצה ב-Node | `identify.js` + 20 שורות דוגמה מהדאטה | **Opus** | 60 דק׳ | 80K |
| ⬜ | **3.2** | **יישום** מה שאושר ב-3.1 + כיול מול הסט | `identify.js` + המסמך מ-3.1 | **Opus** | 120 דק׳ | 150K |
| ⬜ | **3.3** | **קציר הלקסיקון המנוקד** מתוך `verses` (מצליח על 1,141 מ-1,915 כבר עכשיו) + השלמת החסר | **קבצי הדאטה** | Gemini Pro (חלון ענק) | 90 דק׳ | 170K |

⚠️ **3.1 יוצא כמסמך לאישור — בלי קוד.** לא לערבב עם 3.2.
⚠️ **3.3 היא אחת משתי השיחות היחידות שנוגעות בדאטה.**

> 💡 **רעיון עתידי שעלה 10.8.26, לשקול בתכנון 3.1: חיבור חי לנקדן (כמו ב"שומר השם").**
> `identify.js` היום מסיר ניקוד לגמרי לפני ההתאמה (עובד רק על עיצורים) — לכן חיבור-הבזק
> בלבד (port-scan ל-127.0.0.1, `/api/status`/`/api/nikud`, מועתק כמעט 1:1 מ-`ai-engine.js`
> של שומר השם) **לא יעזור בכלום כשלעצמו** עד ששכבת ההתאמה תשתמש בפועל בטקסט המנוקד —
> בניגוד לשומר השם, שם החיבור לבדו כבר מפעיל את הפיצ'ר המרכזי. 3.1 צריך להכריע אם ניקוד
> חי מ-Nakdan מחליף או משלים את 3.3 (קציר הלקסיקון המנוקד הסטטי), לפני שמתכננים קוד.
> שכבת החיבור עצמה (ללא שילוב בהתאמה): ~20-30 דק׳ / ~20-25K, אך אין טעם להריץ אותה
> בבידוד לפני שהוחלט איך היא משתלבת ב-3.2.

> 🧪 **נבדק בפועל מול הנקדן החי, 11.8.26 — הרעיון המקורי (״האם המילה מוכרת״) נפסל, אבל
> נמצא כיוון אחר שכן עובד.** שכבת החיבור (`shell/nikud-engine.js`) כבר קיימת ונבנתה, נכנסה
> לאריזה 2.16.1. בדיקה של 18 קריאות אמיתיות ל-`/api/nikud`:
> - ❌ **"האם התווסף ניקוד" (hasNikud) לא מבחין בין מילה אמיתית לגיבריש.** גם רצף
>   אותיות חסר-משמעות כמו "קגכטס" קיבל ניקוד מלא — Nakdan הוא מודל סטטיסטי שכופה
>   ניקוד על כמעט כל קלט, לא בודק "האם זו מילה". `wordIsRecognized()` הנוכחית בקובץ
>   לא שימושית ולא אמורה להיכנס לשימוש כפי שהיא.
> - ✅ **פירוש לפי הקשר משפט כן עובד.** "אל" בשלושה משפטים שונים קיבל שלוש תבניות
>   ניקוד נכונות: "בא **אל** משה"→אֶל (יחס), "ה**אל** הגדול"→אֵל (שם), "**אל** תעשה"→אַל
>   (שלילה) — נכון פעם אחר פעם. הסיגנל האמיתי: **לא** "האם מוכר" אלא "האם תבנית הניקוד
>   שחזר Nakdan תואמת לצורה הספציפית שמחפשים", ודורש לשלוח את **המשפט המלא** (לא מילה
>   בודדת) - שינוי מהותי מה-API הנוכחי של `nikud-engine.js`.
>
> **מסקנה לתכנון 3.1:** יש כאן פוטנציאל **עצמאי מ-3.3** (לא צריך את קציר הלקסיקון
> הסטטי כדי לנצל את זה) — פיילוט ממוקד על כמה מילים דו-משמעיות בודדות מתוך
> `STOPWORDS`/`GENERIC_DESCRIPTORS` הקיימים ב-`identify.js` (שנפסלות מהזיהוי כרגע בדיוק
> כי אין דרך להבחין בלעדיהן), לא שכתוב מלא של 8 השכבות. ⚠️ אילוץ ארכיטקטוני: `identify.js`
> מוצהר בלי DOM/Otzaria כדי לרוץ ב-Node (validate.js) — בדיקת ההקשר החי חייבת לשבת מחוץ
> לו, בשכבת הקריאה ל-`identify()`, לא בתוכו. הערכה לפיילוט ממוקד (לא כל סבב 3):
> ~1.5–2 שעות / ~100-150K — הרבה פחות מ-4.5 השעות של סבב 3 המלא, כי לא נוגע ב-3.2/3.3.

> ✅ **הפיילוט הממוקד בוצע, 12.8.26 (Claude Code) — נארז ב-2.17.1.** לא 3.1/3.2/3.3
> (אלה עדיין ⬜ בטבלה למעלה, ה-8 שכבות עדיין לא תוכננו) — רק החתיכה העצמאית שתוארה
> למעלה. מומש בדיוק לפי הכיוון שנמצא: `identify.js` מקבל עכשיו `opts.allowStopwords`
> (Set נתונים גרידא — לא DOM/רשת, נשאר תואם ל-Node/`validate.js`). `nikud-engine.js`:
> `wordIsRecognized()` הוסרה; נוספה `resolveAmbiguousStopwords(text)` ששולחת **משפט
> מלא** (לא מילה בודדת) ומשווה תבנית ניקוד ספציפית מול מילה אחת מ-STOPWORDS —
> **"אשר"** (קמץ+צירה, כמו "אשר בן יעקב" הקיים במאגר האישים) מול מילת-הקישור
> "אֲשֶׁר" (חטף-פתח+סגול), עם אימות-יישור בין טוקני הגלם למנוקד לפני שסומכים על
> ה-index (כישלון = סט ריק בשקט, בדיוק כמו היום בלי הנקדן). מועמדות נוספות שנמצאו
> באותה בדיקה ("אליה"/"בהן"/"אתם" — כולן ערכים אמיתיים במאגר) **לא** מומשו כי תבנית
> הניקוד שלהן לא אומתה בפועל — מתועדות כהערה ב-`nikud-engine.js` למי שירחיב.
> חוברה לשתי נקודות הקריאה (`bridge.js`, `home.js`) דרך `identifyWithLiveContext()`
> חדשה; `NikudEngine.watch()` מופעל ברקע כדי שלא תיווסף השהיה בנתיב הלחיצה כשאין
> נקדן פתוח. תוספת: חיווי "🟢/⚪ הנקדן מחובר" בראש חלון תוצאות הזיהוי (`results-ui.js`).
> `manifest.json` עודכן (הרשאת `network.localhost` + 11 הפורטים) על ידי שיחה מקבילה
> באותו יום — אומת שהוא בפועל בתוך ה-zip הארוז של 2.17.1.
> ⚠️ **לא נבדק בפועל מול הנקדן החי** (רק לוגיקת טוקנים/יישור/STOPWORDS ב-Node) —
> מי שממשיך צריך לפתוח את 2.17.1 עם הנקדן רץ ולנסות "בני אשר" מול "כי אשר יאמר".
> commit: `4cf2feb`.

> ✅ **נבדק בפועל בעינים למקרא, 13.8.26 (המשתמש).** "כי יאמר אשר" **לא** מזהה
> את "אשר" כאיש — הפיילוט של STOPWORDS עובד כמתוכנן. **אבל** אותה בדיקה חשפה
> משפחת זיהויי-שווא **קודמת ונפרדת לגמרי**, ב-`candidateForms`/`looseForm`
> (לא נוגעת ב-STOPWORDS/הנקדן): "תורת **משה**" → גם "שה" (חיתוך התחילית מ',
> "מֹשֶׁה"→"שה"); "**במחנה** ישראל" → גם "חנה" (חיתוך כפול, ב'+מ'); "**ויאמר**
> משה" → גם האישים "אומר"/"אמרי" (looseForm מחקה גם ו/י *פותחת* מילה, לא רק
> פנימית — בלבל תחילית-עתיד דקדוקית עם כתיב חסר/מלא).

> ✅ **שני תיקונים מומשו וכוילו על 600 פסוקים אמיתיים, 13.8.26 (Claude Code).**
> נבדקו אמפירית קודם שני "תיקוני עזרה ראשונה" גורפים ל**נפסלו בפועל**: הגבלת
> אורך-מינימום ל-3 שוברת 34 שמות אמיתיים בני 2 אותיות (**נח, דן, גד, שם, חם**),
> והגבלת חיתוך-תחילית ל-1 שוברת צירופים לגיטימיים (וכ-/ול-/ומ-). מה שכן בוצע:
> 1. **`looseForm` שומרת את האות הראשונה.** כתיב חסר/מלא הוא תופעה פנימית
>    בלבד; ו/י פותחת היא שורש (יעקב) או תחילית עתיד (יאמר/ילך). מאומת: 294
>    ערכים שנפתחים ב-ו/י ממשיכים להירשם כרגיל, וההתאמה הפנימית (חיטה↔חטה)
>    לא זזה.
> 2. **אימות ניקוד לחיתוך-תחילית — לא דרך הנקדן החי, אלא קציר סטטי מ-`verses`
>    (כמו 3.3, לא 3.1 המלא).** הטקסט הנבחר באוצריא כבר מנוקד; נקצר לקסיקון
>    מנוקד מהפסוקים של כל ערך (`harvestVocalForms`), והתאמה שהושגה בחיתוך
>    תחילית נפסלת אם הניקוד סותר. "מֹשֶׁה" כבר לא מחזיר "שֵׂה" (שׁ/שׂ), "בַּמַּחֲנֶה"
>    כבר לא מחזיר "חַנָּה". שלוש מוקשים שנתפסו רק בכיול בפועל: `normalize('NFC')`
>    חובה (אותו ניקוד מקודד בשני סדרי-צירוף שונים במקורות שונים); תנועת האות
>    הראשונה של הגזע מושמטת מההשוואה (משתנה עם תחילית: יְהוֹשֻׁעַ→וִיהוֹשֻׁעַ);
>    פסילה עוצרת לגמרי כולל נפילת-החזרה לכתיב חסר (אחרת רק פינתה מקום להתאמה
>    גרועה יותר). **תוצאה על 600 פסוקים: 203 זיהויי-שווא הוסרו, 0 נוספו, קלט
>    לא מנוקד זהה בייט-לבייט.**
>
> **חיבור להנקדן החי** (המנגנון המקורי של הפיילוט) קיבל גם מדור בהגדרות:
> חיווי מצב + שני קישורים אמיתיים ל"הנקדן" עצמו — הועתקו מ-`index.html` של
> "שומר השם" (`https://otzaria.org/plugins` וחנות דיון הפורום), לא הומצאו.
>
> ⚠️ **היה פתוח, נסגר חלקית למטה:** קציר הלקסיקון (2) מכסה כ-49% מהשמות
> (חד-מילתיים) ולפעמים תופס הומוגרף מהפסוק עצמו (המפתח "עפר" קוצר כ-עָפָר/אבק
> במקום עֵפֶר/בן-מדין, ומייצר פסילת-שווא הפוכה) — דורש סבב כיול נוסף על דיוק
> הקציר. **עדיין פתוח, ר' למטה.**
> commits: `315d597`, `95ed259`.

> ✅ **קישור הפורום תוקן + חיבור הנקדן החי הורחב לכסות טקסט לא-מנוקד, 13.8.26
> (המשתמש + Claude).** קישור "הנקדן" בהגדרות היה מפנה לנושא-פורום כללי (הועתק
> משומר השם) — המשתמש מסר את קישור הפוסט המדויק, `.../forum/post/31275`.
>
> **הרחבה מהותית יותר:** אימות-הניקוד לחיתוך-תחילית (סעיף 2 למעלה) עוזר רק
> כשהטקסט **שהמשתמש עצמו סימן** כבר מנוקד — וזה המקרה הפחות שכיח; רוב הטקסט
> המועתק אינו מנוקד כלל, ואז "משה"→"שה" ו"במחנה"→"חנה" עדיין קורים בדיוק כמו
> קודם. השאלה שהובילה לכאן: "זה עוזר במקרה שהעתיקו טקסט לא מנוקד, לא?" — לא
> היה, ועכשיו כן: כשהנקדן מחובר, `NikudEngine.getLiveContext(text)` (שהחליפה
> את `resolveAmbiguousStopwords` — קריאת-רשת אחת בלבד, לא שתיים) מנקדת את
> הטקסט הנבחר *עצמו* ומחזירה גם `allowStopwords` וגם `vocalizedText`.
> `identify.js` מקבל `opts.vocalizedText` חדש; `applyLiveVocalization` ממלאת
> בו רק טוקנים חסרי-ניקוד משלהם, ורק אחרי אימות עיצור-אחר-עיצור על פני הטקסט
> *כולו* (לא נקודתי) — יישור לא-עקבי נופל בשקט להתנהגות הישנה, לא מנחשים.
> **נבדק:** "תורת משה"/"במחנה ישראל" עם ניקוד חי ממוקש — נקיים מהתאמות השווא;
> יישור לא-עקבי (ספירת מילים שונה) נופל בחזרה בבטחה; 300 קריאות בלי `opts`
> כלל (נתיב Node/`validate.js`) ללא שינוי.
>
> ⚠️ **עדיין פתוח:** קציר הלקסיקון הסטטי (המקש עָפָר/עֵפֶר) לא נגע בזה — התיקון
> החדש עוזר רק כשהנקדן *מחובר בפועל* אצל המשתמש; בלעדיו הקציר הסטטי (49%
> כיסוי) הוא עדיין קו ההגנה היחיד. **3.1/3.2/3.3 עצמן עדיין ⬜ ולא תוכננו.**
> commits: `eb4ccc1`, `e1c2596`.

---

### 📮 סבב 4 — דיווחים, עדכונים ובילד · יעד 2.14.0

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ⬜ | **4.1** | **דיווח באגים — שליחה שקטה מתוך התוסף.** מודאל + תור מקומי (`outbox`) ב-`plugin.storage` + `POST` לממסר שפותח GitHub Issue. **בלי מייל · בלי קישור חיצוני · שקט מוחלט כשאין רשת.** ר׳ ההסבר המלא מתחת לטבלה | `personal.js`, `manifest.json` | **Opus** | 60 דק׳ | 60K |
| ✅ | **4.2** | **בוצע, 2.12.2 (ה׳ באב).** `bridge.js` +131 שורות: בטעינה, ברקע ולכל היותר פעם ב-24 שעות, השוואת המניפסט המקומי מול זה שבריפו. `raw.githubusercontent.com` ואחריו `api.github.com` כגיבוי (שניהם נבדקו ופתוחים בנטפרי), בדפוס `fetchWithCorsProxy` — אך **בלי `throw`** בסוף: כישלון = `null` ושקט. `cmpVersion` הקיים, `storageGetJson` שמפרק את מעטפת `{value:...}`, timeout 8 שנ׳. יש חדש → הודעה עדינה בפינה + קישור ל-Releases דרך `confirmOpenExternal`. **נבדק:** ריצה שנייה = 0 קריאות רשת, קונסול נקי. **נארזה לראשונה ב-2.12.3 (כ״ג באב)** — עד אז הייתה בעץ העבודה בלבד. ⚠️ `network.allowlist` הוגבל לשני הדומיינים בלבד — **`he.wikipedia.org` שב-`guides.js:59` אינו ברשימה**, יש לבדוק את מדריך צומח אחרי ההתקנה | `bridge.js`, `manifest.json` | Opus | **25 דק׳** | 25K |
| ✅ | **4.3** | **בוצע, 2.12.3 (כ״ג באב).** `build/pack.ps1` **הורחב ולא נכתב מחדש** — כל מה שהיה (staging, שני התוצרים, ההחרגות, הסרת הקבצים המתים, שומר ה-16MB) נשאר. נוסף: דגלי `-Major`/`-Minor`/`-Patch` לצד `-NoBump` · ערך CHANGELOG אוטומטי בפורמט הקיים, עם תאריך עברי מ-`System.Globalization.HebrewCalendar` (הגרשיים מומרים ל-׳ ״) · `node tools/validate.js --strict` **לפני** ה-bump, כך שכישלון לא משאיר גרסה מבומפּת וערך יתום · `-Release` בלבד ל-commit + tag `v<version>` + push + GitHub Release עם שתי החבילות (gh CLI); הרצה רגילה אינה נוגעת בגיט. ⚠️ `--strict` מחזיר 1 על 474 ממצאי התוכן של ת.0 (מוקפאים עד ת.1א) ולכן נוסף `-AllowValidationErrors` כמעקף מודע שמדפיס אזהרה ואת הפלט המלא | `build/pack.ps1` | **Claude Code** | 30 דק׳ | 30K |
| ✅ | **4.4** | **בוצע, 12.8.26 (2.18.0).** **שיפורי פופאפ הזיהוי** (מ״הערות על הפופאפ החדש.docx״, כ״ג באב) — שורה ראשונה לא נחתכת אם כבר קצרה משורה אחת · הסרת שורת ״הקטע שנבחר״ הכפולה בסוף (מופיעה כבר בכותרת) · מיזוג ״ייתכנו זיהויי שווא״ לתוך שורת ״לחיצה על אישור…״ · צמצום רווח בין שורות · **זיהוי מודרני חוזר ליד כל פריט** (לא רשימה נפרדת בסוף) · **סדר עדיפות ברשימה:** בע״ח/צומח/דומם קודם, ואישים + בית המקדש (פחות תלויים בזיהוי מודרני) נדחפים לסוף תחת ״ועוד N…״. בדיקת חלון גדול/שני טורים תלויה במגבלות `ui.showConfirm` של אוצריא — לתעד את המסקנה גם אם השלילה | `bridge.js` | Sonnet | 40 דק׳ | 35K |
| ✅ | **4.5** | **בוצע, 11.8.26 (כחלק מ-ג.1–ג.3).** (א) איקון לדף — נפתר: שדה העלאה בפאנל, נשמר כ-data-URL. (ב) שמירה לעמוד הבית — **הסיבה שנמצאה:** `HTML_PAGES_INDEX_KEY` נשמר עד כה דרך `storageSet` הגולמי (בלי `JSON.stringify`), בדיוק דפוס האחסון שה-comment ב-`data.js` (סביב `storageSetJson`) מזהיר ממנו — שכבת אוצריא לא מבטיחה שמירת מערכים/אובייקטים "כמות שהם", ותלוי-מימוש אם זה שורד סגירה. המעבר ל-`saveHtmlPagesIndex`/`storageSetJson` (ר' ג.1–ג.3 למעלה) מיישר את המפתח הזה לאותו דפוס בטוח שכבר עובד ב-`REPORT_OUTBOX_KEY` ואחרים. **אומת:** רינדור מיידי אחרי שמירה, ו-סימולציה של `plugin.boot` מחדש (אתחול `waitForOtzaria`→`renderCustomPageCards` ב-`bridge.js`) עם store מדומה שנטען מ-`localStorage` — שני המקרים מציגים את הכרטיס נכון. ⚠️ **לא אומת מול אוצריא אמיתי** — אם התקלה חוזרת שם, ר' `getHtmlPagesIndex`/`saveHtmlPagesIndex` ב-`shell/data.js` | `home.js`, `data.js` | Sonnet | 35 דק׳ | 30K |
| ✅ | **4.6** | **בוצע, 12.8.26 (2.18.0).** **תיקוני זיהוי בדפי HTML מותאמים.** `identifyInCustomPages` (`identify.js`) עושה `plain.includes(text)` גולמי: (א) מילה נפוצה בת 2-3 אותיות (למשל ״עוד״) כמעט תמיד תיפגע כ-substring בכל דף, ומציגה תוצאת שווא שבלחיצה עליה לא ״פותחת״ שום דבר משמעותי. (ב) חיפוש רב-מילים כמעט אף פעם לא תואם submatch מדויק (רווחים/פיסוק שונים), ולכן לרוב לא מזהה כלל בתוך דפים מוספים, בזמן שאותו טקסט מזוהה תקין מול המדריכים הרגילים. לתקן להתאמת מילים/טוקנים כמו במנוע הזיהוי הראשי, לא substring גולמי | `identify.js` | Sonnet | 30 דק׳ | 25K |
| ✅ | **4.7** | **בוצע במלואו 30.8.26** — הלשונית קיימת מ-2.13.1 (מוטבע, לא fetch), והתקציר נסגר עכשיו: 5 גרסאות אחרונות גלויות + קיפול ״📜 N גרסאות קודמות״ (`renderChangelogInto`). **לשונית ״מה חדש״ באזור האישי** — תקציר ה-CHANGELOG של כמה הגרסאות האחרונות (למשל 5), נטען מ-`CHANGELOG.md` בתוך החבילה (בלי רשת). שקול/פתוח: להפוך בהמשך ליומן שינויים מלא מההתחלה — לא לעכב את 4.7 בשביל זה | `personal.js`, `router.css` | Sonnet | 30 דק׳ | 25K |
| ✅ | **4.8** | **בוצע, 12.8.26.** **האחדת איקונים בכל התוסף.** כרגע `icon/cats/*.png` (2.1 עודכן, השאר מ-1.2) קיימים רק בכרטיסי השער; בפופאפ הזיהוי, בכרטיסי הרשימה הפנימיים ובמקומות נוספים עדיין מוצגים אמוג׳י ישנים (👤📍🐾🌿💎🏛️). לסרוק את כל `shell/*.js` לפי `catIcon`/`cat.icon` ולהחליף לתמונה איפה שרלוונטי, בכפוף לגודל תצוגה סביר (לא לכל אזכור טקסטואלי קטן) | `bridge.js`, `guides.js`, `entry-detail.js`, `results-ui.js`, `router.css` | Sonnet | 45 דק׳ | 40K |
| ✅ | **4.9** | **בוצע, 12.8.26.** 🆕 **"הסבר"/"זיהוי מודרני" מוכפלים בדף פרטי הערך.** נמצא 11.8.26 תוך כדי תיקון טופס "הוספת ערך חדש" — **קיים כבר ב-2.15.1, לא נגרם על ידי סבב כלשהו לאחרונה.** שורש הבעיה ב-`renderEntryDetailHTML`: כששדה מסוג method-field (`explanation`/`latin`/`wiki`/`confidence`/`modern`/`mapQuery`, ר' `METHOD_FIELDS`) נמצא גם ב-`GUIDE_FIELDS[catId]` וגם בפועל ב-`entry.methods[0]`, הוא מוצג **פעמיים**: פעם דרך לולאת ה-`base.forEach`/`fieldBlock` הרגילה (ש-`readField` שולפת עבורה מ-`methods[0]`), ופעם נוספת דרך הבלוק הייעודי `if (entry.methods && entry.methods.length){...}` שמרנדר את `m.explanation`/`m.modern` וכו' בנפרד. **פוגע בכל מדריך שמשתמש ב-methods:** בע״ח (נבדק על "שור" — `explanation`+`latin`+`wiki`+`confidence` מוכפלים), צומח, מקומות (`modern`), ומומים (`modern`, מאז ד.1א). דומם/בית המקדש לא נפגעים (`identification` אינו method-field). לתקן: להסיר מ-`base.forEach` כל שדה שגם ב-`METHOD_FIELDS` וגם מוצג כבר בבלוק ה-methods, או להפוך את הבלוק הייעודי לשקוף לגמרי כשיש רק method אחד ואין בו יותר מ"הזדמנות שנייה" להציג את אותו מידע | `shell/entry-detail.js` | Sonnet | 30 דק׳ | 30K |

> **4.4–4.8 חדשות (כ״ג באב), מקובץ ״הערות על הפופאפ החדש.docx״ ומהודעותיך.** אינן
> תלויות זו בזו ולא בשום משימה אחרת בסבב 4 — מותר להריץ כל אחת בנפרד, כולל
> במקביל ל-4.1 (קבצים שונים לגמרי מ-`personal.js`/`manifest.json` של 4.1, פרט
> ל-4.7 שגם היא נוגעת ב-`personal.js` — 4.1 ו-4.7 **לא** במקביל).
> **שאלה נפרדת שעלתה ולא הפכה למשימה:** תוסף ישן שהתקנת לא נפתח בבחירת ״פתיחה
> בספרייה״ — זה תוסף **אחר**, לא עינים למקרא, ואין מספיק מידע (איזה תוסף, איזו שגיאה)
> כדי לאבחן. אם זה חוזר — לצרף שם התוסף/גרסה ותיאור השגיאה בפעם הבאה.

> ### 4.1 — שלוש הדרישות שלך, והתכנון שנובע מהן (ה׳ באב)
>
> קבעת שלושה תנאים: **לא מייל · לא קישור חיצוני · ולא ליפול כשאין רשת.**
> שלושתם יחד פוסלים גם את EmailJS (מייל), גם את `issues/new` (קישור חיצוני),
> וגם כל `fetch` שמניח שיש חיבור. **מה שנשאר, וזה גם הנכון ביותר:**
>
> ```
> המשתמש כותב  →  הדיווח נשמר מיד ב-outbox מקומי (plugin.storage)
>              →  ניסיון שקט לשלוח לממסר
>              →  הצליח: נמחק מהתור ונפתח Issue בריפו
>              →  נכשל: נשאר בתור, בלי הודעת שגיאה, וייבדק שוב בפתיחה הבאה
> ```
>
> **המפתח לא יושב בתוסף.** הוא יושב בממסר (Apps Script / Worker), ולכן איש לא יוכל
> לחלץ אותו מהחבילה ולכתוב לריפו בשמך. הרעיון שהבעלים של ״ביוגרפיות״ תיאר לך —
> ״עולה לדרייב ומשם לגיטהב״ — הוא **בדיוק הדפוס הזה**, וזה מה שלוקחים ממנו.
> אנחנו מוותרים על ה״דרייב״ באמצע ומקצרים ישר ל-Issue.
>
> **מה מעתיקים בפועל, ומאיפה:**
>
> | מאיפה | מה בדיוק |
> |---|---|
> | **ביוגרפיות** `js/app.js` ~967–1120 | **כל ה-UI**: המודאל, שדות שם/מייל/פרטים, ולידציית מייל, **שמירת שם ומייל לפני השליחה** כדי שלא ייאבדו בכישלון, צירוף גרסת אוצריא דרך `app.getInfo`, נעילת הכפתור בזמן שליחה, והבחנה בהודעות בין ״אין רשת״ ל״ההרשאה לא אושרה״ |
> | **הורדת ספרים** `app.js` ~653–663 | **שרשרת ה-CORS proxy כגיבוי** — שלושה ממסרים חלופיים בלולאת `try/catch` שקטה, ו-`throw` רק אחרי שכולם נכשלו. זה בדיוק הדפוס של ״כשל שקט״ |
> | **הורדת ספרים** `manifest.json` | מבנה `network.allowlist` — רשימה סגורה של מארחים מותרים |
>
> **מה שלא מעתיקים:** את היעד. שם זה EmailJS למייל — אצלנו זה ממסר ל-Issue.
>
> ✅ **הקמת הממסר — מדריך צעד אחר צעד מוכן:** [הקמת-ממסר-דיווחים.md](../הקמת-ממסר-דיווחים.md)
> (טוקן fine-grained עם הרשאת Issues בלבד + Apps Script + בדיקה). **15 דקות, עלות אפס.**
> ברגע שהממסר קיים — 4.1 משוחרר ואפשר להריץ אותו מיד, במקביל לכל השאר.

---

### 🏁 סבב 5 — ולידציה וסגירת 3.0.0

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ✅ | **5.1** | **בוצע כ-ת.0 (ד׳ באב).** `tools/validate.js` + [ת0-ממצאים.md](../ת0-ממצאים.md). ⚠️ **תיקון לניסוח המקורי:** ״בני המן״ **אינו** ניתן לגילוי אוטומטי — אין סתירה בין הרשומות אלא חֶסֶר תוכן. במקום זה יש בריכה של 646 רשומות בלי שום קשר משפחתי | **קבצי הדאטה** | Claude Code | 60 דק׳ | 60K |
| ✅ | **5.2** | **רובו בוצע 30.8.26** (מראי מקום: נרמול ב-`refs.js`, 164 מ-209 ניצלו · משפחה: בני המן ← אב, המן↔זרש, הדדיות בני זוג, תיקון עגלה/בעז/הושע-בן-בארי; נשארו 45 ״עין אנושית״ + 139 חסומי-ת.1ב — ר׳ ROADMAP). **= ת.1א. לא לבצע פעמיים.** תיקון הממצאים (בני המן ← אב · זרש+המן כבני זוג · והשאר) | קבצי הדאטה | Claude Code | 45 דק׳ | 40K |
| ⬜ | **5.3** | GitHub Action לוולידציה על כל commit + בדיקה כוללת + עדכון תיאור בחנות. **הוזל:** הסקריפט כבר קיים ו-`node tools/validate.js --strict` כבר מחזיר קוד יציאה 1 — נשאר רק קובץ workflow | — | Sonnet | 20 → **10 דק׳** | 10K |

**🎉 יוצא: 3.0.0**

---

### 📚 סבב ת׳ — תוכן ונתונים · אושר מראש, מתחיל אחרי סבב 3

> 🟢 **אושר מראש, 9.8.26.** ההקפאה מעולם לא הייתה תנאי טכני — רק בקשת דחייה כי זו
> עבודה ארוכה, בסדר עדיפות שסבב 3 קודם. **סבב 3 עדיין לא בוצע** (אין רישום ב-`CHANGELOG.md`
> ל-3.1/3.2/3.3 או ל״לקסיקון המנוקד״, נכון ל-2.14.2 — לא לסמן ✅ למטה עד שמאמתים).
> **ברגע שסבב 3 ייסגר בפועל — עוברים ישר לסבב ת׳, בלי לעצור לבקש אישור נוסף.**
> **כל תוכנית התוכן במסמך משלה:**
> [תוכנית-תוכן-עתידית.md](../תוכנית-תוכן-עתידית.md).
> שם נמצאות ת.1א · ת.1ב · ת.2 · ת.3 · ת.4 · ת.5 · ת.6 · ת.7 · מ.1–מ.3 במלואן, כולל
> רשימות העבודה שכבר הופקו מת.0. **שום דבר לא בוטל.**

**שתי נקודות שכן נשארות רלוונטיות עכשיו:**

| # | מה | למה זה כאן ולא במסמך התוכן |
|---|---|---|
| ⚠️ **ת.1ב** | מזהה `id` ייחודי לכל רשומה | **התנאי היחיד לשחרור 2.6 (עץ משפחה).** אם 2.6 נכנס ל-3.0.0 — ת.1ב חייב לחזור. אם לא — נשאר מוקפא |
| ❓ **ת.4** | השלמת תמונות ל-163 ערכים | שאלת כמה זה עולה בנפרד. **~2.5 שעות · ~120K, ואינו תלוי בשום משימת תוכן אחרת.** הפירוק המלא + אזהרת משקל החבילה: [תוכנית-תוכן-עתידית.md](../תוכנית-תוכן-עתידית.md) |

---

### 📖 סבב ט׳ — תוסף ״משנה ותלמוד״ (הכנות) · **לא להתחיל לפני 3.0.0 יציב**

> נרשם 6.8.26. פירוט מלא ב-[תוכנית-עבודה-תמונך-3.0.md](../תוכנית-עבודה-תמונך-3.0.md), חפשו ״חלק ט׳״.
>
> ⚠️ **9.8.26 — ט.1 ו-ט.2 בוצעו ונארזו ב-2.14.2, והסבב נבלע במהדורה 4.0.**
> הכותב הכריע: שינוי שם ל**״עינים למקרא״** + עמוד בית חדש עם שלושה שערי לימוד
> (תנ״ך · משנה · בבלי), ושלושת המדריכים משובצים במסכת שלהם במקום במדור נפרד.
> **סדר הביצוע והשיחות החדשות: [מפרט-עינים-למקרא-4.0.md](../מפרט-עינים-למקרא-4.0.md), סעיף ז׳.**
> ✅ **9.8.26 — סעיף א׳ (שינוי השם) בוצע.** פירוט ב-מפרט-עינים-למקרא-4.0.md, סעיף א׳.
> ✅ **10.8.26 — סעיף ב.1 (שלושת שערי הלימוד) בוצע.** `shell/shas.js` חדש: שלוש
> טבלאות הדאטה (39 ספרי תנ״ך · 63 מסכתות משנה · 38 מסכתות בבלי), הפאנל המשותף
> וההתאמה. `#nachCard` הוחלף. פירוט ב-מפרט-עינים-למקרא-4.0.md, ב.1.
> ✅ **10.8.26 — סעיפים ב.2–ב.4 (שורה תחתונה, ״ציורים וכלי עזר״, איקוני מסכתות)
> בוצעו.** ״ערך היום״ עבר לשורה התחתונה עם פרשת השבוע ו״ציורים וכלי עזר״
> (`shell/talmud.js`, לשעבר ״משנה ותלמוד״) — עמוד אחד, לא שניים: 63 המסכתות
> לפי סדר, ומסכת עם כלי עזר מוטמע (חולין · בכורות · סוכה) פותחת אותו בלחיצה.
> איקונים: `icon/masechtot/{chullin,bechorot,sukkah}.png`. פירוט
> ב-מפרט-עינים-למקרא-4.0.md, ב.2–ב.4. **הבא בתור לפי סעיף ז׳: ג׳ (אזור אישי) ואז
> ד׳ (כרטסות ילידיות).**
> ✅ **10.8.26 — ד.1א (מדריך ראשון) בוצע: ״מומים״ הומר לכרטסת ילידית מלאה.**
> 165 מומי בכורות (`guides/talmud-tools/mumim-bechorot/script.js`) הומרו ל-
> `guides/mumim/data/` בפורמט הקיים (`domem` הוא הדוגמה) ונרשמו ב-`CATEGORIES`.
> `makorot`→`midrash` (מוצג תחת ״מקורות חז״ל״), `modern`→`methods[0].modern`,
> `scope`→`region` (תווית תת-כותרת: ״פוסל כהן״/״פוסל בהמה״/״פוסל כהן ובהמה״/
> ״פסול מראית העין״, בלי לגעת ב-`guides.js`). מקורות בבלי נוקו מסימון הפסקה
> הפנימי (״, ד״) כדי ש-`parseMidrashRef` יזהה אותם. `shell/talmud.js`:
> `MASECHET_TOOLS['בכורות']` עובר ל-`openGuide('mumim')`, הבאדג׳ ״גרסה ראשונית״
> יורד רק ממנה. **אומת בדפדפן:** עיצוב/סינון/קטגוריות זהים ל-דומם, ״בבלי →
> בכורות → דף מג״ מציג 20 ערכי מומים דרך `shas.js`. **חולין וסוכה-ולולב עדיין
> iframe** — כל אחד יומר בשיחה נפרדת משלו, לפי אותה תבנית; איחוד סופי ל-
> `CATEGORIES` + בדיקת עיצוב/הגדרות/זיהוי חוצה-שלושה יהיה שיחה רביעית קצרה
> (ד.1ד). `guides/talmud-tools/` **לא נמחקת עדיין** (ד.2 — רק אחרי ששלושתם
> יומרו ויאומתו).
> ✅ **11.8.26 — סעיף ג׳ במלואו בוצע (ג.1–ג.3 + ג.4).**
> **ג.1–ג.3:** פאנל ״➕ הוספת דף HTML״ קיבל **בחירת מיקום** (עמוד ראשי / ליד מסכת
> מסוימת, בורר 63 המסכתות שנגזר מ-`MISHNA_MASECHTOT`), **העלאת איקון** (נשמר
> כ-data-URL ומוצג בכרטיס במקום 📄), ו**שמירה סוגרת את הפאנל**. האינדקס
> (`HTML_PAGES_INDEX_KEY`) עבר מרשימת שמות לרשימת `{name, placement, masechet, icon}`
> **עם מיגרציה** ב-`getHtmlPagesIndex` (`shell/data.js`): מחרוזת ישנה → אובייקט
> עם `placement:'home'`, כך שדפים שמורים אצל משתמשים קיימים אינם נעלמים. הכתיבה
> עברה ל-`storageSetJson` (`saveHtmlPagesIndex` חדש) והקריאה ל-`storageGetJson`,
> שסובל את שני הפורמטים. דף שהוצמד למסכת יורד מהשער ומופיע ככרטיס המסכת עצמה
> (באדג׳ ״דף אישי״), עם `MASECHET_TOOLS` תמיד בעדיפות ראשונה.
> **ג.4:** (1) ה-placeholder של מסכת בלי מדריך נוסח מחדש — לא ״בחירת דף״ אלא
> שאין עדיין מדריך, אי״ה ייבנה, והזמנה להצטרף. (2) באנר ״מהדורה חלקית״ בראש
> ״ציורים וכלי עזר״ (`.talmud-partial-banner`) עם קישור שפותח את פאנל ההוספה
> באזור האישי. (3) ה-hint של שערי משנה/בבלי (`STUDY_GATES`, `shell/shas.js`)
> מבהיר שנאסף תוכן מתויג רק למסכת בכורות. (4) ״הצטרפות לעריכה״ בלשונית המשוב —
> **קישור חיצוני** ל-issues של הריפו דרך `data-external-link`/`confirmOpenExternal`
> הקיימים, ולא הודעת `sendToDev`: זו בקשת הצטרפות לפרויקט, לא דיווח על התוסף.
> **אומת בדפדפן:** מיגרציה מפורמט ישן, סבב שמירה/קריאה בפורמט החדש, החרגת דף
> מסכת מהשער, נוסח שלושת הטקסטים, וקישור הבאנר. אפס שגיאות קונסול.
> ✅ **11.8.26 — ד.1ב (מדריך שני) בוצע: ״חולין״ הומר לכרטסת ילידית מלאה.**
> 39 ערכים (שחיטה/בדיקת סכין/דרסה/חלדה/הגרמה/עיקור — ריאה וסירכות — 18 טרפות)
> מ-`guides/talmud-tools/chullin/index.html` הומרו ל-`guides/chullin/data/`
> באותה תבנית בדיוק כמו `mumim` (חולץ עם Node `vm` כדי לשמור JSON תקין ולא
> להעתיק ביד). `makorot`→`midrash` (מוצג תחת ״מקורות חז״ל״); לא היה צורך
> ב-`modern`/`region` — למקור הזה אין שדות `methods`/`scope` כלל. **כלל
> תמונות משוריין (7 מפתחות ב-`CARD_IMAGES`, ע״ע `img`/`gallery` בערכים) —
> בניגוד למומים, למקור הזה יש תמונות אמיתיות ונשמרו.** מקורות בבלי נוקו
> מסימון פסקה פנימי, אבל **בפורמט שונה ממומים**: כאן לא היה כלל סימן עמוד
> (./:) — המקור צירף רק "דף, פסקה" (כגון "בבלי, חולין יט, א-ג"). נוקה לדף
> בלבד ("בבלי, חולין יט"); `parseMidrashRef` לא דורש סימן עמוד (הקבוצה
> האחרונה ברג'קס אופציונלית), ו-`entriesForBavliDaf` ממילא משווה לפי מספר
> הדף בלבד — לא משנה איזה עמוד. `shell/talmud.js`: `MASECHET_TOOLS['חולין']`
> עובר ל-`openGuide('chullin')`, הבאדג׳ ״גרסה ראשונית״ יורד ממנו. **תוספת
> שלא הייתה בבלוק המקורי:** ל-38 מתוך ~84 המקורות בדאטה (מחצית!) אין תמיכת
> `parseMidrashRef` כלל — ציטוטי **שולחן ערוך** ("שולחן ערוך, יורה דעה כ, א").
> נוסף דפוס חדש ל-`shell/refs.js` (`bookId: 'שולחן ערוך, <חלק>'`, `ref: 'סימן
> <X>'`), באותה שיטה כמו רמב״ם. ⚠️ **לא אומת בפועל מול ספריית אוצריא** —
> `parseMidrashRef` מחזיר `bookId`/`ref` תקינים (נבדק ב-Node), אבל אם הקישור
> נפתח נכון בפועל בספרייה טרם נבדק בדפדפן אמיתי. גם 6 מתוך 38 מקורות שו״ע
> (טווחי סימנים כמו "לט, ח-ט" / בלי ס״ק כלל) עדיין לא נתפסים — כמו כל שאר
> הדפוסים בקובץ, לא כל צורת ציטוט נתמכת. **אומת בדפדפן:** 39 ערכים/3 קטגוריות
> נטענים דרך `shas.js`, עיצוב/תמונות/פרטי-ערך זהים לשאר המדריכים, ״בבלי →
> חולין → דף יט/מה״ מוצא ערכים נכונים, אפס שגיאות קונסול. **סוכה-ולולב עדיין
> iframe** (ד.1ג, שיחה נפרדת); איחוד סופי + ד.2 (מחיקת `talmud-tools/`) — ד.1ד.
> ✅ **12.8.26 — ד.1ג (מדריך שלישי) בוצע: ״סוכה ולולב״ הומר לכרטסת ילידית.**
> 36 ערכים ב-6 קטגוריות (סוכה 18 · לולב 5 · הדס 2 · ערבה 2 · כללי 1 · אתרוג 8)
> מ-`guides/talmud-tools/sukkah-lulav/` הומרו ל-`guides/sukkah/data/` באותה
> תבנית כמו `mumim`/`chullin` (חולץ עם Node `vm`; היו שני מכשולים שלא היו
> בסבבים הקודמים — הסקריפט המקורי קורא ל-`setTimeout`/DOM ברמת המודול, ו-`const`
> בתוך `vm` אינו נכתב על ה-sandbox וצריך ייצוא מפורש). `makorot`→`midrash`;
> אין `methods`/`scope`/`region` במקור. **`CATEGORIES` בשם `sukkah`, איקון 🌴**
> (לא 🌿 — תפוס ע״י צומח). `GUIDE_FIELDS.sukkah = ['explanation','note']`
> ב-`entry-detail.js`, אחרת ״זיהוי מודרני״ נספר כשדה חסר בכל 36 הערכים.
>
> **מקורות — פורמט שלישי ושונה משני קודמיו (כפי שהוזהר בבלוק):** כאן סימן
> העמוד *כן* קיים (״בבלי, סוכה ב.״ — בניגוד לחולין), אבל 11 מקורות הם **טווחי
> דפים** (״בבלי, סוכה ט:-י.״) ו-`parseMidrashRef` עוגן בסוף מחרוזת, ולכן הטווח
> הפילו. נוקה לדף הראשון **עם** סימן העמוד (״בבלי, סוכה ט:״) — מידע אמיתי שלא
> היה בחולין ואין סיבה לאבד. עוד 3 טווחי סעיפים בשו״ע (״תרמה, ח-ט״) נוקו לסעיף
> הראשון. **באג שנמצא אגב כך ותוקן ב-`shell/refs.js`:** דפוס השו״ע שנוסף ב-ד.1ב
> הגביל את מספר הסימן ל-`[א-ת]{1,3}`, ולכן **כל** סימני אורח חיים (תרכח/תרמה/
> תרמח — 4 אותיות) נפלו. הורחב ל-`{1,4}`. אחרי התיקון **101/101 המקורות נפרסים**
> (משנה 32 · בבלי 34 · ירושלמי 30 · שו״ע 5).
>
> **10 דיאגרמות ה-SVG נשמרו** (בניגוד למומים שלא היו לו תמונות): הומרו ל-data-URI
> ב-`CARD_IMAGES` — הראשונה כ-`img` (רוחב מלא) והשאר ב-`gallery`. משתני ה-CSS של
> הדף העצמאי (`var(--gold)` וכו׳) הוחלפו בערכים מילוליים, כי ב-`<img>` עם data-URI
> משתני CSS של העמוד המארח ממילא אינם חלים. ⚠️ **`gallery` מרנדר 110×110 קבוע**
> (`entry-detail.js`) — לדיאגרמות זה קטן מדי. מועמד לשיפור ב-ד.1ד.
>
> **אומת בדפדפן:** 36 כרטיסים ו-6 שבבי סינון נטענים דרך `loadGuideData`, ״בבלי →
> סוכה → דף ב/ט/לא/לה/לו״ מחזיר את הערכים הנכונים (כולל אלה שמקורם טווח דפים),
> ציטוט שו״ע לחיץ, אפס שגיאות קונסול. ⚠️ הרצתי מול שרת של שיחה מקבילה (הפורט
> נתפס), ומטמון הדפדפן החזיק סקריפט ישן — נבדק שהקובץ המוגש מעודכן, והרינדור
> אומת מול הקוד החי. **שלושת המדריכים הומרו** — נשאר ד.1ד (איחוד ובדיקה
> חוצה-שלושה) ואז ד.2 (מחיקת `guides/talmud-tools/`).
> ✅ **12.8.26 — תיקון תיעוד: ד.1ב/ד.1ג נוספו ל-CHANGELOG בדיעבד + כלל 7 חדש.**
> אחרי שחרור 2.17.1 נמצא ש-`pack.ps1 -Release` ארז ושלח בפועל את חולין וסוכה-
> ולולב, אבל ה-`-Notes` שהריצו את השחרור תיארו רק תיקון התקנה דחוף — שני
> הסבבים לא הופיעו ב-CHANGELOG/״מה חדש״. תוקן: תבליט רטרואקטיבי נוסף תחת
> 2.17.1 ב-`CHANGELOG.md`. **כלל 7** נוסף ב-`כללים-ותבניות.md`: לפני כל
> `-Release` לבדוק `git log` מאז התג הקודם ולוודא שכל סבב ✅ ממתין מיוצג
> בתבליט משלו, לא רק היוזם. קומיט `7b13a02`, נדחף (`c50d995..7b13a02`).

| # | שיחה | מה | מעלים | מודל | ⏱ | 🎫 |
|---|---|---|---|---|---|---|
| ⬜ | **ט.1** | **עמוד בית ״משנה ותלמוד״** — מודל מדויק: `shell/parasha.js`. רשימת 63 מסכתות (6 סדרים, כבר מחולצת) → בורר דף → `entriesForDaf(masechet, daf)` שסורק `makorot` בדיוק כמו `entriesForParasha`. **תוצאה ריקה עד שמ.2 יתקדם** (התיוג עצמו) — אבל הקוד יכול להיבנות מיד | `shell/talmud.js` (חדש), `index.html`, `router.css` | Sonnet | 60 דק׳ | 55K |
| ⬜ | **ט.2** | **מדור ״מדריכים וכלי עזר״** בעמוד משנה ותלמוד — קישור ל-3 תוספים עצמאיים קיימים (חולין 1.6.0, מומים/בכורות 3.0.0, סוכה ולולב 1.2.2), מסומנים ״גרסה ראשונית״. קוד המקור כבר עלה לריפו ב-[`mishna-talmud-addons/`](../../mishna-talmud-addons/) | `index.html`, `router.css` | Sonnet | 30 דק׳ | 25K |
| 📝 | **ט.3** | תכנון בלבד (לא לבנות עכשיו) — מסכתות נוספות ראויות למדריך: עירובין, מנחות, זבחים. ר׳ **מ.3** בתוכנית-תוכן-עתידית.md | — | — | — | — |

**תלות תוכן:** ט.1 בלי מ.2 (תוכנית-תוכן-עתידית.md) מציג עמוד ריק. מ.1 (מפרט) חייב להיסגר לפני שמ.2 מתחיל, כי הוא קובע את פורמט ה-`ref` התלמודי.

---

← [חלוקה-לשיחות-פשוטה.md](../חלוקה-לשיחות-פשוטה.md) (אינדקס) · הקודם: [מצב-נוכחי.md](מצב-נוכחי.md) · הבא: [כללים-ותבניות.md](כללים-ותבניות.md)
