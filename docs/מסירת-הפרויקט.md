# מסירת הפרויקט — עינים למקרא

**02/09/2026** · עודכן 04/09/2026 · חד בדרא פורש מהפיתוח. הפרויקט עובר לארגון
[`GINIM-LMKRA`](https://github.com/GINIM-LMKRA) עם מנהלים חדשים: `Y-PLONI` ו-`palmoni5`.

> # 🔴 ההעברה טרם בוצעה — ורק בעל הריפו יכול לבצע אותה
>
> **`e0548433917-gif` הוא היחיד שיכול להעביר את הריפו לארגון.** העברה של ריפו
> בבעלות חשבון פרטי היא פעולה של הבעלים בלבד — לא של שותף, גם לא כזה עם
> ההרשאה הגבוהה ביותר, וגם לא של בעלי הארגון. **זה לא ניתן להאצלה.**
>
> **וכל עוד הריפו תחת חשבון פרטי, אי־אפשר לתת לאיש `admin` עליו.** נוסה בפועל
> (04/09/2026): ה-API מחזיר `204 No Content` כאילו הצליח, וההרשאה נשארת `write`.
> סולם ההרשאות המלא — `triage`/`maintain`/`admin` — קיים רק בריפו של ארגון.
>
> **המשמעות המעשית:** עד שההעברה תתבצע, הארגון `GINIM-LMKRA` אינו נותן דבר,
> והמנהלים החדשים נשארים שותפי `write` בלבד — בלי יכולת לנהל הגדרות, סודות,
> תוויות או הרשאות. **אם בעל הריפו פורש בלי לבצע אותה, איש לא יוכל לעשות זאת
> במקומו.**
>
> **ההעברה עצמה אינה תלויה בטוקנים** — היא שתי דקות בדפדפן. הטוקנים משפיעים רק
> על האוטומציה שאחריה, וניתן לתקן אותם בכל רגע (ר׳ למטה).

---

## מצב נוכחי

| | מצב |
|---|---|
| **קוד ותיעוד** | ✅ נדחף במלואו (73MB). אין עבודה לא־מקומטת |
| **חומרי מקור + אריחי מפה** | ✅ גובו ל-`GINIM-LMKRA/madaei-hatanach-materials` (פרטי) — 199MB, 4,888 קבצים, כולל 4,587 אריחים |
| **ספרי המחקר (פליקס)** | ✅ גובו כ-Release assets באותו ריפו (מעל 100MB לקובץ — לא ניתן לדחוף כקובץ רגיל) |
| **הצטרפות מפתחים** | ✅ אוטומטית לחלוטין |
| **העברה לארגון** | 🔴 **ממתינה — רק בעל הריפו יכול לבצע** |
| **המנהלים החדשים** | ⬜ `Y-PLONI` ו-`palmoni5` הוזמנו כבעלי הארגון; שותפי `write` בריפו |
| **טוקנים** | ⬜ חובה להנפיק מחדש **לפני** ההעברה |

---

## מה נשאר לבצע, לפי הסדר

1. **לפנות את השם בארגון** — יושב שם ריפו ריק בשם `_placeholder-empty-2026-08-18`
   (שונה שמו כדי לפנות את הדרך, לא נמחק). למחוק או להשאיר.
2. **להנפיק שני PAT חדשים תחת הארגון** — ר׳ הטבלה למטה. **לפני ההעברה**, אחרת
   דיווחי המשתמשים נעצרים בשקט.
3. **להעביר את הריפו** — Settings ← Danger Zone ← Transfer ownership ← `GINIM-LMKRA`.
   **⚠️ רק `e0548433917-gif` יכול לעשות זאת. אין דרך להאציל את הצעד הזה.**
   Issues, PRs, קומיטים, תגים, Releases וכוכבים עוברים במלואם; הכתובת הישנה מקבלת
   הפניה, וקלונים קיימים ימשיכו לדחוף כרגיל.
4. **למנות את המנהלים** — People ← Owner עבור `Y-PLONI` ו-`palmoni5`. **רק אז** להסיר
   את עצמך, אחרת הארגון נשאר בלי בעלים.
5. **לשנות את שם המשתמש** (רשות) — Settings ← Account ← Change username, אחרי
   ההעברה. הקומיטים ההיסטוריים ישמרו את המייל הישן ב-289 מקומות; שכתוב היסטוריה
   בריפו פעיל אינו מומלץ.

---

## מה יישבר בהעברה — וזו הסיבה שהיא מחכה

שני הטוקנים הם **fine-grained PAT המשויכים לנתיב `e0548433917-gif/madaei-hatanach`**.
ברגע שהריפו עובר לארגון הם מאבדים גישה — ההפניות של גיטהאב לא עוזרות, כי ההרשאה
משויכת לבעלים ולא לכתובת.

| טוקן | איפה יושב | מה נשבר | התיקון |
|---|---|---|---|
| `INVITE_TOKEN` | Secret בריפו | הצטרפות מפתחים מפסיקה להזמין | PAT חדש תחת הארגון, הרשאת **Administration: Read and write** |
| `GH_TOKEN` | Google Apps Script | 🔴 **דיווחי באגים ממשתמשים אמיתיים מפסיקים להיפתח** | PAT חדש (Issues + Contents) ולעדכן את `REPO` בסקריפט |

**שתי הערות תפעוליות:**

* בארגון, fine-grained PAT דורש מדיניות מתירה — Settings ← Personal access tokens —
  ולעיתים אישור פרטני של בעלי הארגון.
* **79 קישורים מקודדים** לנתיב הישן פזורים בקוד ובתיעוד. ההפניות של גיטהאב יגרמו
  לכולם להמשיך לעבוד, ולכן זה לא דחוף — אבל שווה מעבר אחד מסודר אחרי ההעברה.

---

## כלל הממשל

כל מצטרף מקבל `push` אוטומטית. מה שמגן על המוצר הוא ההבחנה הזו:

| סוג | דוגמאות | אישור |
|---|---|---|
| ✅ **ביצוע משימה קיימת** | פריט מ-`ROADMAP.md` · באג פתוח · הרחבת תוכן במדריך קיים · תיקון נתונים | לא נדרש |
| 🔴 **שינוי מהותי** | ארכיטקטורה · מחיקת פיצ׳ר · המפרט המחייב · מחיקת משימה מ-`ROADMAP` · `manifest.json` · `.github/workflows/` · ממסר הדיווחים וסודות · אריזה ושחרור לחנות · שינוי שם ומיתוג | **מנהל** |

מבקשים ב-Issue עם התווית `דרוש-אישור`, מתייגים מנהל, **וממשיכים בינתיים למשימה
הבאה** — לא עוצרים ולא מחכים. **בספק — זה מהותי.**
הפירוט המלא ב-[`הצטרפות-מפתח.md`](הצטרפות-מפתח.md) וב-[`CLAUDE.md`](../CLAUDE.md).

---

## עצמאות מהמחשב של בעל הפרויקט

**זו הייתה מטרת כל מהלך הגיבוי, והיא הושגה.** `build/pack.ps1` אינו מכיל שום תלות
בנתיב מקומי — clone נקי של הריפו מספיק כדי לבנות את החבילה במלואה.

| מה | איפה | האם צריך את המחשב הישן |
|---|---|---|
| קוד, נתונים, מדריכים, סקריפט האריזה | הריפו הראשי | לא |
| חבילות `.otzplugin` שפורסמו | GitHub Releases | לא |
| חבילות ישנות שלא פורסמו (736MB) | — | **לא נשמרו במכוון** — נבנות מהמקור |
| אריחי מפה אופליין (87MB) | `madaei-hatanach-materials` | לא |
| חומרי מקור וטיוטות (110MB) | `madaei-hatanach-materials` | לא |
| ספרי המחקר | Release ב-`madaei-hatanach-materials` | לא |

---

## למנהלים החדשים

כל מה שצריך כדי להיכנס לעבודה נמצא בשני קבצים. **אין ידע שיושב רק אצל חד בדרא.**

| קובץ | מה יש בו |
|---|---|
| [`docs/הצטרפות-מפתח.md`](הצטרפות-מפתח.md) | מקור האמת להודעת הקבלה — נשלחת אוטומטית לכל מצטרף. תור המשימות, כלל הממשל, ובלוקי הפרומט לקלוד. **עריכה שם משנה את ההודעה לכולם** |
| [`CLAUDE.md`](../CLAUDE.md) | כללי העבודה: מה לבדוק בתחילת שיחה, מדיניות ענפים, מלכודות מוכרות, וטיפול בבקשות הצטרפות תקועות |

**תור המשימות הפתוח:**

1. **סבב 3 — מנוע הזיהוי v2** — השדרוג הגדול. פתוח וממתין.
2. **עיצוב** — שלבי המחקר הושלמו וה-CSS מוכן; פתוחים שלבי הבנייה.
3. **סבב ת׳ — תוכן ונתונים** — אושר מראש, אחרי סבב 3.

שתי הראשונות אינן נוגעות באותם קבצים — אפשר לרוץ עליהן במקביל.
