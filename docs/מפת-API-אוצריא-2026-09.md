# מפת ה-API של אוצריא מול מה שהתוסף מממש — 06/09/2026

נכתב אחרי מעבר על `docs/plugin-sdk/spec.json` בענף `dev` של
[Otzaria/otzaria](https://github.com/Otzaria/otzaria) (מחולל אוטומטית מקוד המקור,
`tool/plugins/generate_plugin_spec.dart`), והצלבה מול כל קריאות ה-API בקוד התוסף.
משלים את [`תאימות-SDK-אוצריא.md`](תאימות-SDK-אוצריא.md) שנכתב 19–28/08 — מאז היו
**שמונה עדכוני SDK** (25/08–05/09) שהוא אינו מכיר.

**המספרים:** ל-SDK יש **145 מתודות**. התוסף מממש **23**. **122 אינן בשימוש.**
קיימים 26 אירועים, התוסף מאזין ל-6.

מ-3.7.0 (06/09/2026) `minAppVersion` הוא **0.9.97**, ולכן כל מתודה בטבלאות שלמטה
זמינה לקריאה ישירה — בלי `callIfSupported` ובלי נפילה.

---

## א. מה שכבר בשימוש (23)

`app.getConnectivity` · `app.getInfo` · `app.getTheme` · `app.openUrl` ·
`feedback.report` · `feedback.sendEmail` · `library.getBookContent` ·
`library.getBookToc` · `library.getTree` · `notifications.showInApp` ·
`plugin.backgroundDone` · `plugin.openSelf` · `publishedData.remove` ·
`publishedData.upsert` · `reader.addContextMenuItem` · `reader.getCurrentState` ·
`reader.openBook` · `reader.openBookAtRef` · `shortcut.create` · `storage.get` ·
`storage.set` · `ui.showConfirm` · `ui.showError`

אירועים: `plugin.boot`, `plugin.suspended`, `plugin.resumed`,
`reader.context_menu_item_clicked`, `reader.toolbar_item_clicked`, `theme.changed`.

---

## ב. סדר עדיפויות מומלץ

לפי יחס תועלת/עלות **לתוסף הזה**, לא כרשימה גנרית.

### 🔴 1. `network.fetchStream` — הנקדן ([#47](https://github.com/e0548433917-gif/madaei-hatanach/issues/47))

`shell/nikud-engine.js:43,79` קורא `fetch()` ישיר ל-`127.0.0.1`. התיעוד דורש
במפורש שקריאות loopback יעברו דרך ה-API, כי שרת מקומי שדוחה `Origin: null` חוסם
ב-CORS. **עד 3.6.0 אי-אפשר היה** — עצם הופעת המחרוזת בקוד חסמה אריזה עם רצפת
0.9.96 (`_checkMethodVersions`). עכשיו אפשר. `network.access` ו-`network.localhost`
כבר מוצהרות. ⚠️ לאמת מול נקדן חי.

### 🔴 2. כפתור הסרגל שמזהה את מה שפתוח ([#42](https://github.com/e0548433917-gif/madaei-hatanach/issues/42))

דווח בפועל (06/09, אוצריא 0.9.97): הכפתור מעביר ללשונית **בלי לזהות**.
`openPlugin: true` אינו מוסר הקשר. שתי דרכים: `reader.getCurrentState` /
`reader.getCurrentRef` ב-boot, או `reader.addToolbarItem` (0.9.97) עם מטפל משלנו
במקום ההצהרה.

### 🔴 3. `plugin.openSelf({param})` + `plugin.page_opened` ([#52](https://github.com/e0548433917-gif/madaei-hatanach/issues/52))

מייתר את `consumePendingIdentify` ואת הפולינג של 1.5 שניות ב-`shell/bridge.js:74-150`
(~80 שורות). ה-SDK מוסר את ה-`param` לדף באירוע, ״גם אם הוא נטען רק עכשיו —
האירוע ממתין לסיום ה-boot״.

### 🟢 4. הדגשות בטקסט הספר ([#51](https://github.com/e0548433917-gif/madaei-hatanach/issues/51))

`reader.setHighlight` + `clearHighlight` + `getHighlights` + `revealHighlight`
(הרשאת `reader.highlight`, לא מוצהרת). ההמלצה מספר 1 של הביקורת הקודמת (§ג.1):
אחרי זיהוי — לצבוע בטקסט עצמו. `reader.findTextOccurrences` ו-`getSectionTextMap`
(שניהם 0.9.95, הרשאת `reader.open` שכבר יש) נותנים את כל המופעים בקטע בבת אחת.
⚠️ ההדגשות זמניות בזיכרון — התוסף חייב לשמור ב-`storage` ולהקים מחדש ב-`plugin.boot`.

### 🟢 5. `library.resolveRef` + `resolveBooks` — מראי מקום אמינים

`resolveRef` (נוסף 05/09) פותר הפניה **בלי לפתוח לשונית**; `resolveBooks` פותר עד
100 ספרים בקריאה אחת ומחזיר `id`+`type`+`source`. היום `openInReader` שולח
`bookId` מחרוזתי, שני ספרים באותו שם אינם ניתנים להבחנה, והפתיחה נכשלת בשקט.
פותר גם את הסתרת הקישור לספר שאינו מותקן. ⚠️ `getTree` מחזיר שמות זהים בכמה
מקומות (״עירובין״ בבבלי, בירושלמי ובמשנה) — חובה לשמור נתיב קטגוריה.

### 🟢 6. `search.query` — ״היכן עוד מוזכר הערך״

`AsyncIterable` עם עימוד ופאסטים (הרשאת `search.fulltext.read`). מהכרטיס ישירות,
במקום מראי מקום סטטיים בלבד.

### 🟢 7. `ui.print` / `ui.exportPdf` — הדפסה נכונה

`ui.print` קיבל פרמטרי פריסה ב-03/09 (גודל, כיוון, שוליים, רקעים), ו-`ui.exportPdf`
קיבל `pageSize` מותאם ב-01/09 (`{widthMm, heightMm}`). מחליף את ה-`@media print`
הידני של ״ערך היום״ ושל הכרטיסים. **בלי הרשאה.**

### 🟢 8. `ui.setUnsavedChanges` — עריכה בטוחה

נוסף 02/09. אזהרה לפני סגירת לשונית בעריכת כרטיס / ערך היום. **בלי הרשאה.**

### 🟡 9. `calendar.*` — תאריך עברי מהאפליקציה

`guides/_shared/dates.js` מחשב תאריך עברי לבד. `calendar.getJewishDate`,
`getSelectedDate` (התאריך שהמשתמש **בחר** בלוח) ו-`events.subscribe:calendar.date_changed`
נותנים ל״ערך היום״ להתעדכן כשהמשתמש מדפדף בלוח. בנוסף `getCities` (0.9.96),
`getHalachicTimes`, `getDailyTimes`. הרשאה: `calendar.read` — לא מוצהרת.

### 🟡 10. הפעלת הנקדן בכפתור אחד

בקשה של בעל הפרויקט (06/09). **אין ב-SDK API להרצת תוכנית חיצונית מ-WebView.**
שני כיוונים לבדיקה: `contributes.startup.programs` (״תוכניות מארחות ללא WebView״ —
שדה שלא היה קיים בביקורת של 19/08; יש לברר מה בדיוק הוא מאפשר), או `app.openUrl`
לקיצור דרך שמפעיל את הנקדן. ⚠️ לא מאומת.

### 🟡 11. קטנים ושווים

* `app.getGrantedPermissions` + `events.subscribe:plugin.permissions_changed` — לדעת בזמן אמת שהמשתמש כיבה הרשאה, במקום ״לא קורה כלום״.
* `ui.showWarning` / `showSuccess` / `showMessage` — היום הכול עובר ב-`showConfirm`/`showError`.
* `history.list` / `library.listRecentBooks` — ״הערכים והספרים שקראת לאחרונה״.
* `notifications.scheduleSystem` — תזכורת יומית ל״ערך היום״; זו גם הדרך הנכונה לתזמן ממופע רקע, ש-`setTimeout` ארוך אינו שורד בו.
* `app.registerShortcut` (0.9.97) — קיצור מקלדת אמיתי; קשור ל-[#41](https://github.com/e0548433917-gif/madaei-hatanach/issues/41), שבו `shortcut.create` (קיצור דרך בשולחן העבודה) מעולם לא אומת על מכשיר.
* `tools.gematria` / `tools.dictionary`, `bookmarks.*`, `workspace.*`, `reader.closeTab`/`activateTab`/`scrollToSection`, `fonts.resolveFamilies`, `plugin.listInstalled`/`openOther`, `settings.get`, `notes.*`, `database.query`.

---

## ג. מלכודות מאומתות — מה **לא** יעבוד

כל אחת מהן עלתה בגרסה שנשברה. המקור המלא: [`תאימות-SDK-אוצריא.md`](תאימות-SDK-אוצריא.md).

1. **`feedback.report` — לעולם לא להצהיר כהרשאה.** אינה ב-`plugin_valid_permissions.dart`; ההתקנה נכשלת מיידית (״הרשאה לא חוקית שנדרשת על ידי התוסף״). התיעוד הרשמי סותר את עצמו בנקודה הזו — המכשיר מכריע. הקריאה עובדת בלי שום הצהרה.
2. **הרשאה חסרה חוסמת פרסום, לא רק מזהירה.** `library.getTree` בלי `library.books.read` הפיל את פרסום 3.1.0 בחנות ב-HTTP 400.
3. **שש הרשאות בסיס מיותרות במניפסט:** `app.info.read`, `notifications.send`, `ui.feedback`, `plugin.storage.read`, `plugin.storage.write`, `events.subscribe:theme.changed` — מוענקות אוטומטית (`baselinePermissions` ב-`spec.json`). הן רק מנפחות את דיאלוג ההתקנה. ⚠️ לאמת מול התקנה אמיתית לפני הסרה.
4. **`network.allowlist` — כתובות מדויקות בלבד.** קידומת גנרית כמו `https://api.github.com` נדחית. ההצהרה במניפסט היא תנאי **הכרחי ולא מספיק**: הכתובת חייבת להופיע גם ב-`plugin_network_allowlist.txt` בענף `dev` של אוצריא, אחרת 403. שש הכתובות שלנו מוזגו שם ב-01/09.
5. **`app.openUrl` אינו נבדק מול ה-allowlist** (רק מול סכמת `http/https`) — אין סיבה לפתוח כתובת לרשת ה-WebView רק בשבילו.
6. **הדגשות אינן נשמרות.** זמניות בזיכרון; אחריות התוסף לשמור ולהקים מחדש.
7. **תגית ״מראה תואם לאוצריא״ ניתנת רק ב-0 הערות עיצוב.** ⚠️ הבודק סורק גם `rgba(var(--x-rgb), a)` כצבע מקודד — הפתרון `color-mix(in srgb, var(--x) N%, transparent)`. ה-`.otzignore` **אינו** משתיק הערות: הסורק עובר על תיקיית המקור רקורסיבית.
8. **`description` מעל 150 תווים חוסם התקנה** (הפיל את 2.19.4), ו-`name` חייב להיות זהה מילה-במילה ל-`contributes.toolTab.title`.
9. **השהיה ברקע נייטיבית רק ב-Windows/Android.** בשאר הפלטפורמות התוסף אחראי לעצור לבד ב-`plugin.suspended`.
10. **מעקב ל-0.9.98:** מסלול הטעינה-בעלייה הישן נמחק. 3.7.0 כבר מצהירה `contributes.startup` ולכן עומדת בזה מראש — אבל אם ההרשאה `app.run_on_startup` עצמה תוסר מרשימת ההרשאות החוקיות, הצהרה עליה תפיל התקנה. לבדוק בקדם-0.9.98.

---

## ד. הטבלה המלאה — 122 מתודות שאינן בשימוש

הרשאה ״—״ = אין צורך בהרשאה.

**app**

| API | מגרסה | הרשאה |
|---|---|---|
| `app.getGrantedPermissions` | 0.9.89 | `app.info.read` |
| `app.getLocale` | 0.9.89 | `app.info.read` |
| `app.getUserEmail` | 0.9.89 | `app.user_email.read` |
| `app.registerShortcut` | 0.9.97 | `app.shortcuts` |
| `app.unregisterShortcut` | 0.9.97 | `app.shortcuts` |
| `app.updateShortcut` | 0.9.97 | `app.shortcuts` |

**bookmarks**

| API | מגרסה | הרשאה |
|---|---|---|
| `bookmarks.add` | 0.9.97 | `bookmarks.write` |
| `bookmarks.list` | 0.9.97 | `bookmarks.read` |
| `bookmarks.remove` | 0.9.97 | `bookmarks.write` |

**calendar**

| API | מגרסה | הרשאה |
|---|---|---|
| `calendar.getCities` | 0.9.96 | `calendar.read` |
| `calendar.getDailyTimes` | 0.9.92 | `calendar.read` |
| `calendar.getEvents` | 0.9.89 | `calendar.read` |
| `calendar.getHalachicTimes` | 0.9.92 | `calendar.read` |
| `calendar.getJewishDate` | 0.9.89 | `calendar.read` |
| `calendar.getSelectedDate` | 0.9.89 | `calendar.read` |

**database**

| API | מגרסה | הרשאה |
|---|---|---|
| `database.batchQuery` | 0.9.89 | `database.read` |
| `database.describeSource` | 0.9.89 | `database.read` |
| `database.listSources` | 0.9.89 | `database.read` |
| `database.query` | 0.9.89 | `database.read` |

**feedback**

| API | מגרסה | הרשאה |
|---|---|---|
| `feedback.hasReporterEmail` | 0.9.97 | — |

**fonts**

| API | מגרסה | הרשאה |
|---|---|---|
| `fonts.listInstalled` | 0.9.97 | `app.info.read` |
| `fonts.resolveFamilies` | 0.9.97 | `app.info.read` |

**fs**

| API | מגרסה | הרשאה |
|---|---|---|
| `fs.abortBinaryWrite` | 0.9.97 | `fs.user_files.write` |
| `fs.beginBinaryWrite` | 0.9.97 | `fs.user_files.write` |
| `fs.commitUserFileWrite` | 0.9.97 | `fs.user_files.write` |
| `fs.deleteEntry` | 0.9.97 | — |
| `fs.deleteFile` | 0.9.93 | — |
| `fs.extractZip` | 0.9.93 | — |
| `fs.listDir` | 0.9.97 | — |
| `fs.makeDir` | 0.9.97 | — |
| `fs.pickUserFile` | 0.9.94 | `fs.user_files.read` |
| `fs.readFile` | 0.9.97 | — |
| `fs.readTextFile` | 0.9.94 | `fs.user_files.read` |
| `fs.resolveFileUrl` | 0.9.94 | `fs.user_files.read` |
| `fs.revokeFile` | 0.9.94 | `fs.user_files.read` |
| `fs.stat` | 0.9.97 | — |
| `fs.writeFile` | 0.9.97 | — |

**history**

| API | מגרסה | הרשאה |
|---|---|---|
| `history.clear` | 0.9.89 | `history.write` |
| `history.list` | 0.9.89 | `history.read` |
| `history.listSearches` | 0.9.89 | `history.read` |
| `history.remove` | 0.9.89 | `history.write` |

**library**

| API | מגרסה | הרשאה |
|---|---|---|
| `library.findBooks` | 0.9.89 | `library.books.read` |
| `library.getBookAltToc` | 0.9.96 | `library.content.read` |
| `library.getBookMetadata` | 0.9.89 | `library.books.read` |
| `library.getCommentators` | 0.9.97 | `library.links.read` |
| `library.getLinkContent` | 0.9.97 | `library.content.read` |
| `library.getLinkTargetsSummary` | 0.9.97 | `library.links.read` |
| `library.getLinks` | 0.9.97 | `library.links.read` |
| `library.getRawLinks` | 0.9.97 | `library.links.read` |
| `library.listBookAltStructures` | 0.9.96 | `library.content.read` |
| `library.listRecentBooks` | 0.9.89 | `library.books.read` |
| `library.refreshUserBooks` | 0.9.97 | `library.refresh` |
| `library.resolveBooks` | 0.9.97 | `library.books.read` |
| `library.resolveCategoryPaths` | 0.9.97 | `library.books.read` |
| `library.resolveRef` | 0.9.97 | `library.books.read` |

**navigation**

| API | מגרסה | הרשאה |
|---|---|---|
| `navigation.goTo` | 0.9.89 | `navigation.write` |

**network**

| API | מגרסה | הרשאה |
|---|---|---|
| `network.download` | 0.9.93 | `network.access` |
| `network.fetch` | 0.9.93 | `network.access` |
| `network.fetchStream` | 0.9.97 | `network.access` |

**notes**

| API | מגרסה | הרשאה |
|---|---|---|
| `notes.add` | 0.9.89 | `notes.write` |
| `notes.delete` | 0.9.89 | `notes.write` |
| `notes.getBookNotesSummary` | 0.9.89 | `notes.read` |
| `notes.list` | 0.9.89 | `notes.read` |
| `notes.update` | 0.9.89 | `notes.write` |

**notifications**

| API | מגרסה | הרשאה |
|---|---|---|
| `notifications.cancel` | 0.9.89 | `notifications.system` |
| `notifications.cancelAll` | 0.9.89 | `notifications.system` |
| `notifications.checkPermissions` | 0.9.89 | `notifications.system` |
| `notifications.requestPermissions` | 0.9.89 | `notifications.system` |
| `notifications.scheduleSystem` | 0.9.89 | `notifications.system` |
| `notifications.sendSystem` | 0.9.89 | `notifications.system` |

**plugin**

| API | מגרסה | הרשאה |
|---|---|---|
| `plugin.listInstalled` | 0.9.97 | `app.info.read` |
| `plugin.openOther` | 0.9.97 | `plugin.open_other` |
| `plugin.requestInstall` | ? | `app.info.read` |

**publishedData**

| API | מגרסה | הרשאה |
|---|---|---|
| `publishedData.listOwn` | 0.9.89 | `published_data.write` |

**reader**

| API | מגרסה | הרשאה |
|---|---|---|
| `reader.activateTab` | 0.9.97 | `reader.open` |
| `reader.addToolbarItem` | 0.9.97 | `reader.toolbar` |
| `reader.clearAllHighlights` | 0.9.89 | `reader.highlight` |
| `reader.clearHighlight` | 0.9.89 | `reader.highlight` |
| `reader.closeTab` | 0.9.97 | `reader.open` |
| `reader.findTextOccurrences` | 0.9.95 | `reader.open` |
| `reader.getActiveCommentators` | 0.9.97 | `reader.open` |
| `reader.getCurrentRef` | 0.9.89 | `reader.open` |
| `reader.getHighlightCapabilities` | 0.9.97 | `reader.open` |
| `reader.getHighlights` | 0.9.89 | `reader.highlight` |
| `reader.getPageShapeLayout` | 0.9.97 | `reader.open` |
| `reader.getSectionTextMap` | 0.9.95 | `reader.open` |
| `reader.getSelection` | 0.9.89 | `reader.open` |
| `reader.openSearchTab` | 0.9.89 | `reader.open` |
| `reader.registerExternalSearchProvider` | 0.9.97 | `reader.open` |
| `reader.registerInBookSearchProvider` | 0.9.97 | `reader.open` |
| `reader.removeContextMenuItem` | 0.9.89 | `reader.context_menu` |
| `reader.removeToolbarItem` | 0.9.97 | `reader.toolbar` |
| `reader.respondExternalSearch` | 0.9.97 | `reader.open` |
| `reader.respondInBookSearch` | 0.9.97 | `reader.open` |
| `reader.revealHighlight` | 0.9.96 | `reader.highlight` |
| `reader.scrollToSection` | 0.9.97 | `reader.open` |
| `reader.setActiveCommentators` | 0.9.97 | `reader.open` |
| `reader.setHighlight` | 0.9.89 | `reader.highlight` |
| `reader.setPageShapeCommentatorVisibility` | 0.9.97 | `reader.open` |
| `reader.updateContextMenuItem` | 0.9.95 | `reader.context_menu` |
| `reader.updateHighlight` | 0.9.95 | `reader.highlight` |
| `reader.updateToolbarItem` | 0.9.97 | `reader.toolbar` |

**search**

| API | מגרסה | הרשאה |
|---|---|---|
| `search.fullText` | 0.9.89 | `search.fulltext.read` |
| `search.getOptions` | 0.9.97 | `search.fulltext.read` |
| `search.query` | 0.9.97 | `search.fulltext.read` |

**settings**

| API | מגרסה | הרשאה |
|---|---|---|
| `settings.get` | 0.9.89 | `settings.read` |
| `settings.getMany` | 0.9.89 | `settings.read` |

**storage**

| API | מגרסה | הרשאה |
|---|---|---|
| `storage.list` | 0.9.89 | `plugin.storage.read` |
| `storage.remove` | 0.9.89 | `plugin.storage.write` |

**tools**

| API | מגרסה | הרשאה |
|---|---|---|
| `tools.dictionary` | 0.9.97 | `tools.read` |
| `tools.gematria` | 0.9.97 | `tools.read` |

**ui**

| API | מגרסה | הרשאה |
|---|---|---|
| `ui.exportPdf` | 0.9.97 | — |
| `ui.pickFolder` | 0.9.93 | `fs.folder_access` |
| `ui.print` | 0.9.97 | — |
| `ui.setUnsavedChanges` | 0.9.97 | — |
| `ui.showMessage` | 0.9.89 | `ui.feedback` |
| `ui.showSuccess` | 0.9.89 | `ui.feedback` |
| `ui.showWarning` | 0.9.89 | `ui.feedback` |

**workspace**

| API | מגרסה | הרשאה |
|---|---|---|
| `workspace.create` | 0.9.97 | `workspace.manage` |
| `workspace.getActive` | 0.9.97 | `workspace.read` |
| `workspace.list` | 0.9.97 | `workspace.read` |
| `workspace.switch` | 0.9.97 | `workspace.manage` |

---

## ה. אירועים שאינם בשימוש (20 מתוך 26)

`app.command` · `calendar.city_changed` · `calendar.date_changed` · `contextMenu.colorClicked` · `contextMenu.itemClicked` · `navigation.changed` · `plugin.page_opened` · `plugin.permissions_changed` · `plugin.ready` · `reader.current_book_changed` · `reader.current_ref_changed` · `reader.inBookSearch.requested` · `reader.sectionContentChanged` · `reader.selection_changed` · `search.external.requested` · `search.requested` · `settings.changed` · `ui.messageClicked` · `workspace.changed`

⚠️ `reader.selection_changed` ו-`reader.current_ref_changed` הם הבסיס הטבעי לזיהוי אוטומטי תוך כדי קריאה, בלי שהמשתמש יבקש.
