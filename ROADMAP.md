# יומן שדרוגים עתידי — עינים למקרא

תמונת מצב של מה שמתוכנן, **לא הבטחה ולא סדר סופי** — סדר הביצוע בפועל תלוי
בהחלטות שוטפות. מבנה זהה ל-[CHANGELOG.md](CHANGELOG.md) (כותרת מודגשת + תבליטים),
רק שהערכים כאן טרם בוצעו. כשמשהו יוצא בפועל — המקום שלו הוא ב-CHANGELOG, לא כאן.

מקורות (המסמכים החיים, לעדכון שוטף): [מפרט-עינים-למקרא-4.0.md](docs/מפרט-עינים-למקרא-4.0.md) ·
[טבלת-סבבים.md](docs/סבבים/טבלת-סבבים.md) · [תוכנית-תוכן-עתידית.md](docs/תוכנית-תוכן-עתידית.md).
נכתב 10/08/2026 — ⚠️ **ייתכן שכבר התיישן** ביחס לשלושת הקבצים האלה; הם המקור הקובע.

---

## מהדורה 4.0 (עמוד הבית והמדריכים) — מה נשאר

**מפרט מלא: מפרט-עינים-למקרא-4.0.md.** א׳, ב.1–ב.4, ג׳ ו-ד׳ כבר בוצעו (ר׳ CHANGELOG
2.15.0 ואילך — כולל ד.1א/ב/ג: שלושת מדריכי הבבלי מומים/חולין/סוכה הם כרטסות
ילידיות מלאות, לא iframe). מה שנשאר מהמפרט, לתכנון בלבד — אין עבודה פעילה כרגע:

* **ה — סריקת מסכת בכורות (הוקדמה מסבב ת׳).** משנה+בבלי בוצעו (״אנשים מהתלמוד״,
  2.19.3 — 167 חכמים/קבוצות, 969 מראי מקום). **נשאר רק הירושלמי של בכורות.**
* **ו.2 — ״ערך היום״ להרחבה עתידית לש״ס** (בבלי וירושלמי, לא רק תנ״ך). נרשם, לא מתוכנן לביצוע מיידי.

## סבב 2 (המשך) — פיצ׳רים שנשארו

* **2.7ב — כרטיס נ״ך.** נביא/כתוב + פרק, באותו דפוס בדיוק כמו כרטיס פרשת השבוע (`parasha.js`) — בלי טבלת מיפוי טווחים, כי בנ״ך יש רק ספר+פרק ישירות.
* **2.10א — ״ערך היום״.** מאורעות התנ״ך לפי תאריך עברי, מחולץ מ״יומא דהילולא״ ומ״ביוגרפיות״ (חילוץ וסינון, לא מחקר תוכן חדש — זה ת.2).
* **2.11 — טעינה עצלה של הדאטה.** 19MB נטענים היום בפתיחה; היעד אינדקס קטן מיד + דאטה לפי דרישה. פותר גם את תקרת גודל התמונות של ת.4/ת.5.
* **2.6 — עץ משפחה אינטראקטיבי.** 🔴 **חסום עד ת.1ב** (מזהה ייחודי לכל רשומה) — ״שם״ אינו ייחודי כיום, ולכן 139 קישורים נצמדים לאדם הלא נכון בעל אותו שם.

## סבב 3 — מנוע הזיהוי v2 (השדרוג הגדול)

⚠️ עדיין בתכנון בפועל, לא מסמך סגור — 3.2/3.3 לא מתחילים בלי אישור על 3.1.

* **3.1 — תכנון** 8 השכבות + סט בדיקות של 60–100 פסוקים.
* **3.2 — יישום** מה שאושר ב-3.1 + כיול מול הסט.
* **3.3 — קציר הלקסיקון המנוקד** מתוך שדות `verses` (כבר מצליח על 1,141 מתוך 1,915).

## סבב 4 (המשך) — דיווחים ובאגים

* 🟡 **4.1 — דיווח באגים בשליחה שקטה מתוך התוסף.** מודאל + תור מקומי (`outbox`) +
  שליחה לממסר שפותח GitHub Issue. בלי מייל ובלי נפילה כשאין רשת.
  **המימוש הושלם ב-2.17.2** — כולל תמונות מצורפות, מניעת כפילויות, וקובץ הורדה
  עצמאי שכתובות השליחה בתוכו. **מאומת מקצה לקצה מדפדפן** (Issue #17, 12.8.26).
  ⚠️ **נשאר תנאי אחד לסגירה: אימות שליחה מתוך אוצריא עצמה.** זה בדיוק מה שנכשל
  עד 2.17.1, ודפדפן אינו מוכיח זאת — ר׳ CHANGELOG 2.17.2. עד שדיווח אחד יגיע
  מתוך האפליקציה, המשימה נשארת פתוחה.
  📝 שינוי מדיניות מודע: ״בלי קישור חיצוני״ בוטל חלקית — כשיש רשת מוצג קישור
  נלחץ לטופס גוגל, למי שאין לו חשבון גיטהאב. כשאין רשת הוא מוחלף בכפתור העתקה.
* **4.5 — איקון ושמירה לעמוד הבית לדף HTML.** לבדוק למה דף HTML מוסף לא נשאר נגיש מעמוד הבית אחרי סגירת אוצריא.
* **4.7 — לשונית ״מה חדש״ באזור האישי.** תקציר ה-CHANGELOG (5 גרסאות אחרונות) בתוך התוסף. **חלקי: הלשונית עצמה כבר קיימת ופעילה** (הקובץ הזה מוטבע בה) — מה שנשאר הוא תקציר-5-גרסאות במקום כל היומן.

### המשך המיגרציה ל-SDK החדש של אוצריא (נרשם 12.8.26, עודכן 19.8.26)

⚠️ **המשפט שהיה כאן — ״הליבה נסגרה ב-2.17.0״ — היה שגוי.** ביקורת מלאה מול קוד
המקור של אוצריא (19.8.26, `docs/תאימות-SDK-אוצריא.md`) מצאה ש-`contributes.startup`
היה מנוטרל לגמרי מ-2.17.1 ואילך, ושפריט תפריט ההקשר לא נרשם בשום מסלול בכל
אוצריא 0.9.96 ומעלה. תוקן ב-2.20.3. **המסמך ההוא הוא המקור לכל הסעיפים כאן.**

* 🔴 **4.14 — PR ל-`plugin_network_allowlist.txt` באוצריא.** אף כתובת רשת של
  התוסף אינה ברשימת ההיתר הרשמית, ולכן בדיקת העדכון וממסר הדיווחים מוחזרים
  היום 403 אצל **כל** המשתמשים — לא ״אין רשת״. **שש הכתובות כבר נכתבו** בקלון
  המקומי של אוצריא (ענף `plugin-allowlist-madaei-hatanach`, שני הקבצים שהבדיקה
  מחייבת). **נשאר: push + פתיחת PR מול `dev`** — עד המיזוג אין רשת בתוסף.
  פירוט מלא ב-`docs/תאימות-SDK-אוצריא.md` §ב.1.
* 🟠 **4.15 — `network.fetchStream` לקריאות הנקדן.** התיעוד דורש שקריאות
  loopback יעברו דרך ה-API ולא דרך `fetch()` ישיר (CORS מול `Origin: null`).
  דורש `callIfSupported` (0.9.97) עם נפילה, ובדיקה מול הנקדן החי. §ב.2.
* 🟠 **4.16 — מעבר לחבילה אחת ב-0.9.98.** כשמסלול הטעינה-בעלייה יימחק, וריאנט
  0.9.97 חייב להפוך לחבילה הראשית — אחרת פריט תפריט ההקשר יירשם רק אחרי
  שהמשתמש פתח את הלשונית באותו סשן. §ב.4.
* 🟢 **4.17 — הדגשות בטקסט (`reader.setHighlight`).** צביעת מה שזוהה בגוף הספר
  עצמו. ה-API שהכי מתאים למהות התוסף ואינו בשימוש כלל. §ג.1.
* 🟢 **4.18 — `plugin.openSelf({param})` + `plugin.page_opened`.** מייתר כמעט
  את כל מנגנון המסירה דרך `storage` + פולינג 1.5 שניות שב-`bridge.js`. §ג.2.
* ⏸️ **4.10 — `contributes.background.entrypoint`: עמוד רקע רזה — ממומש אך מנוטרל (14.8.26).**
  `background.html`/`shell/background.js` קיימים בריפו ועובדים במלואם בסימולציה.
  הוסרו מ-`manifest.json` אחרי דיווח משתמש ב-0.9.97 (״הפריט נמצא, לחיצה לא עשתה
  כלום״). 📌 **עדכון 19.8.26:** הביקורת מצאה סיבה טובה בהרבה לאותו דיווח, שאינה
  קשורה לשדה הזה — פריט תפריט ההקשר לא נרשם אז בשום מסלול (ר׳ §א.1/א.2 בתאימות-
  SDK). כלומר החשד נגד `contributes.background.entrypoint` **לא הוכח**, וסביר
  שהוא תקין. ⚠️ בכל זאת לא הוחזר כאן: התיקון של פריט התפריט צריך להתאמת קודם
  לבדו על מכשיר אמיתי, בלי משתנה נוסף באותה אריזה. אחרי אימות כזה — להחזיר
  `"background": {"entrypoint": "background.html"}` (החיסכון: Leaflet + 480KB
  נתונים גאוגרפיים + 14 מודולי UI בכל הפעלת מנוע רקע). **לא למחוק את הקבצים.**
  `build/pack-bgtest-variant.ps1` בונה בדיוק את הווריאנט הזה לבדיקה.
* **4.11 — `contributes.startup.toolbarItems` בחבילה הראשית.** הכפתור בסרגל
  הקורא כבר נבנה ב-`build/pack-997-variant.ps1`, אבל רק שם — הוא דורש
  `reader.toolbar` + `minAppVersion: 0.9.97`. מה שנשאר: אימות על מכשיר 0.9.97,
  ואז מיזוג לבסיס יחד עם 4.16.
* ⏳ **4.13 — שפת ממשק (i18n).** אוצריא תחזיר `app.language`/`textDirection` אמיתיים
  במקום `he-IL` קבוע, עם דפוס מילון מוטמע (`window.TRANSLATIONS.<lang>`, מפתחות
  בעברית, נפילה טבעית לעברית) ועדכון חי דרך `settings.changed`.
  **תלוי ב-PR #758 שטרם מוזג** (נבדק 12.8.26 — `state: open`, base `dev`), ולכן
  אי-אפשר להצהיר על כלום עדיין.
  **הערכה: כנראה לא שווה לתוסף הזה.** התוכן עצמו הוא תנ״ך וש״ס בעברית; תרגום
  הממשק לבדו לא יהפוך אותו לשימושי לדובר אנגלית, וזו עבודת תוכן עצומה.
  מה שכן ייתכן שכדאי: לכבד `textDirection: 'ltr'` בזמן ריצה, אם וכאשר.

## סבב 5 — ולידציה וסגירת 3.0.0

* **5.2 — תיקון ממצאי ת.0** (= ת.1א): בני המן ← אב, זרש+המן כבני זוג, ועוד ממצאים מהוולידציה.
* ✅ שוחרר ב-2.20.3: **5.3 — GitHub Action לוולידציה על כל commit.** הפרסום עצמו נסגר: `.github/workflows/publish.yml` מריץ בדחיפה ל-main בדיקת סנכרון מוטבעים, ולידציה, קיפול ופרסום לחנות (`Otzaria/otzaria-plugin-validator`) + GitHub Release. מה שנשאר: ריצת ולידציה **בלי פרסום** גם על ענפים/PR-ים, ולבדוק אם הפרסום כבר מעדכן את תיאור התוסף בחנות או שנדרש שלב נפרד.

## סבב ת׳ — תוכן ונתונים (🟢 אושר מראש, 09/08/2026 — מתחיל אחרי סבב 3)

* **ת.1א — תיקון ממצאי ת.0.** 152 מראי מקום שבורים, בני זוג לא הדדיים, ילדים בלי הורה מקביל.
* **ת.1ב — מזהה `id` ייחודי לכל רשומה.** התנאי היחיד לשחרור 2.6 (עץ משפחה).
* **ת.2 — מחקר מאורעות התנ״ך לפי תאריך** (סדר עולם רבה/זוטא, סדר הדורות — נחלת הכלל).
* **ת.3 — העשרה מספרי פליקס**, ניסוח מחדש עם ייחוס בלבד. ⚠️ מוגן בזכויות יוצרים עד 2075 — אסור להעתיק או להעלות את הספרים עצמם לריפו.
* **ת.4 — השלמת תמונות ל-163 ערכים** (דומם 90% חסר, בית המקדש 73%). ~2.5 שעות עבודה. **הוכרע: לאסוף באיכות מלאה בלי כיווץ, מסתמכים על 2.11 (טעינה עצלה) לפתרון משקל החבילה** — 2.11 חייב להיסגר לפני ת.4ג (הטמעה).
* **ת.5 — תצלומי אוויר ל-316 כרטיסי מקומות.** שלב א׳ הוא בדיקת רישוי — ויקישיתוף/USGS מותרים, Google Earth/Maps ו-Bing אסורים. תלויה ב-2.11 (משקל).
  📝 **נבדקו 3 חלופות (9-10.8.26), נרשמות לפני שמכריעים:**
  1. **תצלום סטטי בודדת לכל מקום** (ויקישיתוף/USGS) — ~30-40 דק׳/~30K, אך ~8-14MB לחבילה עצמה.
  2. **שכבת לוויין חיה על מפת ה-Leaflet** (בהשראת התוסף "מקומות+" של אותו כותב — Sentinel-2 cloudless מ-EOX, חינמי, בלי מפתח) — **הזולה ביותר: ~20-30 דק׳/~20-25K טוקנים, 0MB לחבילה** (נטען מהרשת, לא מוטמע). סיכון יחיד: לאמת שטעינת tile חי עובדת באוצריא בלי הצהרת `network.access` נוספת (ב"מקומות+" זה לא הוצהר ועדיין עובד — כנראה כי `<img>`/tile של Leaflet לא נכנס תחת אותה הרשאה כמו `fetch`).
  3. **אריחי לוויין מוטמעים לגמרי (אופליין, בלי רשת בכלל)** — נבדק במפורש: **אין קיצור דרך** מהתוסף "מקומות+" (התיקייה `tiles/` שם היא OSM בלבד, לא לוויין — שם הלוויין תמיד חי). אם רוצים אופליין אמיתי, זו תשתית חדשה: הורדת חבילת אריחים (GitHub Release) → שמירה ב-**IndexedDB** של הדפדפן (לא `plugin.storage` — קטן מדי לעשרות MB) → שכבת `L.GridLayer` מותאמת שקוראת מ-IndexedDB. הסמנים/קישורים הקיימים לא נוגעים (שכבה נפרדת מהאריחים). **הערכה: ~1.5-2 שעות / ~90-100K טוקנים**, בעיקר בניית תשתית ה-IndexedDB+הורדה (אין תקדים בקוד). סיכון: אין וידוא ש-IndexedDB עובד כרגיל בתוך ה-WebView של אוצריא.
  **טרם הוכרע איזו חלופה (אם בכלל) לגבי תצלומי אוויר/לוויין עצמם — נרשם לצורך החלטה עתידית.**
  ✅ **בוצע בנפרד (13.8.26) — תשתית "רזה כברירת מחדל" ל-OSM אופליין (לא לוויין; זו החלופה שנבדקה ונדחתה ב-2 למעלה כ"אין קיצור דרך", אבל אין סיבה לא לתמוך בה כתוספת ידנית):**
  `guides/places/js/map.js` קיבל מתג בסיס תלת-מצבי (מצוירת/OSM אופליין/לוויין), כשכל אחד
  מהשניים האחרונים מופיע רק אם זמין בפועל (`probeTile`). תיקיית `guides/places/tiles/`
  **לא קיימת בריפו ולא נארזת** (`.gitignore`) — מי שרוצה, מעתיק ידנית את `tiles/` מהתוסף
  "מקומות+" (מבנה `{z}/{x}/{y}.png` זהה) לתוך תיקיית ההתקנה של עינים למקרא. הדרכה
  מלאה מוצגת בכפתור "ℹ️" הקבוע ליד המפה (`openOfflineMapHelp` ב-`js/app.js`).
  ⚠️ **לא אומת עדיין בפועל באוצריא** — לא הנתיב המדויק של תיקיות תוספים מותקנים
  (הכפתור מודה בכך במפורש למשתמש), ולא שהעתקה ידנית כזו אכן נטענת נכון על ידי Leaflet
  בתוך ה-WebView. תלוי בבדיקה עם המשתמש/מפתח בפעם הבאה שהוא בודק את התוסף.
* **ת.6 — תקופות מפורטות במדריך אישים** (17 תקופות כמו ב״ביוגרפיות״, חוצות ספר אחד). חלק אחד (עשרת הדורות אדם→נח, שם→אברהם) ניתן לתיוג אוטומטי; השאר עבודת תוכן.
* **ת.7 — סריקה מחדש של התנ״ך והמפרשים לערכים חסרים וכפילויות שם.** דוגמאות שכבר אותרו: ״השסועה״ (רש״י על דברים יד,ז) חסרה במדריך בע״ח; ״צילה״ משמש בתנ״ך לשתי נשים שונות.
* **ת.8 — סינון מקומות לפי שיוך לשבט** (יהושע יג–כא). דורש מיפוי גבולות נחלה לכל אחד מ-316 המקומות לפני שבונים את הצ׳יפ.
* **ת.9 — קטגוריית ״צבעים״ במדריך דומם** (תכלת, ארגמן, תולעת שני, ששר וכו׳).
  ✅ **חומר גולמי כבר קיים** — `docs/תוכן-ממתין-25-08-26/צבעים.txt`.
* **🔴 ת.10 — 5 הערות תוכן ממשתמשים במייל**, כולל אחת דחופה (השקפתית, לא להמתין לסבב ת׳): מחאה על ניסוח ״יצורי אגדה ומציאות״ ועל היפוך ציטוט הערוד (ברכות לג ע״א). פירוט: `docs/הערות-תוכן-ממשתמשים.md`.
* **מ.1 — מפרט ref תלמודי** למסכת+דף+עמוד, תנאי לכל שאר עבודת התוכן התלמודית.
* **מ.2 — סריקת 63 מסכתות התלמוד** ותיוג מקורות לרשומות הקיימות בששת המדריכים.
* **מ.3 — תכנון מדריכים ייעודיים למסכתות נוספות** (עירובין, מנחות, זבחים) — תכנון בלבד בשלב זה.

## סבב 6 — נאסף בשיחת Claude (25/08/26), טרם תוכנן/בוצע

⚠️ רשימה גולמית מדברים שהמשתמש ביקש בסוף שיחת פרסום 2.40.0 — נרשמת כדי לא
לאבד אותה, בלי סדר עדיפויות סופי ובלי אימות בפועל באוצריא.

* **בדיקת לחיצה ימנית ב-0.9.96 בפועל.** התיקון של PR #26 (רישום דרך
  `reader.addContextMenuItem` תמידי, בלי תנאי גרסה) *אמור* לפתור גם את הקריסה
  ההיסטורית מ-2.17 ואת ה"לא עשה כלום" של 2.19.1/0.9.97 — אבל לא אומת על מכשיר.
  צריך בדיקה בפועל, לא רק קריאת קוד.
* **כפתור הסרגל ("👁️ זיהוי") מזהה שם ספר ולא טקסט מסומן** — כנראה כי הסימון
  מתבטל לפני שהאירוע מגיע (ר׳ ניתוח מפורט בהערות `bridge.js` בגרסת החבילה
  החיצונית שלא שולבה — לא נלקח כי `bridge.js` כבר נפתר אחרת ב-PR #26; יש
  להעביר את הרעיון בנפרד אם רוצים את הפיצ׳ר הזה).
  ⚠️ **עדכון 25/08 אחה"צ**: הכפתור הזה (`madaei-hatanach-toolbar-identify`,
  `contributes.startup.toolbarItems`) **חסר לגמרי כרגע ב-2.40.0** — לא היה
  ב-`main`/PR #26 ולא הובא מהחבילה החיצונית (חלק מ-`bridge.js` שדולג עליו,
  ר׳ למעלה). המשתמש מדווח שבגרסה ישנה יותר שיש לו בתיקייה הכפתור כן הופיע
  בסרגל הכלים; לא ידוע אם זה קשור לגרסת אוצריא (0.9.96 מול 0.9.97) או
  שנשמט בטעות. צריך: (1) לבדוק אם `manifest.json` הישן שהיה לו כלל
  `toolbarItems`, (2) להחליט אם להחזיר את הכפתור (ומאיזו גרסת אוצריא), (3)
  אז לטפל גם בבעיית "מזהה שם ספר ולא טקסט מסומן" למעלה.
* **לשונית "ניווט" ישנה שעדיין מופיעה** בממשק — צילום מסך שהמשתמש הזכיר לא
  הגיע בפועל לשיחה; צריך צילום/תיאור מדויק כדי לאתר ולהסיר.
* **מספר גרסה ישן מוצג בלשונית "מה חדש"** — לבדוק אם `version-embedded.js`
  אכן נטען שם (ולא `fetch` חי, שנכשל תמיד ב-WebView — ר׳ מלכודת ידועה ב-CLAUDE.md).
* **המרת שמות קדושים לכינויים בציטוטי פסוקים/קטעים, לפי הגדרות התוסף
  "שומר השם"** (יוסי כהן, `com.yosikohn.shemshomrer`) — כדי שציטוטים בתוסף
  הזה יכבדו את אותה מוסכמה. קובץ התוסף נבדק (25/08 אחה"צ, `names.js`).
  ⚠️ **לא "הגדרות" פשוטות — מנוע זיהוי regex שלם ומורכב**, עם מלכודות
  מתועדות בעצמו (למשל ניקוד כתו נפרד אחרי האות, גבולות-מילה עם תחיליות
  ו/ב/כ/ל/מ/ש/ה) והוראה מפורשת בקובץ: "לפני שינוי בטכניקה הזו — תריץ מחדש
  טסטים". **החלטה: לא להעתיק/לשלב עיוור** — מעתיק-הדבק בלי הרצת אותם טסטים
  עלול לשבור בדיוק את המקרים שהמפתח המקורי כבר תיקן בעבר. דורש שיחה נפרדת
  שמתמקדת רק בזה, עם זמן לבדוק.
* ✅ **תיקון הבנה**: הדגשת שדות שהשתנו בשליחת כרטיס נערך **כבר קיימת** —
  `buildDiff` (`shell/edit-forms.js`, מאז 2.17.2) בונה diff שדה-אחר-שדה
  ("שם: X ← Y" וכו') ושולח אותו תחת כותרת "### מה השתנה" דרך `sendToDev`
  → `sendReport` (`personal.js`) — **אותו גוף הודעה** משמש גם לדיווח מקוון
  (GitHub Issue) וגם לקובץ ההורדה במצב אופליין, כך ששני המסלולים כבר
  כוללים את ה-diff. לא נדרש תיקון.
* ✅ **הוראות-ותכנונים-למפתח מוטמע בחבילה** — `הוראות-ותכנונים-למפתח-מצורף.md`
  בשורש הריפו (לא מוחרג ב-`.otzignore`, נארז אוטומטית) + הפניה בראש
  ROADMAP.md. **נשאר**: כפתור/קישור *לחיצה* בממשק עצמו (כרגע רק טקסט מפנה),
  וסנכרון תוכן "מה מתוכנן" מול הקובץ.
* **גרסה מקבילה "עינים למקרא+" עם מפות אופליין מוטמעות** (משקל חבילה גדול
  משמעותית) — כתחליף/השלמה לגרסה הרזה הרגילה. קשור ל-ת.5 למעלה (תצלומי
  אוויר/מפות) ול-2.11 (טעינה עצלה). איזה קובץ בדיוק כדאי לפצל — טרם נבדק.
* **לחיצה על אייקון "ℹ️" במפה תפתח את מיקום התיקייה** להכנסת אריחי המפה
  (ולא רק להסביר בטקסט). ⚠️ **נבדק 25/08**: אין כרגע קישור GitHub שבור
  בהסבר עצמו (`openOfflineMapHelp` ב-`map.js` מפנה כיום רק בשם לתוסף
  "מקומות+", בלי קישור בכלל) — וגם לא ידועה API של אוצריא לפתיחת תיקייה
  בפועל מתוך פלאגין (`app[.]open_url` פותח כתובות http/https בלבד). צריך:
  (1) קישור GitHub אמיתי ל"מקומות+" אם קיים, (2) לוודא אם יש/אין API
  ייעודי לפני שמנחשים קריאה שעלולה לקרוס.
* **ציר זמן כעץ** (לא רשימה) במדריך אישים ואנשים מהתלמוד — שינוי ויזואלי
  משמעותי, לא טריוויאלי; דורש עיצוב UI חדש, לא רק סידור דאטה.
* **יצוא כרטסת זיהוי/עמוד עם אפשרות בחירה חלקית/מותאמת אישית** (לא רק הכל-או-כלום).
* ✅ **תמיכה ב-PDF חיצוני — תוקן סופית אחרי בדיקה אמיתית של המשתמש.**
  כרטיס ליד מדריך סוכה (`shell/talmud.js`, `MASECHET_TOOLS.סוכה.extraPdf`)
  שפותח PDF חיצוני (`taamu.co.il`). ⚠️ **שרשרת התיקונים המלאה**:
  (1) `Otzaria.call('app[.]open_url', ...)` — שם API לא-קיים, נדחה בוולידציית
  החנות (2.40.3/2.40.4 נכשלו בפרסום). (2) `window.open` רגיל — פורסם
  בהצלחה, אבל **לא נפתח בפועל** (המשתמש בדק, דיווח). (3) **הפתרון האמיתי**:
  הקוד כבר הכיל דפוס עובד ומוכח לקישורים חיצוניים — `data-external-link`
  + `data-requires-net`, מטופל ע"י `confirmOpenExternal` (`shell/refs.js`)
  שקורא ל-`Otzaria.call('app.openUrl', ...)` — **camelCase, לא snake_case**
  — עם פופאפ אישור לפני פתיחה. refs.js עצמו מתעד למה: "לא נותנים ל-WebView
  לנווט ישירות — זה מה שקורס". הכרטיס הועבר לדפוס הזה. תמיכה
  כללית ב-PDF כמדריך מלא (לא רק קישור לצד קיים) — עדיין לא קיימת.
* **קובץ `otzaria-wiki.zim` שהועלה** (תקציר ויקי על אוצריא) — לא נפתח/עובד
  בשיחה זו (פורמט ZIM דורש קורא ייעודי); נשמר להפניה עתידית אם יידרש הקשר
  נוסף על אוצריא עצמו.
* **חומר גולמי חדש ל"דומם" ולתוכן נוסף** — הועלה 25/08 אחה"צ, **לא עובד/לא
  שולב**, רק נשמר: `docs/תוכן-ממתין-25-08-26/` (5 קבצי "כלים" מהתנ״ך/משנה/
  תלמוד עם חשד כפילות חזק ביניהם, טבלת מטבעות/משקלות מפורטת, וטבלת צבעים
  שכבר מתאימה בדיוק ל-ת.9 למעלה, **+ קובץ Word נוסף** — "השלמת-ערכים-מורחבת-
  פי-22.docx", אנציקלופדיה/מילון מורחב לתנ״ך וחז״ל, חופף תמטית לקבצי הכלים).
  ר׳ `README.md` בתיקייה לפירוט מלא ולמה שצריך להיבדק לפני שילוב.
* ✅ **תיקון: קישור "הוראות-ותכנונים-למפתח" לא הופיע בכלל בלשונית "מה
  מתוכנן".** הסיבה: `loadRoadmapBlocks` (personal.js) מציג רק קטעים
  שמתחילים ב-`## ` — הקישור נכתב בפסקת הפתיחה, לפני הכותרת הראשונה, ונופל
  בשקט. תוקן ע"י העברתו לתוך סעיף בפועל. ⚠️ **מגבלה שנשארת**: `mdLiteToHtml`
  (אותו קובץ) לא תומך בתחביר קישורים `[טקסט](url)` בכלל — רק `**מודגש**`
  ורשימות — כך שגם עכשיו זה מוצג כטקסט רגיל, לא קישור לחיצה. תיקון אמיתי
  דורש הוספת תמיכה בקישורים ל-`mdLiteToHtml`, לא רק העברת הטקסט.
* **רעיון נוסף (לא מיושם) — לקשר בין "מה מתוכנן" ל"שליחת הצעה" באזור
  האישי.** כרגע שני הפיצ׳רים נפרדים לגמרי: `personal.js` כבר מכיל תשתית
  מלאה לשליחת הצעות/דיווחים (`REPORT_KINDS`, `sendReport`, כולל סוג
  "הצעה"). הרעיון: ליד כל פריט ב"מה מתוכנן" — כפתור קטן "💡 יש לי רעיון על
  זה" שפותח את פאנל ההצעות עם הכותרת/הקשר כבר ממולאים (כמו deep-link
  פנימי). דורש: (1) לפרק כל בולט ב-ROADMAP למזהה יציב (כרגע רק טקסט חופשי),
  (2) לחבר את הכפתור ל-`openReportPanel` הקיים עם prefill. היקף בינוני,
  לא ממומש הלילה.
